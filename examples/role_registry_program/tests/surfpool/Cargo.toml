[package]
name = "role-registry-program-surfpool-tests"
version = "0.0.0"
edition = "2024"
publish = false

# Keep Surfpool's host runtime isolated from native Mollusk tests and SBF builds.
[workspace]

[dependencies]
pina_test = { path = "../../../../crates/pina_test" }
program_under_test = { package = "role_registry_program", path = "../.." }
