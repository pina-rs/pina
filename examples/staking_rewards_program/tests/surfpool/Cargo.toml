[package]
name = "staking-rewards-program-surfpool-tests"
version = "0.0.0"
edition = "2024"
publish = false

[features]
# Keep the unresolved SEC-26 acceptance test out of normal cargo and Surfpool runs.
audit-deferred = []

[dependencies]
pina_test = { workspace = true }
program_under_test = { package = "staking_rewards_program", path = "../.." }
