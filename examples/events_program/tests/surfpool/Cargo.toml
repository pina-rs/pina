[package]
name = "events_program-surfpool-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
base64 = { workspace = true, default-features = true }
pina_test = { workspace = true }
program_under_test = { package = "events_program", path = "../.." }
