[package]
name = "pina_bpf_program"
version = "0.0.0"
edition.workspace = true
homepage.workspace = true
keywords.workspace = true
license.workspace = true
publish = false
readme = "readme.md"
repository.workspace = true

[lib]
crate-type = ["cdylib", "lib"]

[features]
bpf-entrypoint = []
cpi-runtime-tests = ["dep:prop-amm-program-cpi"]

[dependencies]
pina = { workspace = true, features = ["logs", "derive"] }
prop-amm-program-cpi = { path = "../../codama/clients/cpi/prop_amm_program", optional = true }

[lints]
workspace = true
