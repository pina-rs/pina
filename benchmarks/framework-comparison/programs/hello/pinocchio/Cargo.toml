[package]
name = "hello_pinocchio"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
pinocchio = { version = "0.11.2", default-features = false, features = ["cpi"] }
solana-program-log = { version = "1.2.0", default-features = false }
