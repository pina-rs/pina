{
	"schema_version": "0.6",
	"kind": "monochange.releaseRecord",
	"created_at": "2026-09-23T00:05:33Z",
	"command": "release",
	"version": null,
	"versions": {
		"abi": "0.20.0",
		"core": "0.20.0",
		"pina_root": "0.0.10"
	},
	"release_targets": [
		{
			"id": "abi",
			"kind": "group",
			"version": "0.20.0",
			"version_format": "namespaced",
			"tag": true,
			"release": true,
			"tag_name": "abi/v0.20.0",
			"members": [
				"pina_abi"
			]
		},
		{
			"id": "core",
			"kind": "group",
			"version": "0.20.0",
			"version_format": "primary",
			"tag": true,
			"release": true,
			"tag_name": "v0.20.0",
			"members": [
				"pina",
				"pina_cli",
				"pina_lints",
				"pina_macros",
				"pina_cli_renderer",
				"pina_codama_renderer",
				"pina_cpi_renderer",
				"pina_profile",
				"pina_sdk_ids",
				"pina_test",
				"pina_codama_nodes",
				"pina_codama_renderer_cpi",
				"pina_codama_renderer_cli",
				"pina_cli_npm",
				"pina_cli_darwin_arm64",
				"pina_cli_darwin_x64",
				"pina_cli_freebsd_x64",
				"pina_cli_linux_arm64_gnu",
				"pina_cli_linux_arm64_musl",
				"pina_cli_linux_x64_gnu",
				"pina_cli_linux_x64_musl",
				"pina_cli_win32_arm64_msvc",
				"pina_cli_win32_x64_msvc",
				"pina_skill"
			]
		},
		{
			"id": "pina_root",
			"kind": "package",
			"version": "0.0.10",
			"version_format": "namespaced",
			"tag": false,
			"release": false,
			"tag_name": "pina_root/v0.0.10",
			"members": [
				"pina_root"
			]
		}
	],
	"released_packages": [
		"@pina-rs/cli",
		"@pina-rs/cli-darwin-arm64",
		"@pina-rs/cli-darwin-x64",
		"@pina-rs/cli-freebsd-x64",
		"@pina-rs/cli-linux-arm64-gnu",
		"@pina-rs/cli-linux-arm64-musl",
		"@pina-rs/cli-linux-x64-gnu",
		"@pina-rs/cli-linux-x64-musl",
		"@pina-rs/cli-win32-arm64-msvc",
		"@pina-rs/cli-win32-x64-msvc",
		"@pina-rs/codama-nodes",
		"@pina-rs/codama-renderer-cli",
		"@pina-rs/codama-renderer-cpi",
		"@pina-rs/skill",
		"pina",
		"pina_abi",
		"pina_cli",
		"pina_cli_renderer",
		"pina_codama_renderer",
		"pina_cpi_renderer",
		"pina_lints",
		"pina_macros",
		"pina_profile",
		"pina_root",
		"pina_sdk_ids",
		"pina_test"
	],
	"changed_files": [
		"Cargo.lock",
		"Cargo.toml",
		"changelog.md",
		"crates/pina/Cargo.toml",
		"crates/pina_abi/Cargo.toml",
		"crates/pina_abi/changelog.md",
		"crates/pina_cli/Cargo.toml",
		"crates/pina_cli_renderer/Cargo.toml",
		"crates/pina_codama_renderer/Cargo.toml",
		"crates/pina_cpi_renderer/Cargo.toml",
		"crates/pina_lints/Cargo.toml",
		"crates/pina_macros/Cargo.toml",
		"crates/pina_profile/Cargo.toml",
		"crates/pina_sdk_ids/Cargo.toml",
		"crates/pina_test/Cargo.toml",
		"packages/codama-renderer-cli/package.json",
		"packages/codama-renderer-cpi/package.json",
		"packages/nodes-from-pina/package.json",
		"packages/pina__cli/package.json",
		"packages/pina__cli-darwin-arm64/package.json",
		"packages/pina__cli-darwin-x64/package.json",
		"packages/pina__cli-freebsd-x64/package.json",
		"packages/pina__cli-linux-arm64-gnu/package.json",
		"packages/pina__cli-linux-arm64-musl/package.json",
		"packages/pina__cli-linux-x64-gnu/package.json",
		"packages/pina__cli-linux-x64-musl/package.json",
		"packages/pina__cli-win32-arm64-msvc/package.json",
		"packages/pina__cli-win32-x64-msvc/package.json",
		"packages/pina__skill/package.json",
		"pnpm-lock.yaml"
	],
	"package_publications": [
		{
			"package": "pina",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_abi",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_darwin_arm64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_darwin_x64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_freebsd_x64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_linux_arm64_gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_linux_arm64_musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_linux_x64_gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_linux_x64_musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_npm",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_renderer",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_win32_arm64_msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cli_win32_x64_msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_codama_nodes",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_codama_renderer",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_codama_renderer_cli",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_codama_renderer_cpi",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_cpi_renderer",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_lints",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_macros",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_profile",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_sdk_ids",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_skill",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			}
		},
		{
			"package": "pina_test",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.20.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"mode": "required",
				"repository": "pina-rs/pina",
				"workflow": "publish.yml",
				"environment": "publisher"
			},
			"timeout": {
				"timeout_seconds": 900,
				"retries": 2
			}
		}
	],
	"updated_changelogs": [
		"changelog.md",
		"crates/pina_abi/changelog.md"
	],
	"deleted_changesets": [
		".changeset/abi-version-reset.md",
		".changeset/adopt-pinapod-0-4-3.md",
		".changeset/align-advisory-gates.md",
		".changeset/ata-address-check-cost-docs.md",
		".changeset/checked-discriminator-write.md",
		".changeset/cli-client-explorer-and-keypair.md",
		".changeset/cli-renderer-fix.md",
		".changeset/codama-nodes-contract-test.md",
		".changeset/const-capacities.md",
		".changeset/cpi-scaffold-workspace-dependency.md",
		".changeset/deduplicate-ata-derivations.md",
		".changeset/deploy-stdin-test-timing.md",
		".changeset/driver-spawn-retries-on-busy.md",
		".changeset/drop-bytemuck-default-features.md",
		".changeset/escrow-empty-offer-and-maker-writable.md",
		".changeset/event-record-stack-budget.md",
		".changeset/js-migrate-type-arguments.md",
		".changeset/lint-driver-download-integrity.md",
		".changeset/lockfile-and-tooling-hygiene.md",
		".changeset/migrate-trigger-matches-discriminator-width.md",
		".changeset/migration-auto-schema-enum.md",
		".changeset/migration-transition-offsets.md",
		".changeset/multisig-example-and-cli-fixes.md",
		".changeset/opt-in-heap-entrypoint.md",
		".changeset/pda-qualified-address.md",
		".changeset/pda-seed-count-cap.md",
		".changeset/pina-generate-all-clients.md",
		".changeset/rename-migrations-make-to-create.md",
		".changeset/rename-stored-bump-pda-loader.md",
		".changeset/sanitize-idl-names-in-docs.md",
		".changeset/scaffold-release-line-test.md",
		".changeset/security-audit-examples.md",
		".changeset/security-audit-framework.md",
		".changeset/security-audit-supply-chain.md",
		".changeset/security-disclosure-channel.md",
		".changeset/surfpool-1-6-agave-4-2.md",
		".changeset/sweep-security-fixes.md",
		".changeset/validate-cargo-library-name.md"
	],
	"changesets": [
		{
			"path": ".changeset/abi-version-reset.md",
			"summary": "Version the ABI document on `pina_abi`'s own release line",
			"details": "`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.",
			"targets": [
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "4ae83fa4fb74e1a1c08351abadeb2c79d999fe65",
						"short_sha": "4ae83fa",
						"url": "https://github.com/pina-rs/pina/commit/4ae83fa4fb74e1a1c08351abadeb2c79d999fe65",
						"authored_at": "2026-09-20T00:58:14+01:00",
						"committed_at": "2026-09-20T00:58:14+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#468",
						"title": "feat(pina_abi)!: version the ABI document on its own release line",
						"url": "https://github.com/pina-rs/pina/pull/468",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/adopt-pinapod-0-4-3.md",
			"summary": "Adopt the Pinapod 0.4.3 compact-writer hardening",
			"details": "Move from the temporary `pinapod` 0.4.1 hold to 0.4.3, which brings the 0.4.2 commit-entry hardening at its intended compute-unit cost. 0.4.2 validates every generated compact buffer at `commit` before tails are relocated: a writer whose layout is corrupt fails closed before any offset or pointer work, and a preflight and commit length disagreement returns `InvalidLength` in release builds instead of relying on a debug assertion. By default, semantic defects such as non-UTF-8 tail bytes or out-of-range tags are caught at the next interpreting boundary rather than by `commit` itself; the `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it. 0.4.3 keeps all of this and restores the compute-unit profile.\n\nThe intermediate 0.4.2 release was held back on a diagnosis that turned out to be wrong: Pina reported that 0.4.2 moved `commit` ahead of the inline writes and turned a length check into an unconditional panic. Neither was true — the panic-shaped block was 0.4.1's own `debug_assert_eq!` expansion, and `update` never changed order. [pinapod#36](https://github.com/pina-rs/pinapod/issues/36) recorded the correction and [pinapod#38](https://github.com/pina-rs/pinapod/issues/38) specified how to keep the safety without the cost, which is what 0.4.3 implements.\n\n`commit` entry now proves layout rather than semantics through `PinaPodCompact::validate_layout`: the storage length, each stored length prefix, its capacity, and the chained bounds. Relocation's pointer arithmetic consumes exactly those, so this is the complete precondition and not a weakening of it. The element walks that checked UTF-8, enum ranges, and tags move to the boundaries that interpret those bytes, which already had them — the `Ref` and `Mut` constructors, `updated_len`, and `try_initialize`'s post-commit check. Trivially valid element types such as `u64` and `u8` skip their walks entirely rather than relying on the compiler to delete a dead loop, which SBF at `-C opt-level=3` does not reliably do.\n\nAn account that is layout-valid but semantically invalid, such as non-UTF-8 bytes in an untouched tail, now commits and is rejected at the next read instead of by `commit` itself. The update path keeps its existing guarantee by construction, because `updated_len` runs the full validation over the same bytes the staged edits will produce. A `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it.\n\nThe observable effect on Pina is the regenerated `pda.expanded.rs` snapshot, and the compute units that 0.4.2 added are gone: the `compact_accounts_program` instructions that regressed by 46 to 308 units under 0.4.2 measure at or below their 0.4.1 cost again.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "6d21bd1effc59585add7f5b764c69afda643c1d2",
						"short_sha": "6d21bd1",
						"url": "https://github.com/pina-rs/pina/commit/6d21bd1effc59585add7f5b764c69afda643c1d2",
						"authored_at": "2026-09-22T00:38:20+01:00",
						"committed_at": "2026-09-22T00:38:20+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#485",
						"title": "fix(deps): adopt Pinapod 0.4.3 — commit-entry hardening without the CU cost",
						"url": "https://github.com/pina-rs/pina/pull/485",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#38",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/38",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#39",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/39",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/align-advisory-gates.md",
			"summary": "Align the advisory gates on deny.toml",
			"details": "`security:deny` ran `cargo-deny check bans licenses sources`, so the `[advisories]` section of `deny.toml` — and the four suppressions it documented — never executed. The task now runs the advisories check with `--workspace`, because cargo-deny's default graph roots at the workspace members' normal dependencies and therefore omits the dev/test stack (surfpool, litesvm, mollusk, solana-runtime) where every suppressed advisory actually lives. Without `--workspace` the check cannot see them at all.\n\n`security:audit` carried a second, different seven-id ignore list. `deny.toml` is now the single source of advisory policy: the task reads the `[advisories] ignore` array out of `deny.toml` and passes each id to `cargo-audit`, so the two gates cannot drift. Every suppression in that array carries a reachability justification and a review date, and each needs a tracking issue filed on the repository — the entries are all dev/test-only today, verified with `cargo tree -i <spec> -p pina -p pina_abi -p pina_macros -p pina_cli` and the `--workspace` cargo-deny run, but a file cannot open issues on its own.\n\nThe `[advisories]` section also needed the `RUSTSEC-2021-0139`, `RUSTSEC-2024-0375`, `RUSTSEC-2024-0384`, `RUSTSEC-2020-0016`, `RUSTSEC-2025-0134`, and `RUSTSEC-2026-0173` entries that only the old cargo-audit list had, so moving advisories into the deny gate does not weaken it.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/ata-address-check-cost-docs.md",
			"summary": "Document the cost of the ATA address checks",
			"details": "`assert_associated_token_address` and `as_associated_token_account` each perform a canonical associated-token-address search, which costs on the order of a thousand compute units and grows with how far the address's canonical bump sits below 255. Both doc comments now state that cost and warn against calling the assert and the loader with the same wallet, mint, and token program in one handler, because the second call repeats a search the first already paid.\n\nThe guidance consolidates what two example handlers (`vesting_program`'s `Claim` and `Cancel`) had drifted into — each paid the search twice per instruction until the companion example change removed the redundant calls and recorded the measured savings. The same audit confirmed `escrow_program`'s `Take` derives its `taker_ata_b` exactly once, so its search is not redundant and stays. Docs-only change; no behavior, wire, or measurement impact.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "3886af957ecfa0c89cb8878b9bd619fea3a527fa",
						"short_sha": "3886af9",
						"url": "https://github.com/pina-rs/pina/commit/3886af957ecfa0c89cb8878b9bd619fea3a527fa",
						"authored_at": "2026-09-22T12:58:46+01:00",
						"committed_at": "2026-09-22T12:58:46+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#488",
						"title": "docs(pina): record the ATA address checks' derivation cost",
						"url": "https://github.com/pina-rs/pina/pull/488",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#487",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/487",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/checked-discriminator-write.md",
			"summary": "Add a checked discriminator write",
			"details": "`IntoDiscriminator::write_discriminator` returns `()` and drops the write when the destination slice is shorter than the discriminator: a `debug_assert!` fires in debug builds, but a release build silently leaves the buffer untouched. Generated code always sizes its buffers from `Self::BYTES`, so this only bit hand-written implementations, where a missing discriminator produces an account that looks initialized but decodes as an unknown type.\n\nThe new `IntoDiscriminator::try_write_discriminator` reports the undersized buffer with `PinaProgramError::DataTooShort` (0xFFFF_FFFA, \"Account or instruction data is shorter than the expected minimum\") instead. That is the variant `HasMigrationVersion::write_le` and the migration decoders already return for a destination that cannot hold the value, so a caller sees one error for every short-buffer case.\n\nThe method has a default implementation that length-checks and then delegates to `write_discriminator`, so a manual implementer inherits the checked path without writing anything; the primitive implementations (`u8`, `u16`, `u32`, `u64`) and `into_discriminator!` override it to write through `get_mut` in one step. `write_discriminator` is unchanged, and its doc comment now points new manual implementers at the checked form.\n\nTests cover short buffers of every length up to `BYTES` (including a short inner slice of a long array), exact and oversized buffers, and that the checked write produces the same bytes as the unchecked one.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/cli-client-explorer-and-keypair.md",
			"summary": "Fix CLI Explorer links and keypair validation",
			"details": "The Dart CLI renderer printed an Explorer URL without a cluster selector, so a signature from a devnet or testnet run linked to the mainnet Explorer page. It now derives the selector from the resolved endpoint and appends `?cluster=devnet` or `?cluster=testnet`. Mainnet Beta and arbitrary custom endpoints stay bare, because the Explorer only understands the named clusters and a guessed selector for an unknown host would point at the wrong chain. The match is on the parsed hostname rather than a substring, so a custom endpoint that merely contains a cluster's host as a path or subdomain is not mislabeled.\n\nBoth renderers also accepted a keypair array of 64 values without checking the values themselves. `Uint8Array` and `Uint8List` wrap out-of-range bytes rather than rejecting them, so a malformed keypair file produced a different signer instead of an error — one that would simply fail every transaction with no explanation. Each byte must now be an integer from 0 to 255, and anything else fails with the existing keypair-loading error.",
			"targets": [
				{
					"id": "pina_codama_renderer_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "de21f558297c88371b7f68a892c03eb5af23cea7",
						"short_sha": "de21f55",
						"url": "https://github.com/pina-rs/pina/commit/de21f558297c88371b7f68a892c03eb5af23cea7",
						"authored_at": "2026-09-19T22:46:26+01:00",
						"committed_at": "2026-09-19T22:46:26+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#465",
						"title": "feat(entrypoint): add an opt-in heap allocation macro",
						"url": "https://github.com/pina-rs/pina/pull/465",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#462",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/462",
						"relationship": "closed_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/cli-renderer-fix.md",
			"summary": "Fix generated CLI payer and PDA handling",
			"details": "The generated TypeScript, Dart, and Rust CLIs had related defects rooted in the renderers. Fixed-seed PDA accounts could not be fetched from the TypeScript or Dart CLIs: `--address` was mandatory even though the help text promised PDA derivation, while accounts without any PDA node kept requiring it. The Dart `fetch` command also registered no subcommands, so `fetch <account>` could never dispatch.\n\nPayer-resolution accounts (accounts defaulted to the transaction payer) accepted an override option in every CLI, but the override was either silently ignored (TypeScript) or produced a transaction whose signer meta pointed at an address no loaded keypair signs for (Dart and Rust). Since these CLIs sign with exactly one keypair, all three renderers now resolve payer-resolution accounts to the loaded payer and stop emitting the unusable override options.",
			"targets": [
				{
					"id": "pina_cli_renderer",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_codama_renderer_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "88820f04c49ce4d5e2474fcc3bda4afe84c6dd60",
						"short_sha": "88820f0",
						"url": "https://github.com/pina-rs/pina/commit/88820f04c49ce4d5e2474fcc3bda4afe84c6dd60",
						"authored_at": "2026-09-22T04:24:59+01:00",
						"committed_at": "2026-09-22T04:24:59+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#486",
						"title": "feat(examples): add the tiered-disclosure privacy pool program",
						"url": "https://github.com/pina-rs/pina/pull/486",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/codama-nodes-contract-test.md",
			"summary": "Pin the client contract test to the released surfaces",
			"details": "The vesting and staking clients gained accounts in this release (the claim clock sysvar and refund path, the staking reward vault), so the package's contract test pins the new slot counts and roles. Test-only; no release note.",
			"targets": [
				{
					"id": "pina_codama_nodes",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/const-capacities.md",
			"summary": "Accept const capacities in schemas",
			"details": "Compact account and instruction schemas now accept a `const` item wherever they previously required an integer literal:\n\n```rust\nconst MAX_MEMBERS: usize = 24;\n\n#[account(discriminator = MultisigAccountType, compact)]\npub struct Multisig {\n\tpub bump: u8,\n\tpub member_keys: Vec<Address, MAX_MEMBERS>,\n}\n```\n\n`Vec<T, N>`, `PodVec<T, N, PFX>`, `String<N>`, `PodString<N, PFX>`, an `Option<...>` around them, and fixed `[T; N]` arrays and instruction arguments all resolve a capacity through a constant. Constants may be arithmetic over other constants and may be declared in any module of the crate, so one bound is declared once and reused in the account, the instruction that writes it, and the helper that sizes it.\n\nPina resolves each capacity during expansion and records the number it evaluates to. The ABI layer still receives concrete values, so replacing a literal with a constant of the same value leaves `migrations/manifest.json`, the generated `tests/abi_layout.rs` assertions, `MAX_SIZE`/`MIN_SIZE`/`HEADER_SIZE`, and `projected_bytes(...)` byte-identical, and `pina migrations check` and the Codama IDL unchanged.\n\nA capacity that cannot be evaluated at expansion time now fails the build with a diagnostic naming the expression and pointing at the workaround, instead of the previous `unsupported compact field` or `arrays require an integer literal length` message. An associated constant such as `Bounds::MAX_MEMBERS` is not resolved; declare the bound as a `const` item.\n\n`pina_abi` gains `SchemaConsts`, the shared evaluator both the macros and the CLI use so the two layers agree on what a constant means. `pina_cli` resolves capacities while it parses a program, so migration and IDL tooling reads the same numbers the compiler does.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b1aaa533e5deb9bb512e0394dcf5d39b170d92c4",
						"short_sha": "b1aaa53",
						"url": "https://github.com/pina-rs/pina/commit/b1aaa533e5deb9bb512e0394dcf5d39b170d92c4",
						"authored_at": "2026-09-19T14:55:03+01:00",
						"committed_at": "2026-09-19T14:55:03+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#463",
						"title": "feat(schemas): accept const capacities in account and instruction schemas",
						"url": "https://github.com/pina-rs/pina/pull/463",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#460",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/460",
						"relationship": "closed_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/cpi-scaffold-workspace-dependency.md",
			"summary": "Fix the CPI scaffold's pina dependency for workspace crates",
			"details": "`pina generate` scaffolds a CPI crate for every example, but the scaffold hardcoded `pina = { version = \"0.17\", default-features = false }` — a registry pin that stopped matching this workspace's own `pina` releases. Every previously committed CPI crate had already been pinned to `pina = { workspace = true }` by hand, so the defect only surfaced on the next newly generated example: it added a second `pina` to the dependency graph, which makes `cargo kani -p pina` fail with \"multiple `pina` packages in your project\" and breaks both Kani proof tiers.\n\nThe scaffold now takes the dependency source from the caller. `pina generate`, whose output is always a workspace member, inherits the workspace dependency; `pina cpi` and `pina import`, whose output lives in the caller's own project with no workspace entry to inherit, pin the renderer's own release line instead of a stale literal. A regression test covers both modes.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_cpi_renderer",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b0106aa564ab5badb6ae64ca84f84b3b0c0c9dab",
						"short_sha": "b0106aa",
						"url": "https://github.com/pina-rs/pina/commit/b0106aa564ab5badb6ae64ca84f84b3b0c0c9dab",
						"authored_at": "2026-09-22T06:00:36+01:00",
						"committed_at": "2026-09-22T06:00:36+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#489",
						"title": "fix(cli): inherit the workspace pina in the scaffolded CPI crate",
						"url": "https://github.com/pina-rs/pina/pull/489",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#486",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/486",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/deduplicate-ata-derivations.md",
			"summary": "Drop duplicated ATA derivations from hot paths",
			"details": "The canonical associated-token-address search costs roughly 1,500 CU per call on Solana because every attempt re-hashes the seeds and re-runs the point-decompression check, and the total scales with how far the address's canonical bump sits below 255. Two handlers paid that search twice for the same account in one instruction:\n\n- `vesting_program`'s `Claim` asserted the vault was the schedule's associated token account in the validation chain and then derived the identical `(wallet, mint, token program)` tuple again when it read the vault balance. The chain assert is gone; the balance load (`as_associated_token_account`) calls the exact same address validation and still rejects a caller-supplied account, which is the property that stops the schedule's signature from draining a foreign vault. The check runs before any state mutation or CPI.\n- `vesting_program`'s `Cancel` had the same assert-then-load pair for the refund vault. The chain assert is gone and the refund-balance load is the vault's only derivation, again ahead of the `cancelled` write and every CPI.\n\nMeasured on the Surfpool simulation maximum (the same statistic the performance policy enforces): `vesting_program/claim` 22,202 to 20,642 (-1,560, -7.0%) and `vesting_program/cancel` 29,281 to 27,722 (-1,559, -5.3%). Failure attribution is preserved: the surviving load calls the identical `assert_associated_token_address` the removed chain step called, so a substituted vault still fails with `InvalidSeeds`.\n\n`escrow_program`'s `Take` was audited for the same shape and deliberately left alone: its `taker_ata_b` derivation is the account's only canonical pin (the handler's `CreateIdempotent` CPI derives the maker's ATA, and the transfer CPI only proves signer authority), so removing it would let a taker pay from any mint-B account they control. Two adversarial Surfpool cases added here prove both sides: a foreign vault is rejected by both vesting handlers with no state change, and `Take` rejects both a foreign and a funded non-canonical taker-controlled token-B account.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b30699e11d72d8625ef3ee292a2a5198e9365baf",
						"short_sha": "b30699e",
						"url": "https://github.com/pina-rs/pina/commit/b30699e11d72d8625ef3ee292a2a5198e9365baf",
						"authored_at": "2026-09-22T12:57:48+01:00",
						"committed_at": "2026-09-22T12:57:48+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#487",
						"title": "perf(examples): derive each ATA once in the vesting and escrow hot paths",
						"url": "https://github.com/pina-rs/pina/pull/487",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/deploy-stdin-test-timing.md",
			"summary": "Stop the deploy stdin test racing a startup deadline",
			"details": "The test that proves `solana` inherits a closed stdin polled the child against a three-second deadline, which Pina's `cargo metadata` project discovery exceeded on a loaded runner. It now waits for the marker the fake `solana` writes after reading EOF and keeps a thirty-second deadline purely as a hang bound. Test-only; no release note.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "3b45d969ba4754adb223c5fcc093676dfd9e3f5d",
						"short_sha": "3b45d96",
						"url": "https://github.com/pina-rs/pina/commit/3b45d969ba4754adb223c5fcc093676dfd9e3f5d",
						"authored_at": "2026-09-19T19:47:28+01:00",
						"committed_at": "2026-09-19T19:47:28+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#469",
						"title": "test(pina_cli): fix the deploy stdin test's false hang detection",
						"url": "https://github.com/pina-rs/pina/pull/469",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/driver-spawn-retries-on-busy.md",
			"summary": "Retry the lint driver probe while the kernel reports it busy",
			"details": "`pina lint` starts the driver it just installed to check that it loads. The kernel refuses an `exec` while a write descriptor for the same file is still open — including one a concurrent fork inherited — and the install-then-probe sequence can overlap with that window, so the probe failed with a busy error on a driver that was perfectly runnable. The spawn now retries briefly for that one error and fails immediately for anything else, which also stops the driver tests from flaking under a parallel test run.\n\n`pina` carries no behavior change in this release line; the entry records the added test coverage for the public `into_discriminator!` macro.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "97908338f3733197db9b394fce93545a1cf49322",
						"short_sha": "9790833",
						"url": "https://github.com/pina-rs/pina/commit/97908338f3733197db9b394fce93545a1cf49322",
						"authored_at": "2026-09-19T14:52:25+01:00",
						"committed_at": "2026-09-19T14:52:25+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#461",
						"title": "fix: repair the post-merge program e2e suites and the driver spawn race",
						"url": "https://github.com/pina-rs/pina/pull/461",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#457",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/457",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/drop-bytemuck-default-features.md",
			"summary": "Stop enabling `bytemuck` on `solana-address`/`solana-pubkey`",
			"details": "The workspace requested `solana-address/bytemuck` and `solana-pubkey/bytemuck`. Pina used `bytemuck` for its own pod primitives until the migration to `pinapod`, so nothing in the workspace reads the `Pod` or `Zeroable` derives those features add, and no first-party code calls a `bytemuck` API.\n\nBoth entries now request what Pina actually depends on. `solana-address` asks for `copy`, which derives `Copy` on `Address` — the bound `pinapod`'s `ZcElem` requires and the only part of `bytemuck`'s feature set that was load-bearing. Requesting it directly removes the dependence on `bytemuck`'s implication, and on `pinocchio` and `pinapod` enabling `copy` transitively. `solana-pubkey` no longer requests `bytemuck` at all.\n\nDeployable programs are unchanged: `bytemuck` contributed no symbols to any built program, so all 24 `bpf-entrypoint` examples produce byte-identical sizes and an identical instruction mix with and without it. This is a dependency-graph cleanup, not a size or compute-unit change.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "36c1a113a2baa3f2001bc5721046d3ee9f5c8c73",
						"short_sha": "36c1a11",
						"url": "https://github.com/pina-rs/pina/commit/36c1a113a2baa3f2001bc5721046d3ee9f5c8c73",
						"authored_at": "2026-09-19T13:28:49+01:00",
						"committed_at": "2026-09-19T13:28:49+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#464",
						"title": "chore(deps): stop enabling bytemuck on solana-address/solana-pubkey",
						"url": "https://github.com/pina-rs/pina/pull/464",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/escrow-empty-offer-and-maker-writable.md",
			"summary": "Reject empty escrow offers and prove the maker is writable",
			"details": "`escrow_program`'s `Make` handler now rejects an offer whose `amount_a` or `amount_b` is zero with a new `EscrowError::EmptyOffer` (code 2), before any account is created or any token moves. A zero `amount_b` let a maker escrow token A for nothing, and a zero `amount_a` let a taker receive the vault for nothing; both are fat-finger shapes rather than real offers. The variant is new rather than a reuse of `OfferKeyMismatch` or `TokenAccountMismatch`, because wire values are part of the program ABI and remapping an existing code would change what deployed clients decode. The doc comment on the variant becomes the message in the regenerated Codama IDL and the Rust, JavaScript, and Dart clients.\n\n`Take` now asserts the maker is writable next to the existing address check, so a read-only maker fails with Pina's own `InvalidAccountData` diagnostic instead of a generic runtime failure from the vault or escrow close CPIs. The `&mut AccountView` field already enforced this at parse time, so the assert is defense in depth: the test passes with it removed. It costs 16 CU on `Take` (32,241 to 32,257), which is the price of keeping the requirement visible in the handler, and the maker is credited twice there (vault rent and escrow rent).\n\nTwo Surfpool cases cover the behavior: one asserts `EmptyOffer` and that no balance moves and no account is created, and one presents a read-only maker through a non-payer keypair and asserts the failure plus unchanged escrow, vault, and taker balances.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/event-record-stack-budget.md",
			"summary": "Reject event schemas that cannot fit the SBF stack",
			"details": "The generated `emit` function materializes a whole event record in one stack frame (`let mut record = [0u8; Self::SIZE];`) before calling into the runtime, so a large `#[event]` schema compiled cleanly and then exhausted the 4 KiB SBF stack at runtime — a self-inflicted denial of service with no compile-time signal.\n\n`pina` gains `MAX_EVENT_RECORD_BYTES`, set to `4096 - 512`. The 512-byte reserve is headroom for the callee frame and the caller's own frame, the same reasoning `MAX_MIGRATION_WORKSPACE` already applies to the historical-normalization workspace. Because the constant is public, the bound a caller can read is the bound the macro enforces.\n\n`pina_macros` emits a compile-time assertion in every `#[event]` expansion, so an oversized schema now fails the build with a message naming the struct and the remedy. A record exactly at the bound still compiles; the assertion is inclusive.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/js-migrate-type-arguments.md",
			"summary": "Emit valid TypeScript for reserved Migrate type arguments",
			"details": "The reserved `Migrate` composer closed its `MigrateInput<...>` type argument list with a trailing comma. That is a `TS1009` syntax error — trailing commas are legal in type parameter _declarations_ but not in type argument _uses_ — so raw `pina generate` output could not be parsed by `tsc`, `esbuild`, or any tool stricter than dprint's formatter. Repository pipelines only survived because `verify-codama-idls.sh` runs `dprint fmt` over the generated tree, silently deleting the comma before anything parsed the file. A consumer running `pina generate` without that reformatting step got uncompilable clients.\n\nThe generator now emits the comma-free shape dprint normalizes to, so committed clients are unchanged and raw output is valid TypeScript. Caught by the migrations walkthrough, which loads the freshly generated client through esbuild without a formatting pass.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "2b17b43a7afcccbef2dcc17df6b26c8b9c34dbf8",
						"short_sha": "2b17b43",
						"url": "https://github.com/pina-rs/pina/commit/2b17b43a7afcccbef2dcc17df6b26c8b9c34dbf8",
						"authored_at": "2026-09-21T04:48:57+01:00",
						"committed_at": "2026-09-21T04:48:57+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#481",
						"title": "fix(pina_cli): read transition offsets from the stored schema",
						"url": "https://github.com/pina-rs/pina/pull/481",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/lint-driver-download-integrity.md",
			"summary": "Verify the lint driver download before installing it",
			"details": "`pina lint` fetches a prebuilt driver binary, marks it executable, and runs it as its version probe. The download trusted the transport alone: no checksum was verified, the response body was read without a size bound, and the origin could be redirected through `PINA_LINT_DRIVER_*` environment variables.\n\nThe download path now fetches the `sha256` published beside the asset, compares it against the bytes received, and refuses to install on mismatch, a malformed checksum, or a checksum file that is not valid UTF-8. A new `VerifiedDriver` type wraps the bytes so only a checksum-verified download can reach the installer — a future edit cannot silently reintroduce an unverified path, because the compiler rejects it. The body read is capped at 256 MiB, and the release workflow now uploads the standalone driver's `.sha256` beside the binary, which it previously did not (only the archives carried published checksums).\n\nA locally built driver (`pina lint --build-driver`) keeps its existing trust root — a cargo build against the pinned `pina_lints` release — and installs through a separate function, since no published checksum exists for an artifact built on the local machine.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/lockfile-and-tooling-hygiene.md",
			"summary": "Fix the IDL size message and sanitize doc topics",
			"details": "`pina idl fetch` rejected raw client output above 8 MiB but reported the limit as \"4 MiB\". The check and the message now share one constant, `MAX_RAW_HEX_BYTES`, so the two cannot drift again, and the message states the true 8 MiB limit.\n\n`pina docs <topic>` joined the caller-supplied topic directly into `<PINA_TEMPLATES_DIR>/<topic>.t.md`, so a topic such as `../../etc/passwd` escaped the template directory. Only the file-name component of the topic is now used, and a topic with no file-name component fails with a clear error.\n\nThe unused `tar` dependency is gone from `pina_cli` and the workspace dependency table; nothing in the crate referenced it, and the only other lockfile consumer is `agave-snapshots`, which declares it directly.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/migrate-trigger-matches-discriminator-width.md",
			"summary": "Match the Migrate trigger to the discriminator width",
			"details": "`#[discriminator]` reserves the all-ones value of the enum's own primitive, so a `u16` program reserves `0xffff` and a `u32` program reserves `0xffff_ffff`. The generated entrypoint guarded the reserved path with `is_migrate_instruction`, which only tests one byte, so for any program whose instruction enum used `primitive = u16`, `u32`, or `u64` the reserved discriminator never matched and `process_migrate` was unreachable.\n\n`pina` adds width-specific helpers alongside the existing one: `is_migrate_instruction_u16`, `is_migrate_instruction_u32`, and `is_migrate_instruction_u64`. Each matches only its own width, so a one-byte `0xff` is not a two-byte `0xffff` and neither prefix-matches the other. They compare the decoded little-endian value rather than a slice equality, keeping the inline comparison the one-byte helper already had instead of the `memcmp` call `data == CONSTANT.to_le_bytes()` would emit on SBF.\n\n`pina_macros` selects the helper from the enum's `primitive`, so `#[discriminator(entrypoint, migrations(...))]` on a wider discriminator now reaches the reserved path. The generated guard for a default `u8` program is byte-identical to before, so the common case takes no compute regression. The documentation on the reserved-discriminator constants and in `docs/src/migrations/flow.md` now names the width pairing.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/migration-auto-schema-enum.md",
			"summary": "Enumerate the auto policy's kind names in the schema",
			"details": "`MigrationAuto` has hand-written serde impls, so its JSON Schema is hand-written too, and it described its items as any string. A consumer validating a manifest against the published schema therefore admitted `[\"states\"]` while the reader rejected it with `unknown migration kind` — the schema promised more than the code accepted, which defers the failure to runtime.\n\nThe items now carry the valid names, read from `ContractKind::config_name`, the same source `from_config_name` resolves through, so the schema and the reader cannot drift apart. The checked-in artifacts under `crates/pina_abi/schemas/`, the frozen fixtures, and the published copies under the book are regenerated to match, and a test asserts agreement in both directions: every name the schema advertises decodes, and every spelling the reader rejects stays unadvertised.",
			"targets": [
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a4cb5ab67c8d2c9e07109b84d2e3d0161f0f0061",
						"short_sha": "a4cb5ab",
						"url": "https://github.com/pina-rs/pina/commit/a4cb5ab67c8d2c9e07109b84d2e3d0161f0f0061",
						"authored_at": "2026-09-20T10:42:50+01:00",
						"committed_at": "2026-09-20T10:42:50+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#473",
						"title": "fix(pina_abi): enumerate the auto policy's kind names in its schema",
						"url": "https://github.com/pina-rs/pina/pull/473",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#468",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/468",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/migration-transition-offsets.md",
			"summary": "Read transition offsets from the stored schema",
			"details": "A generated automatic transition derived its byte offsets from a source schema with dropped and renamed fields already resolved. The bytes it edits are the stored layout, where a dropped field still occupies its space, so any field after a removal was read from the wrong offset and a field after a rename could be read from the wrong one too. The destination validation accepted the result whenever the shifted bytes still formed a valid layout, which made the corruption silent: `{ a, b, c } -> { a, c }` copied `a` to `a` and left `c` reading the old `b`. `SOURCE_SIZE` was wrong in the same way, understating the stored payload.\n\nThe planner now proves a transition from the stored schema against the developer's resolved intent and emits its proof: surviving fields copy from their stored offsets, dropped fields keep occupying their bytes, and every stored field must be either copied or explicitly discarded. A change that no single copy order can express — one field moving right while another moves left, a same-name type change, two destination fields reading one stored field — falls back to a manual transition instead of emitting bytes its own proof does not justify.\n\nFixed-layout accounts gain a manual escape hatch. `--manual <field>` names an added field whose conversion the developer writes. The recorded transition mode and renames carry that intent, so repeated `make` runs keep the authored body instead of regenerating an automatic transition over it. Pairing it with `--rename` allows a rename whose type changed, which a verbatim byte copy cannot express; the previously impossible \"combine `first_name` and `last_name` into `name`\" migration is now a generated manual draft with the correct stored sizes. Contradictory answers fail closed in both directions, including across the persisted `[migrations.answers]` table.\n\nA generated manual draft now carries an offset table in its header: one row per field with its payload-relative range in the stored layout and in the destination, `removed` for acknowledged discards, and prefix notes for compact tails. The developer implementing the body can see at a glance which bytes can move and which stay put.\n\nA recorded manual transition now scopes itself to the hop it was written for. Previously the recorded mode seeded the _next_ adjacent change too, so any hop after a hand-written transition was branded manual forever, even one the byte-level proof fully justifies — removing a field two versions after a narrowing conversion wrongly demanded another manual body. Repeated `make` runs over the manual hop's own draft still replay its recorded mode, so authored bodies remain stable.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "2b17b43a7afcccbef2dcc17df6b26c8b9c34dbf8",
						"short_sha": "2b17b43",
						"url": "https://github.com/pina-rs/pina/commit/2b17b43a7afcccbef2dcc17df6b26c8b9c34dbf8",
						"authored_at": "2026-09-21T04:48:57+01:00",
						"committed_at": "2026-09-21T04:48:57+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#481",
						"title": "fix(pina_cli): read transition offsets from the stored schema",
						"url": "https://github.com/pina-rs/pina/pull/481",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/multisig-example-and-cli-fixes.md",
			"summary": "Add the multisig example and fix generators",
			"details": "`examples/multisig_program` is a production-shaped multisig built on pina: bitmask voting over a sorted compact roster, unified proposal accounts carrying compiled vault messages or config action streams, timelocked execution with per-proposal ephemeral signers, vote revocation, proposal expiry, spending limits, rent collection, a generic Anchor-layout legacy import, and the full migration envelope. Building it surfaced two generator defects that this changeset also ships.\n\nFreshly scaffolded Rust CLI clients hard-coded `../../rust/<example>` as the dependency path, but the crate lives three directories deeper, so the manifest never resolved; existing examples only survived because update mode preserves their committed manifests, meaning no new example could scaffold a working CLI. The path is now derived from the configured rust and CLI output roots. The Dart CLI renderer emitted boolean instruction arguments as mandatory string options cast to `as String`, which failed `dart analyze` with a String-to-bool error; booleans are now declared as flags and read as `bool`, defaulting to false. The `pina docs` snapshot scratch space also moved under the workspace `target/` directory so recorded argument paths keep the same relative form on every machine and CI runner instead of leaking environment-specific absolute temp paths.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_codama_renderer_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "abebdeb1ac4ac8c7317f7b587fd5cdfd71833835",
						"short_sha": "abebdeb",
						"url": "https://github.com/pina-rs/pina/commit/abebdeb1ac4ac8c7317f7b587fd5cdfd71833835",
						"authored_at": "2026-09-19T19:11:46+01:00",
						"committed_at": "2026-09-19T19:11:46+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#466",
						"title": "feat(examples): add the multisig program",
						"url": "https://github.com/pina-rs/pina/pull/466",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#462",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/462",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/opt-in-heap-entrypoint.md",
			"summary": "Add an opt-in heap allocation entrypoint",
			"details": "`nostd_entrypoint!` installs `pinocchio::no_allocator!`, so a Pina program cannot use the heap at all: any dynamic allocation aborts at runtime. That stays the default, but programs that want allocation no longer have to hand-roll the entrypoint through Pina's `pinocchio` re-export.\n\nThe heap is off by default and opt-in twice over. Pina now depends on pinocchio with `default-features = false` and enables only `copy` and `sha2`, so pinocchio's `alloc` feature — and the `slice-cpi` code it gates — is no longer compiled into every Pina program. A new `alloc` feature forwards to it: enabling `pina/alloc` turns the heap on, and `nostd_entrypoint_alloc!` only exists when it is enabled. Calling the macro without the feature fails with a symbol named `ALLOC_FEATURE_REQUIRED_FOR_HEAP_ENTRYPOINT`, which spells out the remedy.\n\n`nostd_entrypoint_alloc!` is the same macro with one substitution — `pinocchio::default_allocator!` instead of `no_allocator!` — so a program that declares `extern crate alloc` can use `Box`, `Vec`, and the rest of `alloc` while keeping Pina's entrypoint wiring. `nostd_entrypoint!` and its expansion are unchanged; the heap path is purely opt-in and per program.\n\nThe macro's rustdoc documents the costs that `no_allocator!` currently makes impossible, because each is a failure mode an opted-in program can hit: the runtime grants a 32 KiB heap frame at 0 CU and anything larger needs a caller-sent `request_heap_frame` (multiple of 1024, at most 256 KiB, 8 CU per extra 32 KiB page); the bump allocator's `dealloc` is a no-op, so a transaction's peak heap use is the sum of every allocation it makes; an allocation the frame cannot satisfy aborts instead of returning a `ProgramError`; and the allocator stores its position in the first word of the region, so the usable bytes are the granted frame minus one word.\n\n`heap_alloc_program` exercises the new macro end to end. Its mollusk suite holds the heap frame at a fixed size and shows a fill inside the frame completing, a fill past it aborting, and the same fill succeeding once the frame is raised. Its Surfpool suite runs the same artifact on a real runtime, where `Box` round-trips through the bump allocator. Three doc sites that described allocator-freeness as an unconditional Pina property now say \"by default\".",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "de21f558297c88371b7f68a892c03eb5af23cea7",
						"short_sha": "de21f55",
						"url": "https://github.com/pina-rs/pina/commit/de21f558297c88371b7f68a892c03eb5af23cea7",
						"authored_at": "2026-09-19T22:46:26+01:00",
						"committed_at": "2026-09-19T22:46:26+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#465",
						"title": "feat(entrypoint): add an opt-in heap allocation macro",
						"url": "https://github.com/pina-rs/pina/pull/465",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#462",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/462",
						"relationship": "closed_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/pda-qualified-address.md",
			"summary": "Qualify every `Address` in generated PDA code",
			"details": "`#[pda]` emitted its generated `Address` types unqualified: the `program_id` parameter of `try_find_pda`, `find_pda`, and `assert_seeds`, and the seed parameter and stored field types of the generated seeds struct. Those spellings resolve against the user's own scope, so a program with its own type named `Address` could silently change which address type a generated signature takes. Every generated `Address` is now the crate-qualified form, matching what the surrounding generated code and the `#[account]` schema path already emitted.\n\nThe expansion also emits the same identity proof the schema path uses — `const _: fn(Address) -> pina::Address = |value| value;` — but only when a declaration actually has an `Address` seed, so a numeric-only `#[pda]` never requires `Address` in scope. The proof binds the caller's bare `Address` spelling to the crate's type, so a future unqualified spelling is a compile error in the user's crate rather than a silent shadow.",
			"targets": [
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/pda-seed-count-cap.md",
			"summary": "Reject PDA declarations that cannot derive",
			"details": "Every generated `#[pda]` derivation appends a bump seed: `try_find_pda` and `find_pda` search for the canonical one, `with_bump` supplies the stored or explicit one, and the generated loaders derive from the stored field. The runtime caps a derivation at 16 seeds total, so a declaration of 16 seeds produced 17 and no instruction could ever load the account.\n\nThe macro now caps the declared list at 15 seeds and names both numbers in the error. The cap applies whether or not the declaration has a `bump =` field, because the bump-free variant derives through `try_find_pda` and appends a bump there. A declaration that previously compiled with 16 seeds was already unreachable at runtime; it now fails the build instead of shipping a PDA that always returns `InvalidSeeds`.",
			"targets": [
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/pina-generate-all-clients.md",
			"summary": "Generate every repository client with `pina generate`",
			"details": "`pina generate` supersedes `pina codama generate`, which is removed. It discovers a project through its `pina.toml`, refreshes the IDL, and renders only the configured client ecosystems, so a repository generates its clients by running the command once per project instead of through a second command with its own path flags. The bundled skill reference documents the loop.\n\nDogfooding this path in the repository surfaced a bug in shared Dart CLI packages: `pina generate` published the whole staged `bin/` directory for each program, so generating one project deleted the entrypoints of every other project sharing the package. Only the single entrypoint owned by the invocation is published now.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina_skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "6147d17bccecc005d6331182b049dc1418c504b2",
						"short_sha": "6147d17",
						"url": "https://github.com/pina-rs/pina/commit/6147d17bccecc005d6331182b049dc1418c504b2",
						"authored_at": "2026-09-20T19:28:46+01:00",
						"committed_at": "2026-09-20T19:28:46+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#474",
						"title": "refactor(cli)!: generate all clients with `pina generate`",
						"url": "https://github.com/pina-rs/pina/pull/474",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/rename-migrations-make-to-create.md",
			"summary": "Rename `pina migrations make` to `pina migrations create`",
			"details": "`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "86ed8c285d0aff77da2ff8442932a47f8fcf6521",
						"short_sha": "86ed8c2",
						"url": "https://github.com/pina-rs/pina/commit/86ed8c285d0aff77da2ff8442932a47f8fcf6521",
						"authored_at": "2026-09-20T21:19:12+01:00",
						"committed_at": "2026-09-20T21:19:12+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#475",
						"title": "refactor(pina_cli)!: rename `migrations make` to `migrations create`",
						"url": "https://github.com/pina-rs/pina/pull/475",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/rename-stored-bump-pda-loader.md",
			"summary": "Name the stored-bump compact PDA loader for what it verifies",
			"details": "The release that split the compact PDA loader into `with_pda` and `with_checked_pda` gave the two methods names that do not state their difference: `with_pda` kept its name while its verification was weakened from a canonical bump search to a single derivation from the account's stored bump. A program upgrading across that release lost canonical-bump rejection at every existing `with_pda` call site with no compiler signal.\n\nThe weak loader is now `with_stored_bump_pda`, which says what it checks. The old `with_pda` name remains as a deprecated forwarding alias for one release, so existing calls compile but emit a `deprecated` warning naming the difference and pointing at `with_checked_pda` for the trustless case. `with_checked_pda` is unchanged.\n\n`pina_cli`'s IDL parser recognizes `with_stored_bump_pda` as a PDA loader alongside the other names, so account properties are unchanged.",
			"targets": [
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/sanitize-idl-names-in-docs.md",
			"summary": "Keep untrusted IDL names inside generated doc comments",
			"details": "`CamelCaseString` derives `Deserialize` on its inner `String`, so the normalization its constructor applies never runs for JSON input: a name read from an IDL reaches the renderer verbatim, newlines included. Two sites interpolated such a name straight into a single-line `///` comment — the CPI account line in `render_account` and the argument line in `render_argument` — so everything after an embedded newline was emitted as uncommented Rust. A crafted IDL handed to `pina cpi --idl` produced a generated crate containing attacker-chosen items, which then compile (and execute under `#[cfg(test)]`) in the victim's workspace.\n\nBoth sites now route through `render_doc`, which splits on newlines and prefixes every line as a comment, so an embedded newline becomes another comment line instead of code. `pina_cpi_renderer` gains a regression test that deserializes a malicious IDL through the same `read_root_node` path `pina cpi` uses, asserts the newline survives parsing (so the test keeps exercising the real path), and fails if any payload line escapes its comment.",
			"targets": [
				{
					"id": "pina_cpi_renderer",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/scaffold-release-line-test.md",
			"summary": "Derive the scaffold release-line test from the crate version",
			"details": "`scaffold_declares_pina_from_the_configured_source` computes the expected `pina` pin from `CARGO_PKG_VERSION` the same way `ScaffoldDependency::Published` renders it, so the assertion follows the crate's release line instead of holding a hard-coded `0.19` that the next version bump would leave behind.",
			"targets": [
				{
					"id": "pina_cpi_renderer",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "1a3032c7eda382e6580b80caad52ca21331b9bdb",
						"short_sha": "1a3032c",
						"url": "https://github.com/pina-rs/pina/commit/1a3032c7eda382e6580b80caad52ca21331b9bdb",
						"authored_at": "2026-09-22T22:30:19+01:00",
						"committed_at": "2026-09-22T22:30:19+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#490",
						"title": "fix(pina_cpi_renderer): derive the scaffold release line from the crate version",
						"url": "https://github.com/pina-rs/pina/pull/490",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/security-audit-examples.md",
			"summary": "Close the remaining audit findings in the example programs",
			"details": "`multisig_program` closes three findings. `MultisigImport` binds its provenance: the legacy account must sit at the PDA its parsed `create_key` derives under the caller-named legacy program, and the holder of that `create_key` must sign the import, so a fabricated account carrying a borrowed roster of keys that never consented no longer imports (the instruction gains the `legacy_create_key` signer account). `ConfigAuthorityExecute` rejects `AddSpendingLimit` actions with a new `SpendingLimitRequiresProposal` error — creating an allowance moves vault funds, which stays behind threshold and timelock even in controlled mode, so the config authority manages configuration rather than custody. `VaultExecute` additionally protects every program-owned writable non-signer account, not just the multisig and the executing proposal, so a vault message can no longer write another proposal or a spending limit through a nested CPI.\n\n`escrow_program` gains a maker `Cancel` (discriminator 3) that refunds the full vault balance and closes both accounts, so a maker is no longer stranded until a taker appears. `prop_amm_program`'s `Update` now accepts the oracle's stored authority in addition to the benchmark's static key, making `RotateAuthority` mean what it says — previously the rotated key could never publish a price. The multisig readme documents the permissionless `ConfigInitialize` race (initialize the config in the deployment transaction or a sniper owns the fee treasury) and the spending-limit rules, and the staking readme documents the same first-initializer shape for pool creation.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "2895f0d2c54e1651874de0de8149c722b09fe51f",
						"short_sha": "2895f0d",
						"url": "https://github.com/pina-rs/pina/commit/2895f0d2c54e1651874de0de8149c722b09fe51f",
						"authored_at": "2026-09-21T22:15:24+01:00",
						"committed_at": "2026-09-21T22:15:24+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#483",
						"title": "fix(security)!: close the outstanding 2026-09-21 audit findings",
						"url": "https://github.com/pina-rs/pina/pull/483",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#472",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/472",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/security-audit-framework.md",
			"summary": "Close the shadow-PDA and discriminator-collision gaps",
			"details": "Fixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.",
			"targets": [
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina_lints",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "pina",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_root",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_renderer",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_codama_nodes",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_codama_renderer",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_codama_renderer_cli",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_codama_renderer_cpi",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cpi_renderer",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_profile",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_sdk_ids",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_test",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "2895f0d2c54e1651874de0de8149c722b09fe51f",
						"short_sha": "2895f0d",
						"url": "https://github.com/pina-rs/pina/commit/2895f0d2c54e1651874de0de8149c722b09fe51f",
						"authored_at": "2026-09-21T22:15:24+01:00",
						"committed_at": "2026-09-21T22:15:24+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#483",
						"title": "fix(security)!: close the outstanding 2026-09-21 audit findings",
						"url": "https://github.com/pina-rs/pina/pull/483",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#472",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/472",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/security-audit-supply-chain.md",
			"summary": "Document the lint-driver origin overrides as operator-only",
			"details": "`PINA_LINT_DRIVER_RELEASE`, `_REPO`, and `_BASE_URL` redirect the driver download and its checksum together, so they are a trusted-origin control rather than a convenience knob: whatever serves the override chooses which binary the CLI runs. The constants now say so, which is the documentation half of the 2026-09-21 audit's supply-chain finding; the version-pinned `npx` renderer fetches stay registry-trust-by-design.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_cli_npm",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_darwin_arm64",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_darwin_x64",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_freebsd_x64",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_linux_arm64_gnu",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_linux_arm64_musl",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_linux_x64_gnu",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_linux_x64_musl",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_win32_arm64_msvc",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				},
				{
					"id": "pina_cli_win32_x64_msvc",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "none"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "2895f0d2c54e1651874de0de8149c722b09fe51f",
						"short_sha": "2895f0d",
						"url": "https://github.com/pina-rs/pina/commit/2895f0d2c54e1651874de0de8149c722b09fe51f",
						"authored_at": "2026-09-21T22:15:24+01:00",
						"committed_at": "2026-09-21T22:15:24+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#483",
						"title": "fix(security)!: close the outstanding 2026-09-21 audit findings",
						"url": "https://github.com/pina-rs/pina/pull/483",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#472",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/472",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/security-disclosure-channel.md",
			"summary": "Lead with GitHub private vulnerability reporting",
			"details": "`SECURITY.md` documented a personal Gmail address as the only private channel for reporting a vulnerability. It now leads with GitHub's private advisory form at <https://github.com/pina-rs/pina/security/advisories/new>, which opens a draft advisory visible only to the reporter and the maintainers, keeps the reproduction and the fix attached to the repository, and lets the fix ship before the advisory is published. The email stays as the fallback for reporters who cannot use the form, and the \"do not open a public GitHub issue\" instruction, the acknowledgement and disclosure process, the supported-versions table, and the pre-1.0 and unaudited framing are unchanged.",
			"targets": [
				{
					"id": "pina_root",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/surfpool-1-6-agave-4-2.md",
			"summary": "Move the host stack to Surfpool 1.6 / Agave 4.2",
			"details": "`pina_test` anchored every host-side Solana dependency on the Agave 4.1 train because Surfpool 1.5 capped `litesvm` at `^0.14.0`, and that release does not compile against Agave 4.2. Surfpool 1.6 ships `litesvm` 0.16 on Agave 4.2, so the cap is gone and the workspace moves up with it:\n\n- `surfpool-sdk` `~1.5` → `~1.6`\n- `solana-client`, `solana-rpc-client`, and `solana-runtime` `~4.1` → `~4.2`\n- `mollusk-svm` 0.14.0 → 0.15.1, which is the Mollusk release built on Agave 4.2\n\nEvery other tilde range in the crate already accommodated the newer train, so nothing else moved by hand. The resolver now proves out a single `solana-runtime` 4.2.2 island; the remaining multi-version crates (`solana-message`, `solana-transaction`, `solana-fee-calculator`) are the program-side crates that `pinocchio` and the examples depend on and were already split before this change. The devenv `ifiokjr-nixpkgs` pin moves to the revision that packages `surfpool` 1.6.0 so the CLI and the SDK agree; that revision keeps agave at 4.2.2.\n\n`OfflineSurfnet::start` now selects `BlockProductionMode::Clock`. Surfpool 1.6.0's SDK defaults to `BlockProductionMode::Transaction`, and on an embedded offline instance that mode never confirms a submitted transaction: the client's confirmation loop polls `getSignatureStatuses` indefinitely and the test hangs with no error. The same suite passes in about five seconds in clock mode. This is a Surfpool regression rather than a Pina defect and is reported upstream as [surfpool#814](https://github.com/solana-foundation/surfpool/issues/814); the override comes out once that is fixed.\n\nThe upgrade also removes a documented limitation. Surfpool 1.5 could not derive CPI signers for PDAs with four or more seed arguments, so any program seeding a PDA that way could not run its Surfpool suite end to end. On 1.6.0 the vesting example's `[b\"vesting\", admin, beneficiary, mint]` plus bump PDA completes `Initialize` → `Claim` → `Cancel`, which the previous train could not reach. `crates/pina_test/readme.md` now records the fix instead of the caveat.",
			"targets": [
				{
					"id": "pina_test",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "aa81c8d1d5816bb932832adce1c5550cd74b6782",
						"short_sha": "aa81c8d",
						"url": "https://github.com/pina-rs/pina/commit/aa81c8d1d5816bb932832adce1c5550cd74b6782",
						"authored_at": "2026-09-21T07:05:39+01:00",
						"committed_at": "2026-09-21T07:05:39+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#482",
						"title": "feat(pina_test)!: move the host test stack to Surfpool 1.6 and Agave 4.2",
						"url": "https://github.com/pina-rs/pina/pull/482",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#289",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/289",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#802",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/802",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/sweep-security-fixes.md",
			"summary": "Close the confirmed 2026-09-19 sweep findings",
			"details": "`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.",
			"targets": [
				{
					"id": "pina",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_macros",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "pina_abi",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				},
				{
					"id": "pina_codama_nodes",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "26cdddf7e712ed09ee4c0bf710f6f5c0abbecd50",
						"short_sha": "26cdddf",
						"url": "https://github.com/pina-rs/pina/commit/26cdddf7e712ed09ee4c0bf710f6f5c0abbecd50",
						"authored_at": "2026-09-20T13:08:12+01:00",
						"committed_at": "2026-09-20T13:08:12+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#472",
						"title": "fix(security): close the confirmed 2026-09-19 sweep findings",
						"url": "https://github.com/pina-rs/pina/pull/472",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/validate-cargo-library-name.md",
			"summary": "Reject Cargo library names that cannot be path components",
			"details": "Cargo accepts any string as a `[lib] name` and reports it verbatim through `cargo metadata`, so a checked-in manifest controls the value. Pina joined that name into file and directory paths (`target/deploy`, generated IDL files, client crate directories) without validating it, so `[lib] name = \"../../escaped\"` made `pina generate` write outside the project root — reproduced with an artifact landing next to the project directory while the command reported success.\n\n`Project::discover` now validates the library name once, where it enters the project model, and rejects anything outside `[A-Za-z0-9_-]` with the new `ProjectError::InvalidLibraryName`. Cargo already refuses the empty name before metadata reaches pina. Validating at the ingestion point keeps every current and future path join safe instead of patching individual sinks, and no legitimate library name is affected because Cargo's own naming rules are a subset of the accepted set.",
			"targets": [
				{
					"id": "pina_cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "fix"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b90be754a53dae43a9cd00754da9a97f49e85a19",
						"short_sha": "b90be75",
						"url": "https://github.com/pina-rs/pina/commit/b90be754a53dae43a9cd00754da9a97f49e85a19",
						"authored_at": "2026-09-19T04:37:17+01:00",
						"committed_at": "2026-09-19T04:37:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#457",
						"title": "fix!: remediate the 2026-09-18 deep-audit findings",
						"url": "https://github.com/pina-rs/pina/pull/457",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"short_sha": "5c6c80b",
						"url": "https://github.com/pina-rs/pina/commit/5c6c80b125fb7c6f8d3d91430cf195b61d04f8a9",
						"authored_at": "2026-09-23T00:55:01+01:00",
						"committed_at": "2026-09-23T00:55:01+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#492",
						"title": "chore(release): revert the 0.20.0 release preparation",
						"url": "https://github.com/pina-rs/pina/pull/492",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#442",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/442",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#448",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/448",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#456",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/456",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#459",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/459",
						"relationship": "referenced_by_review_request"
					},
					{
						"provider": "github",
						"host": "github.com",
						"id": "#491",
						"title": null,
						"url": "https://github.com/pina-rs/pina/issues/491",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		}
	],
	"changelogs": [
		{
			"owner_id": "core",
			"owner_kind": "group",
			"output": "default",
			"stream": "default",
			"path": "changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.20.0](https://github.com/pina-rs/pina/releases/tag/v0.20.0) (2026-09-23)",
				"summary": [
					"Grouped release for `core`."
				],
				"sections": [
					{
						"title": "Breaking Changes",
						"collapsed": false,
						"entries": [
							"#### Name the stored-bump compact PDA loader for what it verifies\n\n_Packages:_ _pina_, _pina_macros_\n\nThe release that split the compact PDA loader into `with_pda` and `with_checked_pda` gave the two methods names that do not state their difference: `with_pda` kept its name while its verification was weakened from a canonical bump search to a single derivation from the account's stored bump. A program upgrading across that release lost canonical-bump rejection at every existing `with_pda` call site with no compiler signal.\n\nThe weak loader is now `with_stored_bump_pda`, which says what it checks. The old `with_pda` name remains as a deprecated forwarding alias for one release, so existing calls compile but emit a `deprecated` warning naming the difference and pointing at `with_checked_pda` for the trustless case. `with_checked_pda` is unchanged.\n\n`pina_cli`'s IDL parser recognizes `with_stored_bump_pda` as a PDA loader alongside the other names, so account properties are unchanged.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Generate every repository client with `pina generate`\n\n_Packages:_ _pina_cli_\n\n`pina generate` supersedes `pina codama generate`, which is removed. It discovers a project through its `pina.toml`, refreshes the IDL, and renders only the configured client ecosystems, so a repository generates its clients by running the command once per project instead of through a second command with its own path flags. The bundled skill reference documents the loop.\n\nDogfooding this path in the repository surfaced a bug in shared Dart CLI packages: `pina generate` published the whole staged `bin/` directory for each program, so generating one project deleted the entrypoints of every other project sharing the package. Only the single entrypoint owned by the invocation is published now.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #474](https://github.com/pina-rs/pina/pull/474) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_cli_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_macros_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					},
					{
						"title": "Features",
						"collapsed": false,
						"entries": [
							"#### Add a checked discriminator write\n\n_Packages:_ _pina_\n\n`IntoDiscriminator::write_discriminator` returns `()` and drops the write when the destination slice is shorter than the discriminator: a `debug_assert!` fires in debug builds, but a release build silently leaves the buffer untouched. Generated code always sizes its buffers from `Self::BYTES`, so this only bit hand-written implementations, where a missing discriminator produces an account that looks initialized but decodes as an unknown type.\n\nThe new `IntoDiscriminator::try_write_discriminator` reports the undersized buffer with `PinaProgramError::DataTooShort` (0xFFFF_FFFA, \"Account or instruction data is shorter than the expected minimum\") instead. That is the variant `HasMigrationVersion::write_le` and the migration decoders already return for a destination that cannot hold the value, so a caller sees one error for every short-buffer case.\n\nThe method has a default implementation that length-checks and then delegates to `write_discriminator`, so a manual implementer inherits the checked path without writing anything; the primitive implementations (`u8`, `u16`, `u32`, `u64`) and `into_discriminator!` override it to write through `get_mut` in one step. `write_discriminator` is unchanged, and its doc comment now points new manual implementers at the checked form.\n\nTests cover short buffers of every length up to `BYTES` (including a short inner slice of a long array), exact and oversized buffers, and that the checked write produces the same bytes as the unchecked one.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Accept const capacities in schemas\n\n_Packages:_ _pina_, _pina_cli_, _pina_macros_\n\nCompact account and instruction schemas now accept a `const` item wherever they previously required an integer literal:\n\n```rust\nconst MAX_MEMBERS: usize = 24;\n\n#[account(discriminator = MultisigAccountType, compact)]\npub struct Multisig {\n\tpub bump: u8,\n\tpub member_keys: Vec<Address, MAX_MEMBERS>,\n}\n```\n\n`Vec<T, N>`, `PodVec<T, N, PFX>`, `String<N>`, `PodString<N, PFX>`, an `Option<...>` around them, and fixed `[T; N]` arrays and instruction arguments all resolve a capacity through a constant. Constants may be arithmetic over other constants and may be declared in any module of the crate, so one bound is declared once and reused in the account, the instruction that writes it, and the helper that sizes it.\n\nPina resolves each capacity during expansion and records the number it evaluates to. The ABI layer still receives concrete values, so replacing a literal with a constant of the same value leaves `migrations/manifest.json`, the generated `tests/abi_layout.rs` assertions, `MAX_SIZE`/`MIN_SIZE`/`HEADER_SIZE`, and `projected_bytes(...)` byte-identical, and `pina migrations check` and the Codama IDL unchanged.\n\nA capacity that cannot be evaluated at expansion time now fails the build with a diagnostic naming the expression and pointing at the workaround, instead of the previous `unsupported compact field` or `arrays require an integer literal length` message. An associated constant such as `Bounds::MAX_MEMBERS` is not resolved; declare the bound as a `const` item.\n\n`pina_abi` gains `SchemaConsts`, the shared evaluator both the macros and the CLI use so the two layers agree on what a constant means. `pina_cli` resolves capacities while it parses a program, so migration and IDL tooling reads the same numbers the compiler does.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #463](https://github.com/pina-rs/pina/pull/463) · _Closed issues:_ [#460](https://github.com/pina-rs/pina/issues/460) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Reject event schemas that cannot fit the SBF stack\n\n_Packages:_ _pina_, _pina_macros_\n\nThe generated `emit` function materializes a whole event record in one stack frame (`let mut record = [0u8; Self::SIZE];`) before calling into the runtime, so a large `#[event]` schema compiled cleanly and then exhausted the 4 KiB SBF stack at runtime — a self-inflicted denial of service with no compile-time signal.\n\n`pina` gains `MAX_EVENT_RECORD_BYTES`, set to `4096 - 512`. The 512-byte reserve is headroom for the callee frame and the caller's own frame, the same reasoning `MAX_MIGRATION_WORKSPACE` already applies to the historical-normalization workspace. Because the constant is public, the bound a caller can read is the bound the macro enforces.\n\n`pina_macros` emits a compile-time assertion in every `#[event]` expansion, so an oversized schema now fails the build with a message naming the struct and the remedy. A record exactly at the bound still compiles; the assertion is inclusive.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Match the Migrate trigger to the discriminator width\n\n_Packages:_ _pina_, _pina_macros_\n\n`#[discriminator]` reserves the all-ones value of the enum's own primitive, so a `u16` program reserves `0xffff` and a `u32` program reserves `0xffff_ffff`. The generated entrypoint guarded the reserved path with `is_migrate_instruction`, which only tests one byte, so for any program whose instruction enum used `primitive = u16`, `u32`, or `u64` the reserved discriminator never matched and `process_migrate` was unreachable.\n\n`pina` adds width-specific helpers alongside the existing one: `is_migrate_instruction_u16`, `is_migrate_instruction_u32`, and `is_migrate_instruction_u64`. Each matches only its own width, so a one-byte `0xff` is not a two-byte `0xffff` and neither prefix-matches the other. They compare the decoded little-endian value rather than a slice equality, keeping the inline comparison the one-byte helper already had instead of the `memcmp` call `data == CONSTANT.to_le_bytes()` would emit on SBF.\n\n`pina_macros` selects the helper from the enum's `primitive`, so `#[discriminator(entrypoint, migrations(...))]` on a wider discriminator now reaches the reserved path. The generated guard for a default `u8` program is byte-identical to before, so the common case takes no compute regression. The documentation on the reserved-discriminator constants and in `docs/src/migrations/flow.md` now names the width pairing.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Add an opt-in heap allocation entrypoint\n\n_Packages:_ _pina_\n\n`nostd_entrypoint!` installs `pinocchio::no_allocator!`, so a Pina program cannot use the heap at all: any dynamic allocation aborts at runtime. That stays the default, but programs that want allocation no longer have to hand-roll the entrypoint through Pina's `pinocchio` re-export.\n\nThe heap is off by default and opt-in twice over. Pina now depends on pinocchio with `default-features = false` and enables only `copy` and `sha2`, so pinocchio's `alloc` feature — and the `slice-cpi` code it gates — is no longer compiled into every Pina program. A new `alloc` feature forwards to it: enabling `pina/alloc` turns the heap on, and `nostd_entrypoint_alloc!` only exists when it is enabled. Calling the macro without the feature fails with a symbol named `ALLOC_FEATURE_REQUIRED_FOR_HEAP_ENTRYPOINT`, which spells out the remedy.\n\n`nostd_entrypoint_alloc!` is the same macro with one substitution — `pinocchio::default_allocator!` instead of `no_allocator!` — so a program that declares `extern crate alloc` can use `Box`, `Vec`, and the rest of `alloc` while keeping Pina's entrypoint wiring. `nostd_entrypoint!` and its expansion are unchanged; the heap path is purely opt-in and per program.\n\nThe macro's rustdoc documents the costs that `no_allocator!` currently makes impossible, because each is a failure mode an opted-in program can hit: the runtime grants a 32 KiB heap frame at 0 CU and anything larger needs a caller-sent `request_heap_frame` (multiple of 1024, at most 256 KiB, 8 CU per extra 32 KiB page); the bump allocator's `dealloc` is a no-op, so a transaction's peak heap use is the sum of every allocation it makes; an allocation the frame cannot satisfy aborts instead of returning a `ProgramError`; and the allocator stores its position in the first word of the region, so the usable bytes are the granted frame minus one word.\n\n`heap_alloc_program` exercises the new macro end to end. Its mollusk suite holds the heap frame at a fixed size and shows a fill inside the frame completing, a fill past it aborting, and the same fill succeeding once the frame is raised. Its Surfpool suite runs the same artifact on a real runtime, where `Box` round-trips through the bump allocator. Three doc sites that described allocator-freeness as an unconditional Pina property now say \"by default\".\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #465](https://github.com/pina-rs/pina/pull/465) · _Closed issues:_ [#462](https://github.com/pina-rs/pina/issues/462) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_cli_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Name the stored-bump compact PDA loader for what it verifies\n\n_Packages:_ _pina_cli_\n\nThe release that split the compact PDA loader into `with_pda` and `with_checked_pda` gave the two methods names that do not state their difference: `with_pda` kept its name while its verification was weakened from a canonical bump search to a single derivation from the account's stored bump. A program upgrading across that release lost canonical-bump rejection at every existing `with_pda` call site with no compiler signal.\n\nThe weak loader is now `with_stored_bump_pda`, which says what it checks. The old `with_pda` name remains as a deprecated forwarding alias for one release, so existing calls compile but emit a `deprecated` warning naming the difference and pointing at `with_checked_pda` for the trustless case. `with_checked_pda` is unchanged.\n\n`pina_cli`'s IDL parser recognizes `with_stored_bump_pda` as a PDA loader alongside the other names, so account properties are unchanged.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_lints_, _pina_macros_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Move the host stack to Surfpool 1.6 / Agave 4.2\n\n_Packages:_ _pina_test_\n\n`pina_test` anchored every host-side Solana dependency on the Agave 4.1 train because Surfpool 1.5 capped `litesvm` at `^0.14.0`, and that release does not compile against Agave 4.2. Surfpool 1.6 ships `litesvm` 0.16 on Agave 4.2, so the cap is gone and the workspace moves up with it:\n\n- `surfpool-sdk` `~1.5` → `~1.6`\n- `solana-client`, `solana-rpc-client`, and `solana-runtime` `~4.1` → `~4.2`\n- `mollusk-svm` 0.14.0 → 0.15.1, which is the Mollusk release built on Agave 4.2\n\nEvery other tilde range in the crate already accommodated the newer train, so nothing else moved by hand. The resolver now proves out a single `solana-runtime` 4.2.2 island; the remaining multi-version crates (`solana-message`, `solana-transaction`, `solana-fee-calculator`) are the program-side crates that `pinocchio` and the examples depend on and were already split before this change. The devenv `ifiokjr-nixpkgs` pin moves to the revision that packages `surfpool` 1.6.0 so the CLI and the SDK agree; that revision keeps agave at 4.2.2.\n\n`OfflineSurfnet::start` now selects `BlockProductionMode::Clock`. Surfpool 1.6.0's SDK defaults to `BlockProductionMode::Transaction`, and on an embedded offline instance that mode never confirms a submitted transaction: the client's confirmation loop polls `getSignatureStatuses` indefinitely and the test hangs with no error. The same suite passes in about five seconds in clock mode. This is a Surfpool regression rather than a Pina defect and is reported upstream as [surfpool#814](https://github.com/solana-foundation/surfpool/issues/814); the override comes out once that is fixed.\n\nThe upgrade also removes a documented limitation. Surfpool 1.5 could not derive CPI signers for PDAs with four or more seed arguments, so any program seeding a PDA that way could not run its Surfpool suite end to end. On 1.6.0 the vesting example's `[b\"vesting\", admin, beneficiary, mint]` plus bump PDA completes `Initialize` → `Claim` → `Cancel`, which the previous train could not reach. `crates/pina_test/readme.md` now records the fix instead of the caveat.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #482](https://github.com/pina-rs/pina/pull/482) · _Related issues:_ [#289](https://github.com/pina-rs/pina/issues/289), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491), [#802](https://github.com/pina-rs/pina/issues/802)"
						]
					},
					{
						"title": "Fixes",
						"collapsed": false,
						"entries": [
							"#### Adopt the Pinapod 0.4.3 compact-writer hardening\n\n_Packages:_ _pina_\n\nMove from the temporary `pinapod` 0.4.1 hold to 0.4.3, which brings the 0.4.2 commit-entry hardening at its intended compute-unit cost. 0.4.2 validates every generated compact buffer at `commit` before tails are relocated: a writer whose layout is corrupt fails closed before any offset or pointer work, and a preflight and commit length disagreement returns `InvalidLength` in release builds instead of relying on a debug assertion. By default, semantic defects such as non-UTF-8 tail bytes or out-of-range tags are caught at the next interpreting boundary rather than by `commit` itself; the `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it. 0.4.3 keeps all of this and restores the compute-unit profile.\n\nThe intermediate 0.4.2 release was held back on a diagnosis that turned out to be wrong: Pina reported that 0.4.2 moved `commit` ahead of the inline writes and turned a length check into an unconditional panic. Neither was true — the panic-shaped block was 0.4.1's own `debug_assert_eq!` expansion, and `update` never changed order. [pinapod#36](https://github.com/pina-rs/pinapod/issues/36) recorded the correction and [pinapod#38](https://github.com/pina-rs/pinapod/issues/38) specified how to keep the safety without the cost, which is what 0.4.3 implements.\n\n`commit` entry now proves layout rather than semantics through `PinaPodCompact::validate_layout`: the storage length, each stored length prefix, its capacity, and the chained bounds. Relocation's pointer arithmetic consumes exactly those, so this is the complete precondition and not a weakening of it. The element walks that checked UTF-8, enum ranges, and tags move to the boundaries that interpret those bytes, which already had them — the `Ref` and `Mut` constructors, `updated_len`, and `try_initialize`'s post-commit check. Trivially valid element types such as `u64` and `u8` skip their walks entirely rather than relying on the compiler to delete a dead loop, which SBF at `-C opt-level=3` does not reliably do.\n\nAn account that is layout-valid but semantically invalid, such as non-UTF-8 bytes in an untouched tail, now commits and is rejected at the next read instead of by `commit` itself. The update path keeps its existing guarantee by construction, because `updated_len` runs the full validation over the same bytes the staged edits will produce. A `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it.\n\nThe observable effect on Pina is the regenerated `pda.expanded.rs` snapshot, and the compute units that 0.4.2 added are gone: the `compact_accounts_program` instructions that regressed by 46 to 308 units under 0.4.2 measure at or below their 0.4.1 cost again.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #485](https://github.com/pina-rs/pina/pull/485) · _Related issues:_ [#38](https://github.com/pina-rs/pina/issues/38), [#39](https://github.com/pina-rs/pina/issues/39), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Stop enabling `bytemuck` on `solana-address`/`solana-pubkey`\n\n_Packages:_ _pina_\n\nThe workspace requested `solana-address/bytemuck` and `solana-pubkey/bytemuck`. Pina used `bytemuck` for its own pod primitives until the migration to `pinapod`, so nothing in the workspace reads the `Pod` or `Zeroable` derives those features add, and no first-party code calls a `bytemuck` API.\n\nBoth entries now request what Pina actually depends on. `solana-address` asks for `copy`, which derives `Copy` on `Address` — the bound `pinapod`'s `ZcElem` requires and the only part of `bytemuck`'s feature set that was load-bearing. Requesting it directly removes the dependence on `bytemuck`'s implication, and on `pinocchio` and `pinapod` enabling `copy` transitively. `solana-pubkey` no longer requests `bytemuck` at all.\n\nDeployable programs are unchanged: `bytemuck` contributed no symbols to any built program, so all 24 `bpf-entrypoint` examples produce byte-identical sizes and an identical instruction mix with and without it. This is a dependency-graph cleanup, not a size or compute-unit change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #464](https://github.com/pina-rs/pina/pull/464) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_, _pina_codama_nodes_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Fix the CPI scaffold's pina dependency for workspace crates\n\n_Packages:_ _pina_cli_, _pina_cpi_renderer_\n\n`pina generate` scaffolds a CPI crate for every example, but the scaffold hardcoded `pina = { version = \"0.17\", default-features = false }` — a registry pin that stopped matching this workspace's own `pina` releases. Every previously committed CPI crate had already been pinned to `pina = { workspace = true }` by hand, so the defect only surfaced on the next newly generated example: it added a second `pina` to the dependency graph, which makes `cargo kani -p pina` fail with \"multiple `pina` packages in your project\" and breaks both Kani proof tiers.\n\nThe scaffold now takes the dependency source from the caller. `pina generate`, whose output is always a workspace member, inherits the workspace dependency; `pina cpi` and `pina import`, whose output lives in the caller's own project with no workspace entry to inherit, pin the renderer's own release line instead of a stale literal. A regression test covers both modes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #489](https://github.com/pina-rs/pina/pull/489) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#486](https://github.com/pina-rs/pina/issues/486), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Retry the lint driver probe while the kernel reports it busy\n\n_Packages:_ _pina_cli_\n\n`pina lint` starts the driver it just installed to check that it loads. The kernel refuses an `exec` while a write descriptor for the same file is still open — including one a concurrent fork inherited — and the install-then-probe sequence can overlap with that window, so the probe failed with a busy error on a driver that was perfectly runnable. The spawn now retries briefly for that one error and fails immediately for anything else, which also stops the driver tests from flaking under a parallel test run.\n\n`pina` carries no behavior change in this release line; the entry records the added test coverage for the public `into_discriminator!` macro.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #461](https://github.com/pina-rs/pina/pull/461) · _Related issues:_ [#457](https://github.com/pina-rs/pina/issues/457), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Emit valid TypeScript for reserved Migrate type arguments\n\n_Packages:_ _pina_cli_\n\nThe reserved `Migrate` composer closed its `MigrateInput<...>` type argument list with a trailing comma. That is a `TS1009` syntax error — trailing commas are legal in type parameter _declarations_ but not in type argument _uses_ — so raw `pina generate` output could not be parsed by `tsc`, `esbuild`, or any tool stricter than dprint's formatter. Repository pipelines only survived because `verify-codama-idls.sh` runs `dprint fmt` over the generated tree, silently deleting the comma before anything parsed the file. A consumer running `pina generate` without that reformatting step got uncompilable clients.\n\nThe generator now emits the comma-free shape dprint normalizes to, so committed clients are unchanged and raw output is valid TypeScript. Caught by the migrations walkthrough, which loads the freshly generated client through esbuild without a formatting pass.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Verify the lint driver download before installing it\n\n_Packages:_ _pina_cli_\n\n`pina lint` fetches a prebuilt driver binary, marks it executable, and runs it as its version probe. The download trusted the transport alone: no checksum was verified, the response body was read without a size bound, and the origin could be redirected through `PINA_LINT_DRIVER_*` environment variables.\n\nThe download path now fetches the `sha256` published beside the asset, compares it against the bytes received, and refuses to install on mismatch, a malformed checksum, or a checksum file that is not valid UTF-8. A new `VerifiedDriver` type wraps the bytes so only a checksum-verified download can reach the installer — a future edit cannot silently reintroduce an unverified path, because the compiler rejects it. The body read is capped at 256 MiB, and the release workflow now uploads the standalone driver's `.sha256` beside the binary, which it previously did not (only the archives carried published checksums).\n\nA locally built driver (`pina lint --build-driver`) keeps its existing trust root — a cargo build against the pinned `pina_lints` release — and installs through a separate function, since no published checksum exists for an artifact built on the local machine.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Fix the IDL size message and sanitize doc topics\n\n_Packages:_ _pina_cli_\n\n`pina idl fetch` rejected raw client output above 8 MiB but reported the limit as \"4 MiB\". The check and the message now share one constant, `MAX_RAW_HEX_BYTES`, so the two cannot drift again, and the message states the true 8 MiB limit.\n\n`pina docs <topic>` joined the caller-supplied topic directly into `<PINA_TEMPLATES_DIR>/<topic>.t.md`, so a topic such as `../../etc/passwd` escaped the template directory. Only the file-name component of the topic is now used, and a topic with no file-name component fails with a clear error.\n\nThe unused `tar` dependency is gone from `pina_cli` and the workspace dependency table; nothing in the crate referenced it, and the only other lockfile consumer is `agave-snapshots`, which declares it directly.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Read transition offsets from the stored schema\n\n_Packages:_ _pina_cli_\n\nA generated automatic transition derived its byte offsets from a source schema with dropped and renamed fields already resolved. The bytes it edits are the stored layout, where a dropped field still occupies its space, so any field after a removal was read from the wrong offset and a field after a rename could be read from the wrong one too. The destination validation accepted the result whenever the shifted bytes still formed a valid layout, which made the corruption silent: `{ a, b, c } -> { a, c }` copied `a` to `a` and left `c` reading the old `b`. `SOURCE_SIZE` was wrong in the same way, understating the stored payload.\n\nThe planner now proves a transition from the stored schema against the developer's resolved intent and emits its proof: surviving fields copy from their stored offsets, dropped fields keep occupying their bytes, and every stored field must be either copied or explicitly discarded. A change that no single copy order can express — one field moving right while another moves left, a same-name type change, two destination fields reading one stored field — falls back to a manual transition instead of emitting bytes its own proof does not justify.\n\nFixed-layout accounts gain a manual escape hatch. `--manual <field>` names an added field whose conversion the developer writes. The recorded transition mode and renames carry that intent, so repeated `make` runs keep the authored body instead of regenerating an automatic transition over it. Pairing it with `--rename` allows a rename whose type changed, which a verbatim byte copy cannot express; the previously impossible \"combine `first_name` and `last_name` into `name`\" migration is now a generated manual draft with the correct stored sizes. Contradictory answers fail closed in both directions, including across the persisted `[migrations.answers]` table.\n\nA generated manual draft now carries an offset table in its header: one row per field with its payload-relative range in the stored layout and in the destination, `removed` for acknowledged discards, and prefix notes for compact tails. The developer implementing the body can see at a glance which bytes can move and which stay put.\n\nA recorded manual transition now scopes itself to the hop it was written for. Previously the recorded mode seeded the _next_ adjacent change too, so any hop after a hand-written transition was branded manual forever, even one the byte-level proof fully justifies — removing a field two versions after a narrowing conversion wrongly demanded another manual body. Repeated `make` runs over the manual hop's own draft still replay its recorded mode, so authored bodies remain stable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Add the multisig example and fix generators\n\n_Packages:_ _pina_cli_, _pina_codama_renderer_cli_\n\n`examples/multisig_program` is a production-shaped multisig built on pina: bitmask voting over a sorted compact roster, unified proposal accounts carrying compiled vault messages or config action streams, timelocked execution with per-proposal ephemeral signers, vote revocation, proposal expiry, spending limits, rent collection, a generic Anchor-layout legacy import, and the full migration envelope. Building it surfaced two generator defects that this changeset also ships.\n\nFreshly scaffolded Rust CLI clients hard-coded `../../rust/<example>` as the dependency path, but the crate lives three directories deeper, so the manifest never resolved; existing examples only survived because update mode preserves their committed manifests, meaning no new example could scaffold a working CLI. The path is now derived from the configured rust and CLI output roots. The Dart CLI renderer emitted boolean instruction arguments as mandatory string options cast to `as String`, which failed `dart analyze` with a String-to-bool error; booleans are now declared as flags and read as `bool`, defaulting to false. The `pina docs` snapshot scratch space also moved under the workspace `target/` directory so recorded argument paths keep the same relative form on every machine and CI runner instead of leaking environment-specific absolute temp paths.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #466](https://github.com/pina-rs/pina/pull/466) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#462](https://github.com/pina-rs/pina/issues/462), [#491](https://github.com/pina-rs/pina/issues/491)",
							"- **pina_cli**: **Document the lint-driver origin overrides as operator-only.** `PINA_LINT_DRIVER_RELEASE`, `_REPO`, and `_BASE_URL` redirect the driver download and its checksum together, so they are a trusted-origin control rather than a convenience knob: whatever serves the override chooses which binary the CLI runs. The constants now say so, which is the documentation half of the 2026-09-21 audit's supply-chain finding; the version-pinned `npx` renderer fetches stay registry-trust-by-design.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Reject Cargo library names that cannot be path components\n\n_Packages:_ _pina_cli_\n\nCargo accepts any string as a `[lib] name` and reports it verbatim through `cargo metadata`, so a checked-in manifest controls the value. Pina joined that name into file and directory paths (`target/deploy`, generated IDL files, client crate directories) without validating it, so `[lib] name = \"../../escaped\"` made `pina generate` write outside the project root — reproduced with an artifact landing next to the project directory while the command reported success.\n\n`Project::discover` now validates the library name once, where it enters the project model, and rejects anything outside `[A-Za-z0-9_-]` with the new `ProjectError::InvalidLibraryName`. Cargo already refuses the empty name before metadata reaches pina. Validating at the ingestion point keeps every current and future path join safe instead of patching individual sinks, and no legitimate library name is affected because Cargo's own naming rules are a subset of the accepted set.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_macros_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Qualify every `Address` in generated PDA code\n\n_Packages:_ _pina_macros_\n\n`#[pda]` emitted its generated `Address` types unqualified: the `program_id` parameter of `try_find_pda`, `find_pda`, and `assert_seeds`, and the seed parameter and stored field types of the generated seeds struct. Those spellings resolve against the user's own scope, so a program with its own type named `Address` could silently change which address type a generated signature takes. Every generated `Address` is now the crate-qualified form, matching what the surrounding generated code and the `#[account]` schema path already emitted.\n\nThe expansion also emits the same identity proof the schema path uses — `const _: fn(Address) -> pina::Address = |value| value;` — but only when a declaration actually has an `Address` seed, so a numeric-only `#[pda]` never requires `Address` in scope. The proof binds the caller's bare `Address` spelling to the crate's type, so a future unqualified spelling is a compile error in the user's crate rather than a silent shadow.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Reject PDA declarations that cannot derive\n\n_Packages:_ _pina_macros_\n\nEvery generated `#[pda]` derivation appends a bump seed: `try_find_pda` and `find_pda` search for the canonical one, `with_bump` supplies the stored or explicit one, and the generated loaders derive from the stored field. The runtime caps a derivation at 16 seeds total, so a declaration of 16 seeds produced 17 and no instruction could ever load the account.\n\nThe macro now caps the declared list at 15 seeds and names both numbers in the error. The cap applies whether or not the declaration has a `bump =` field, because the bump-free variant derives through `try_find_pda` and appends a bump there. A declaration that previously compiled with 16 seeds was already unreachable at runtime; it now fails the build instead of shipping a PDA that always returns `InvalidSeeds`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Fix generated CLI payer and PDA handling\n\n_Packages:_ _pina_cli_renderer_, _pina_codama_renderer_cli_\n\nThe generated TypeScript, Dart, and Rust CLIs had related defects rooted in the renderers. Fixed-seed PDA accounts could not be fetched from the TypeScript or Dart CLIs: `--address` was mandatory even though the help text promised PDA derivation, while accounts without any PDA node kept requiring it. The Dart `fetch` command also registered no subcommands, so `fetch <account>` could never dispatch.\n\nPayer-resolution accounts (accounts defaulted to the transaction payer) accepted an override option in every CLI, but the override was either silently ignored (TypeScript) or produced a transaction whose signer meta pointed at an address no loaded keypair signs for (Dart and Rust). Since these CLIs sign with exactly one keypair, all three renderers now resolve payer-resolution accounts to the loaded payer and stop emitting the unusable override options.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #486](https://github.com/pina-rs/pina/pull/486) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Keep untrusted IDL names inside generated doc comments\n\n_Packages:_ _pina_cpi_renderer_\n\n`CamelCaseString` derives `Deserialize` on its inner `String`, so the normalization its constructor applies never runs for JSON input: a name read from an IDL reaches the renderer verbatim, newlines included. Two sites interpolated such a name straight into a single-line `///` comment — the CPI account line in `render_account` and the argument line in `render_argument` — so everything after an embedded newline was emitted as uncommented Rust. A crafted IDL handed to `pina cpi --idl` produced a generated crate containing attacker-chosen items, which then compile (and execute under `#[cfg(test)]`) in the victim's workspace.\n\nBoth sites now route through `render_doc`, which splits on newlines and prefixes every line as a comment, so an embedded newline becomes another comment line instead of code. `pina_cpi_renderer` gains a regression test that deserializes a malicious IDL through the same `read_root_node` path `pina cpi` uses, asserts the newline survives parsing (so the test keeps exercising the real path), and fails if any payload line escapes its comment.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Fix CLI Explorer links and keypair validation\n\n_Packages:_ _pina_codama_renderer_cli_\n\nThe Dart CLI renderer printed an Explorer URL without a cluster selector, so a signature from a devnet or testnet run linked to the mainnet Explorer page. It now derives the selector from the resolved endpoint and appends `?cluster=devnet` or `?cluster=testnet`. Mainnet Beta and arbitrary custom endpoints stay bare, because the Explorer only understands the named clusters and a guessed selector for an unknown host would point at the wrong chain. The match is on the parsed hostname rather than a substring, so a custom endpoint that merely contains a cluster's host as a path or subdomain is not mislabeled.\n\nBoth renderers also accepted a keypair array of 64 values without checking the values themselves. `Uint8Array` and `Uint8List` wrap out-of-range bytes rather than rejecting them, so a malformed keypair file produced a different signer instead of an error — one that would simply fail every transaction with no explanation. Each byte must now be an integer from 0 to 255, and anything else fails with the existing keypair-loading error.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #465](https://github.com/pina-rs/pina/pull/465) · _Closed issues:_ [#462](https://github.com/pina-rs/pina/issues/462) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					},
					{
						"title": "Notes",
						"collapsed": false,
						"entries": [
							"#### Document the cost of the ATA address checks\n\n_Packages:_ _pina_\n\n`assert_associated_token_address` and `as_associated_token_account` each perform a canonical associated-token-address search, which costs on the order of a thousand compute units and grows with how far the address's canonical bump sits below 255. Both doc comments now state that cost and warn against calling the assert and the loader with the same wallet, mint, and token program in one handler, because the second call repeats a search the first already paid.\n\nThe guidance consolidates what two example handlers (`vesting_program`'s `Claim` and `Cancel`) had drifted into — each paid the search twice per instruction until the companion example change removed the redundant calls and recorded the measured savings. The same audit confirmed `escrow_program`'s `Take` derives its `taker_ata_b` exactly once, so its search is not redundant and stays. Docs-only change; no behavior, wire, or measurement impact.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #488](https://github.com/pina-rs/pina/pull/488) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#487](https://github.com/pina-rs/pina/issues/487), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Drop duplicated ATA derivations from hot paths\n\n_Packages:_ _pina_\n\nThe canonical associated-token-address search costs roughly 1,500 CU per call on Solana because every attempt re-hashes the seeds and re-runs the point-decompression check, and the total scales with how far the address's canonical bump sits below 255. Two handlers paid that search twice for the same account in one instruction:\n\n- `vesting_program`'s `Claim` asserted the vault was the schedule's associated token account in the validation chain and then derived the identical `(wallet, mint, token program)` tuple again when it read the vault balance. The chain assert is gone; the balance load (`as_associated_token_account`) calls the exact same address validation and still rejects a caller-supplied account, which is the property that stops the schedule's signature from draining a foreign vault. The check runs before any state mutation or CPI.\n- `vesting_program`'s `Cancel` had the same assert-then-load pair for the refund vault. The chain assert is gone and the refund-balance load is the vault's only derivation, again ahead of the `cancelled` write and every CPI.\n\nMeasured on the Surfpool simulation maximum (the same statistic the performance policy enforces): `vesting_program/claim` 22,202 to 20,642 (-1,560, -7.0%) and `vesting_program/cancel` 29,281 to 27,722 (-1,559, -5.3%). Failure attribution is preserved: the surviving load calls the identical `assert_associated_token_address` the removed chain step called, so a substituted vault still fails with `InvalidSeeds`.\n\n`escrow_program`'s `Take` was audited for the same shape and deliberately left alone: its `taker_ata_b` derivation is the account's only canonical pin (the handler's `CreateIdempotent` CPI derives the maker's ATA, and the transfer CPI only proves signer authority), so removing it would let a taker pay from any mint-B account they control. Two adversarial Surfpool cases added here prove both sides: a foreign vault is rejected by both vesting handlers with no state change, and `Take` rejects both a foreign and a funded non-canonical taker-controlled token-B account.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #487](https://github.com/pina-rs/pina/pull/487) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Retry the lint driver probe while the kernel reports it busy\n\n_Packages:_ _pina_\n\n`pina lint` starts the driver it just installed to check that it loads. The kernel refuses an `exec` while a write descriptor for the same file is still open — including one a concurrent fork inherited — and the install-then-probe sequence can overlap with that window, so the probe failed with a busy error on a driver that was perfectly runnable. The spawn now retries briefly for that one error and fails immediately for anything else, which also stops the driver tests from flaking under a parallel test run.\n\n`pina` carries no behavior change in this release line; the entry records the added test coverage for the public `into_discriminator!` macro.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #461](https://github.com/pina-rs/pina/pull/461) · _Related issues:_ [#457](https://github.com/pina-rs/pina/issues/457), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Reject empty escrow offers and prove the maker is writable\n\n_Packages:_ _pina_\n\n`escrow_program`'s `Make` handler now rejects an offer whose `amount_a` or `amount_b` is zero with a new `EscrowError::EmptyOffer` (code 2), before any account is created or any token moves. A zero `amount_b` let a maker escrow token A for nothing, and a zero `amount_a` let a taker receive the vault for nothing; both are fat-finger shapes rather than real offers. The variant is new rather than a reuse of `OfferKeyMismatch` or `TokenAccountMismatch`, because wire values are part of the program ABI and remapping an existing code would change what deployed clients decode. The doc comment on the variant becomes the message in the regenerated Codama IDL and the Rust, JavaScript, and Dart clients.\n\n`Take` now asserts the maker is writable next to the existing address check, so a read-only maker fails with Pina's own `InvalidAccountData` diagnostic instead of a generic runtime failure from the vault or escrow close CPIs. The `&mut AccountView` field already enforced this at parse time, so the assert is defense in depth: the test passes with it removed. It costs 16 CU on `Take` (32,241 to 32,257), which is the price of keeping the requirement visible in the handler, and the maker is credited twice there (vault rent and escrow rent).\n\nTwo Surfpool cases cover the behavior: one asserts `EmptyOffer` and that no balance moves and no account is created, and one presents a read-only maker through a non-payer keypair and asserts the failure plus unchanged escrow, vault, and taker balances.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_, _pina_macros_, _pina_skill_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the remaining audit findings in the example programs\n\n_Packages:_ _pina_\n\n`multisig_program` closes three findings. `MultisigImport` binds its provenance: the legacy account must sit at the PDA its parsed `create_key` derives under the caller-named legacy program, and the holder of that `create_key` must sign the import, so a fabricated account carrying a borrowed roster of keys that never consented no longer imports (the instruction gains the `legacy_create_key` signer account). `ConfigAuthorityExecute` rejects `AddSpendingLimit` actions with a new `SpendingLimitRequiresProposal` error — creating an allowance moves vault funds, which stays behind threshold and timelock even in controlled mode, so the config authority manages configuration rather than custody. `VaultExecute` additionally protects every program-owned writable non-signer account, not just the multisig and the executing proposal, so a vault message can no longer write another proposal or a spending limit through a nested CPI.\n\n`escrow_program` gains a maker `Cancel` (discriminator 3) that refunds the full vault balance and closes both accounts, so a maker is no longer stranded until a taker appears. `prop_amm_program`'s `Update` now accepts the oracle's stored authority in addition to the benchmark's static key, making `RotateAuthority` mean what it says — previously the rotated key could never publish a price. The multisig readme documents the permissionless `ConfigInitialize` race (initialize the config in the deployment transaction or a sniper owns the fee treasury) and the spending-limit rules, and the staking readme documents the same first-initializer shape for pool creation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Align the advisory gates on deny.toml\n\n_Packages:_ _pina_cli_\n\n`security:deny` ran `cargo-deny check bans licenses sources`, so the `[advisories]` section of `deny.toml` — and the four suppressions it documented — never executed. The task now runs the advisories check with `--workspace`, because cargo-deny's default graph roots at the workspace members' normal dependencies and therefore omits the dev/test stack (surfpool, litesvm, mollusk, solana-runtime) where every suppressed advisory actually lives. Without `--workspace` the check cannot see them at all.\n\n`security:audit` carried a second, different seven-id ignore list. `deny.toml` is now the single source of advisory policy: the task reads the `[advisories] ignore` array out of `deny.toml` and passes each id to `cargo-audit`, so the two gates cannot drift. Every suppression in that array carries a reachability justification and a review date, and each needs a tracking issue filed on the repository — the entries are all dev/test-only today, verified with `cargo tree -i <spec> -p pina -p pina_abi -p pina_macros -p pina_cli` and the `--workspace` cargo-deny run, but a file cannot open issues on its own.\n\nThe `[advisories]` section also needed the `RUSTSEC-2021-0139`, `RUSTSEC-2024-0375`, `RUSTSEC-2024-0384`, `RUSTSEC-2020-0016`, `RUSTSEC-2025-0134`, and `RUSTSEC-2026-0173` entries that only the old cargo-audit list had, so moving advisories into the deny gate does not weaken it.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"- **pina_cli**: **Stop the deploy stdin test racing a startup deadline.** The test that proves `solana` inherits a closed stdin polled the child against a three-second deadline, which Pina's `cargo metadata` project discovery exceeded on a loaded runner. It now waits for the marker the fake `solana` writes after reading EOF and keeps a thirty-second deadline purely as a hang bound. Test-only; no release note.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #469](https://github.com/pina-rs/pina/pull/469) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_cli_renderer_, _pina_codama_renderer_, _pina_cpi_renderer_, _pina_profile_, _pina_sdk_ids_, _pina_test_, _pina_codama_nodes_, _pina_codama_renderer_cpi_, _pina_codama_renderer_cli_, _pina_skill_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"- **pina_cpi_renderer**: **Derive the scaffold release-line test from the crate version.** `scaffold_declares_pina_from_the_configured_source` computes the expected `pina` pin from `CARGO_PKG_VERSION` the same way `ScaffoldDependency::Published` renders it, so the assertion follows the crate's release line instead of holding a hard-coded `0.19` that the next version bump would leave behind.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #490](https://github.com/pina-rs/pina/pull/490) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"- **pina_codama_nodes**: **Pin the client contract test to the released surfaces.** The vesting and staking clients gained accounts in this release (the claim clock sysvar and refund path, the staking reward vault), so the package's contract test pins the new slot counts and roles. Test-only; no release note.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"- _Packages:_ _pina_cli_npm_, _pina_cli_darwin_arm64_, _pina_cli_darwin_x64_, _pina_cli_freebsd_x64_, _pina_cli_linux_arm64_gnu_, _pina_cli_linux_arm64_musl_, _pina_cli_linux_x64_gnu_, _pina_cli_linux_x64_musl_, _pina_cli_win32_arm64_msvc_, _pina_cli_win32_x64_msvc_ **Document the lint-driver origin overrides as operator-only.** `PINA_LINT_DRIVER_RELEASE`, `_REPO`, and `_BASE_URL` redirect the driver download and its checksum together, so they are a trusted-origin control rather than a convenience knob: whatever serves the override chooses which binary the CLI runs. The constants now say so, which is the documentation half of the 2026-09-21 audit's supply-chain finding; the version-pinned `npx` renderer fetches stay registry-trust-by-design.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Read transition offsets from the stored schema\n\n_Packages:_ _pina_skill_\n\nA generated automatic transition derived its byte offsets from a source schema with dropped and renamed fields already resolved. The bytes it edits are the stored layout, where a dropped field still occupies its space, so any field after a removal was read from the wrong offset and a field after a rename could be read from the wrong one too. The destination validation accepted the result whenever the shifted bytes still formed a valid layout, which made the corruption silent: `{ a, b, c } -> { a, c }` copied `a` to `a` and left `c` reading the old `b`. `SOURCE_SIZE` was wrong in the same way, understating the stored payload.\n\nThe planner now proves a transition from the stored schema against the developer's resolved intent and emits its proof: surviving fields copy from their stored offsets, dropped fields keep occupying their bytes, and every stored field must be either copied or explicitly discarded. A change that no single copy order can express — one field moving right while another moves left, a same-name type change, two destination fields reading one stored field — falls back to a manual transition instead of emitting bytes its own proof does not justify.\n\nFixed-layout accounts gain a manual escape hatch. `--manual <field>` names an added field whose conversion the developer writes. The recorded transition mode and renames carry that intent, so repeated `make` runs keep the authored body instead of regenerating an automatic transition over it. Pairing it with `--rename` allows a rename whose type changed, which a verbatim byte copy cannot express; the previously impossible \"combine `first_name` and `last_name` into `name`\" migration is now a generated manual draft with the correct stored sizes. Contradictory answers fail closed in both directions, including across the persisted `[migrations.answers]` table.\n\nA generated manual draft now carries an offset table in its header: one row per field with its payload-relative range in the stored layout and in the destination, `removed` for acknowledged discards, and prefix notes for compact tails. The developer implementing the body can see at a glance which bytes can move and which stay put.\n\nA recorded manual transition now scopes itself to the hop it was written for. Previously the recorded mode seeded the _next_ adjacent change too, so any hop after a hand-written transition was branded manual forever, even one the byte-level proof fully justifies — removing a field two versions after a narrowing conversion wrongly demanded another manual body. Repeated `make` runs over the manual hop's own draft still replay its recorded mode, so authored bodies remain stable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Generate every repository client with `pina generate`\n\n_Packages:_ _pina_skill_\n\n`pina generate` supersedes `pina codama generate`, which is removed. It discovers a project through its `pina.toml`, refreshes the IDL, and renders only the configured client ecosystems, so a repository generates its clients by running the command once per project instead of through a second command with its own path flags. The bundled skill reference documents the loop.\n\nDogfooding this path in the repository surfaced a bug in shared Dart CLI packages: `pina generate` published the whole staged `bin/` directory for each program, so generating one project deleted the entrypoints of every other project sharing the package. Only the single entrypoint owned by the invocation is published now.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #474](https://github.com/pina-rs/pina/pull/474) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					}
				]
			},
			"rendered": "## [0.20.0](https://github.com/pina-rs/pina/releases/tag/v0.20.0) (2026-09-23)\n\nGrouped release for `core`.\n\n### Breaking Changes\n\n#### Name the stored-bump compact PDA loader for what it verifies\n\n_Packages:_ _pina_, _pina_macros_\n\nThe release that split the compact PDA loader into `with_pda` and `with_checked_pda` gave the two methods names that do not state their difference: `with_pda` kept its name while its verification was weakened from a canonical bump search to a single derivation from the account's stored bump. A program upgrading across that release lost canonical-bump rejection at every existing `with_pda` call site with no compiler signal.\n\nThe weak loader is now `with_stored_bump_pda`, which says what it checks. The old `with_pda` name remains as a deprecated forwarding alias for one release, so existing calls compile but emit a `deprecated` warning naming the difference and pointing at `with_checked_pda` for the trustless case. `with_checked_pda` is unchanged.\n\n`pina_cli`'s IDL parser recognizes `with_stored_bump_pda` as a PDA loader alongside the other names, so account properties are unchanged.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Generate every repository client with `pina generate`\n\n_Packages:_ _pina_cli_\n\n`pina generate` supersedes `pina codama generate`, which is removed. It discovers a project through its `pina.toml`, refreshes the IDL, and renders only the configured client ecosystems, so a repository generates its clients by running the command once per project instead of through a second command with its own path flags. The bundled skill reference documents the loop.\n\nDogfooding this path in the repository surfaced a bug in shared Dart CLI packages: `pina generate` published the whole staged `bin/` directory for each program, so generating one project deleted the entrypoints of every other project sharing the package. Only the single entrypoint owned by the invocation is published now.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #474](https://github.com/pina-rs/pina/pull/474) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_cli_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_macros_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n### Features\n\n#### Add a checked discriminator write\n\n_Packages:_ _pina_\n\n`IntoDiscriminator::write_discriminator` returns `()` and drops the write when the destination slice is shorter than the discriminator: a `debug_assert!` fires in debug builds, but a release build silently leaves the buffer untouched. Generated code always sizes its buffers from `Self::BYTES`, so this only bit hand-written implementations, where a missing discriminator produces an account that looks initialized but decodes as an unknown type.\n\nThe new `IntoDiscriminator::try_write_discriminator` reports the undersized buffer with `PinaProgramError::DataTooShort` (0xFFFF_FFFA, \"Account or instruction data is shorter than the expected minimum\") instead. That is the variant `HasMigrationVersion::write_le` and the migration decoders already return for a destination that cannot hold the value, so a caller sees one error for every short-buffer case.\n\nThe method has a default implementation that length-checks and then delegates to `write_discriminator`, so a manual implementer inherits the checked path without writing anything; the primitive implementations (`u8`, `u16`, `u32`, `u64`) and `into_discriminator!` override it to write through `get_mut` in one step. `write_discriminator` is unchanged, and its doc comment now points new manual implementers at the checked form.\n\nTests cover short buffers of every length up to `BYTES` (including a short inner slice of a long array), exact and oversized buffers, and that the checked write produces the same bytes as the unchecked one.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Accept const capacities in schemas\n\n_Packages:_ _pina_, _pina_cli_, _pina_macros_\n\nCompact account and instruction schemas now accept a `const` item wherever they previously required an integer literal:\n\n```rust\nconst MAX_MEMBERS: usize = 24;\n\n#[account(discriminator = MultisigAccountType, compact)]\npub struct Multisig {\n\tpub bump: u8,\n\tpub member_keys: Vec<Address, MAX_MEMBERS>,\n}\n```\n\n`Vec<T, N>`, `PodVec<T, N, PFX>`, `String<N>`, `PodString<N, PFX>`, an `Option<...>` around them, and fixed `[T; N]` arrays and instruction arguments all resolve a capacity through a constant. Constants may be arithmetic over other constants and may be declared in any module of the crate, so one bound is declared once and reused in the account, the instruction that writes it, and the helper that sizes it.\n\nPina resolves each capacity during expansion and records the number it evaluates to. The ABI layer still receives concrete values, so replacing a literal with a constant of the same value leaves `migrations/manifest.json`, the generated `tests/abi_layout.rs` assertions, `MAX_SIZE`/`MIN_SIZE`/`HEADER_SIZE`, and `projected_bytes(...)` byte-identical, and `pina migrations check` and the Codama IDL unchanged.\n\nA capacity that cannot be evaluated at expansion time now fails the build with a diagnostic naming the expression and pointing at the workaround, instead of the previous `unsupported compact field` or `arrays require an integer literal length` message. An associated constant such as `Bounds::MAX_MEMBERS` is not resolved; declare the bound as a `const` item.\n\n`pina_abi` gains `SchemaConsts`, the shared evaluator both the macros and the CLI use so the two layers agree on what a constant means. `pina_cli` resolves capacities while it parses a program, so migration and IDL tooling reads the same numbers the compiler does.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #463](https://github.com/pina-rs/pina/pull/463) · _Closed issues:_ [#460](https://github.com/pina-rs/pina/issues/460) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Reject event schemas that cannot fit the SBF stack\n\n_Packages:_ _pina_, _pina_macros_\n\nThe generated `emit` function materializes a whole event record in one stack frame (`let mut record = [0u8; Self::SIZE];`) before calling into the runtime, so a large `#[event]` schema compiled cleanly and then exhausted the 4 KiB SBF stack at runtime — a self-inflicted denial of service with no compile-time signal.\n\n`pina` gains `MAX_EVENT_RECORD_BYTES`, set to `4096 - 512`. The 512-byte reserve is headroom for the callee frame and the caller's own frame, the same reasoning `MAX_MIGRATION_WORKSPACE` already applies to the historical-normalization workspace. Because the constant is public, the bound a caller can read is the bound the macro enforces.\n\n`pina_macros` emits a compile-time assertion in every `#[event]` expansion, so an oversized schema now fails the build with a message naming the struct and the remedy. A record exactly at the bound still compiles; the assertion is inclusive.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Match the Migrate trigger to the discriminator width\n\n_Packages:_ _pina_, _pina_macros_\n\n`#[discriminator]` reserves the all-ones value of the enum's own primitive, so a `u16` program reserves `0xffff` and a `u32` program reserves `0xffff_ffff`. The generated entrypoint guarded the reserved path with `is_migrate_instruction`, which only tests one byte, so for any program whose instruction enum used `primitive = u16`, `u32`, or `u64` the reserved discriminator never matched and `process_migrate` was unreachable.\n\n`pina` adds width-specific helpers alongside the existing one: `is_migrate_instruction_u16`, `is_migrate_instruction_u32`, and `is_migrate_instruction_u64`. Each matches only its own width, so a one-byte `0xff` is not a two-byte `0xffff` and neither prefix-matches the other. They compare the decoded little-endian value rather than a slice equality, keeping the inline comparison the one-byte helper already had instead of the `memcmp` call `data == CONSTANT.to_le_bytes()` would emit on SBF.\n\n`pina_macros` selects the helper from the enum's `primitive`, so `#[discriminator(entrypoint, migrations(...))]` on a wider discriminator now reaches the reserved path. The generated guard for a default `u8` program is byte-identical to before, so the common case takes no compute regression. The documentation on the reserved-discriminator constants and in `docs/src/migrations/flow.md` now names the width pairing.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Add an opt-in heap allocation entrypoint\n\n_Packages:_ _pina_\n\n`nostd_entrypoint!` installs `pinocchio::no_allocator!`, so a Pina program cannot use the heap at all: any dynamic allocation aborts at runtime. That stays the default, but programs that want allocation no longer have to hand-roll the entrypoint through Pina's `pinocchio` re-export.\n\nThe heap is off by default and opt-in twice over. Pina now depends on pinocchio with `default-features = false` and enables only `copy` and `sha2`, so pinocchio's `alloc` feature — and the `slice-cpi` code it gates — is no longer compiled into every Pina program. A new `alloc` feature forwards to it: enabling `pina/alloc` turns the heap on, and `nostd_entrypoint_alloc!` only exists when it is enabled. Calling the macro without the feature fails with a symbol named `ALLOC_FEATURE_REQUIRED_FOR_HEAP_ENTRYPOINT`, which spells out the remedy.\n\n`nostd_entrypoint_alloc!` is the same macro with one substitution — `pinocchio::default_allocator!` instead of `no_allocator!` — so a program that declares `extern crate alloc` can use `Box`, `Vec`, and the rest of `alloc` while keeping Pina's entrypoint wiring. `nostd_entrypoint!` and its expansion are unchanged; the heap path is purely opt-in and per program.\n\nThe macro's rustdoc documents the costs that `no_allocator!` currently makes impossible, because each is a failure mode an opted-in program can hit: the runtime grants a 32 KiB heap frame at 0 CU and anything larger needs a caller-sent `request_heap_frame` (multiple of 1024, at most 256 KiB, 8 CU per extra 32 KiB page); the bump allocator's `dealloc` is a no-op, so a transaction's peak heap use is the sum of every allocation it makes; an allocation the frame cannot satisfy aborts instead of returning a `ProgramError`; and the allocator stores its position in the first word of the region, so the usable bytes are the granted frame minus one word.\n\n`heap_alloc_program` exercises the new macro end to end. Its mollusk suite holds the heap frame at a fixed size and shows a fill inside the frame completing, a fill past it aborting, and the same fill succeeding once the frame is raised. Its Surfpool suite runs the same artifact on a real runtime, where `Box` round-trips through the bump allocator. Three doc sites that described allocator-freeness as an unconditional Pina property now say \"by default\".\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #465](https://github.com/pina-rs/pina/pull/465) · _Closed issues:_ [#462](https://github.com/pina-rs/pina/issues/462) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_cli_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Name the stored-bump compact PDA loader for what it verifies\n\n_Packages:_ _pina_cli_\n\nThe release that split the compact PDA loader into `with_pda` and `with_checked_pda` gave the two methods names that do not state their difference: `with_pda` kept its name while its verification was weakened from a canonical bump search to a single derivation from the account's stored bump. A program upgrading across that release lost canonical-bump rejection at every existing `with_pda` call site with no compiler signal.\n\nThe weak loader is now `with_stored_bump_pda`, which says what it checks. The old `with_pda` name remains as a deprecated forwarding alias for one release, so existing calls compile but emit a `deprecated` warning naming the difference and pointing at `with_checked_pda` for the trustless case. `with_checked_pda` is unchanged.\n\n`pina_cli`'s IDL parser recognizes `with_stored_bump_pda` as a PDA loader alongside the other names, so account properties are unchanged.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_lints_, _pina_macros_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Move the host stack to Surfpool 1.6 / Agave 4.2\n\n_Packages:_ _pina_test_\n\n`pina_test` anchored every host-side Solana dependency on the Agave 4.1 train because Surfpool 1.5 capped `litesvm` at `^0.14.0`, and that release does not compile against Agave 4.2. Surfpool 1.6 ships `litesvm` 0.16 on Agave 4.2, so the cap is gone and the workspace moves up with it:\n\n- `surfpool-sdk` `~1.5` → `~1.6`\n- `solana-client`, `solana-rpc-client`, and `solana-runtime` `~4.1` → `~4.2`\n- `mollusk-svm` 0.14.0 → 0.15.1, which is the Mollusk release built on Agave 4.2\n\nEvery other tilde range in the crate already accommodated the newer train, so nothing else moved by hand. The resolver now proves out a single `solana-runtime` 4.2.2 island; the remaining multi-version crates (`solana-message`, `solana-transaction`, `solana-fee-calculator`) are the program-side crates that `pinocchio` and the examples depend on and were already split before this change. The devenv `ifiokjr-nixpkgs` pin moves to the revision that packages `surfpool` 1.6.0 so the CLI and the SDK agree; that revision keeps agave at 4.2.2.\n\n`OfflineSurfnet::start` now selects `BlockProductionMode::Clock`. Surfpool 1.6.0's SDK defaults to `BlockProductionMode::Transaction`, and on an embedded offline instance that mode never confirms a submitted transaction: the client's confirmation loop polls `getSignatureStatuses` indefinitely and the test hangs with no error. The same suite passes in about five seconds in clock mode. This is a Surfpool regression rather than a Pina defect and is reported upstream as [surfpool#814](https://github.com/solana-foundation/surfpool/issues/814); the override comes out once that is fixed.\n\nThe upgrade also removes a documented limitation. Surfpool 1.5 could not derive CPI signers for PDAs with four or more seed arguments, so any program seeding a PDA that way could not run its Surfpool suite end to end. On 1.6.0 the vesting example's `[b\"vesting\", admin, beneficiary, mint]` plus bump PDA completes `Initialize` → `Claim` → `Cancel`, which the previous train could not reach. `crates/pina_test/readme.md` now records the fix instead of the caveat.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #482](https://github.com/pina-rs/pina/pull/482) · _Related issues:_ [#289](https://github.com/pina-rs/pina/issues/289), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491), [#802](https://github.com/pina-rs/pina/issues/802)\n\n### Fixes\n\n#### Adopt the Pinapod 0.4.3 compact-writer hardening\n\n_Packages:_ _pina_\n\nMove from the temporary `pinapod` 0.4.1 hold to 0.4.3, which brings the 0.4.2 commit-entry hardening at its intended compute-unit cost. 0.4.2 validates every generated compact buffer at `commit` before tails are relocated: a writer whose layout is corrupt fails closed before any offset or pointer work, and a preflight and commit length disagreement returns `InvalidLength` in release builds instead of relying on a debug assertion. By default, semantic defects such as non-UTF-8 tail bytes or out-of-range tags are caught at the next interpreting boundary rather than by `commit` itself; the `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it. 0.4.3 keeps all of this and restores the compute-unit profile.\n\nThe intermediate 0.4.2 release was held back on a diagnosis that turned out to be wrong: Pina reported that 0.4.2 moved `commit` ahead of the inline writes and turned a length check into an unconditional panic. Neither was true — the panic-shaped block was 0.4.1's own `debug_assert_eq!` expansion, and `update` never changed order. [pinapod#36](https://github.com/pina-rs/pinapod/issues/36) recorded the correction and [pinapod#38](https://github.com/pina-rs/pinapod/issues/38) specified how to keep the safety without the cost, which is what 0.4.3 implements.\n\n`commit` entry now proves layout rather than semantics through `PinaPodCompact::validate_layout`: the storage length, each stored length prefix, its capacity, and the chained bounds. Relocation's pointer arithmetic consumes exactly those, so this is the complete precondition and not a weakening of it. The element walks that checked UTF-8, enum ranges, and tags move to the boundaries that interpret those bytes, which already had them — the `Ref` and `Mut` constructors, `updated_len`, and `try_initialize`'s post-commit check. Trivially valid element types such as `u64` and `u8` skip their walks entirely rather than relying on the compiler to delete a dead loop, which SBF at `-C opt-level=3` does not reliably do.\n\nAn account that is layout-valid but semantically invalid, such as non-UTF-8 bytes in an untouched tail, now commits and is rejected at the next read instead of by `commit` itself. The update path keeps its existing guarantee by construction, because `updated_len` runs the full validation over the same bytes the staged edits will produce. A `compact-commit-full-validation` feature restores the full walk at commit entry for consumers who prefer it.\n\nThe observable effect on Pina is the regenerated `pda.expanded.rs` snapshot, and the compute units that 0.4.2 added are gone: the `compact_accounts_program` instructions that regressed by 46 to 308 units under 0.4.2 measure at or below their 0.4.1 cost again.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #485](https://github.com/pina-rs/pina/pull/485) · _Related issues:_ [#38](https://github.com/pina-rs/pina/issues/38), [#39](https://github.com/pina-rs/pina/issues/39), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Stop enabling `bytemuck` on `solana-address`/`solana-pubkey`\n\n_Packages:_ _pina_\n\nThe workspace requested `solana-address/bytemuck` and `solana-pubkey/bytemuck`. Pina used `bytemuck` for its own pod primitives until the migration to `pinapod`, so nothing in the workspace reads the `Pod` or `Zeroable` derives those features add, and no first-party code calls a `bytemuck` API.\n\nBoth entries now request what Pina actually depends on. `solana-address` asks for `copy`, which derives `Copy` on `Address` — the bound `pinapod`'s `ZcElem` requires and the only part of `bytemuck`'s feature set that was load-bearing. Requesting it directly removes the dependence on `bytemuck`'s implication, and on `pinocchio` and `pinapod` enabling `copy` transitively. `solana-pubkey` no longer requests `bytemuck` at all.\n\nDeployable programs are unchanged: `bytemuck` contributed no symbols to any built program, so all 24 `bpf-entrypoint` examples produce byte-identical sizes and an identical instruction mix with and without it. This is a dependency-graph cleanup, not a size or compute-unit change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #464](https://github.com/pina-rs/pina/pull/464) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_, _pina_codama_nodes_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Fix the CPI scaffold's pina dependency for workspace crates\n\n_Packages:_ _pina_cli_, _pina_cpi_renderer_\n\n`pina generate` scaffolds a CPI crate for every example, but the scaffold hardcoded `pina = { version = \"0.17\", default-features = false }` — a registry pin that stopped matching this workspace's own `pina` releases. Every previously committed CPI crate had already been pinned to `pina = { workspace = true }` by hand, so the defect only surfaced on the next newly generated example: it added a second `pina` to the dependency graph, which makes `cargo kani -p pina` fail with \"multiple `pina` packages in your project\" and breaks both Kani proof tiers.\n\nThe scaffold now takes the dependency source from the caller. `pina generate`, whose output is always a workspace member, inherits the workspace dependency; `pina cpi` and `pina import`, whose output lives in the caller's own project with no workspace entry to inherit, pin the renderer's own release line instead of a stale literal. A regression test covers both modes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #489](https://github.com/pina-rs/pina/pull/489) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#486](https://github.com/pina-rs/pina/issues/486), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Retry the lint driver probe while the kernel reports it busy\n\n_Packages:_ _pina_cli_\n\n`pina lint` starts the driver it just installed to check that it loads. The kernel refuses an `exec` while a write descriptor for the same file is still open — including one a concurrent fork inherited — and the install-then-probe sequence can overlap with that window, so the probe failed with a busy error on a driver that was perfectly runnable. The spawn now retries briefly for that one error and fails immediately for anything else, which also stops the driver tests from flaking under a parallel test run.\n\n`pina` carries no behavior change in this release line; the entry records the added test coverage for the public `into_discriminator!` macro.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #461](https://github.com/pina-rs/pina/pull/461) · _Related issues:_ [#457](https://github.com/pina-rs/pina/issues/457), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Emit valid TypeScript for reserved Migrate type arguments\n\n_Packages:_ _pina_cli_\n\nThe reserved `Migrate` composer closed its `MigrateInput<...>` type argument list with a trailing comma. That is a `TS1009` syntax error — trailing commas are legal in type parameter _declarations_ but not in type argument _uses_ — so raw `pina generate` output could not be parsed by `tsc`, `esbuild`, or any tool stricter than dprint's formatter. Repository pipelines only survived because `verify-codama-idls.sh` runs `dprint fmt` over the generated tree, silently deleting the comma before anything parsed the file. A consumer running `pina generate` without that reformatting step got uncompilable clients.\n\nThe generator now emits the comma-free shape dprint normalizes to, so committed clients are unchanged and raw output is valid TypeScript. Caught by the migrations walkthrough, which loads the freshly generated client through esbuild without a formatting pass.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Verify the lint driver download before installing it\n\n_Packages:_ _pina_cli_\n\n`pina lint` fetches a prebuilt driver binary, marks it executable, and runs it as its version probe. The download trusted the transport alone: no checksum was verified, the response body was read without a size bound, and the origin could be redirected through `PINA_LINT_DRIVER_*` environment variables.\n\nThe download path now fetches the `sha256` published beside the asset, compares it against the bytes received, and refuses to install on mismatch, a malformed checksum, or a checksum file that is not valid UTF-8. A new `VerifiedDriver` type wraps the bytes so only a checksum-verified download can reach the installer — a future edit cannot silently reintroduce an unverified path, because the compiler rejects it. The body read is capped at 256 MiB, and the release workflow now uploads the standalone driver's `.sha256` beside the binary, which it previously did not (only the archives carried published checksums).\n\nA locally built driver (`pina lint --build-driver`) keeps its existing trust root — a cargo build against the pinned `pina_lints` release — and installs through a separate function, since no published checksum exists for an artifact built on the local machine.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Fix the IDL size message and sanitize doc topics\n\n_Packages:_ _pina_cli_\n\n`pina idl fetch` rejected raw client output above 8 MiB but reported the limit as \"4 MiB\". The check and the message now share one constant, `MAX_RAW_HEX_BYTES`, so the two cannot drift again, and the message states the true 8 MiB limit.\n\n`pina docs <topic>` joined the caller-supplied topic directly into `<PINA_TEMPLATES_DIR>/<topic>.t.md`, so a topic such as `../../etc/passwd` escaped the template directory. Only the file-name component of the topic is now used, and a topic with no file-name component fails with a clear error.\n\nThe unused `tar` dependency is gone from `pina_cli` and the workspace dependency table; nothing in the crate referenced it, and the only other lockfile consumer is `agave-snapshots`, which declares it directly.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Read transition offsets from the stored schema\n\n_Packages:_ _pina_cli_\n\nA generated automatic transition derived its byte offsets from a source schema with dropped and renamed fields already resolved. The bytes it edits are the stored layout, where a dropped field still occupies its space, so any field after a removal was read from the wrong offset and a field after a rename could be read from the wrong one too. The destination validation accepted the result whenever the shifted bytes still formed a valid layout, which made the corruption silent: `{ a, b, c } -> { a, c }` copied `a` to `a` and left `c` reading the old `b`. `SOURCE_SIZE` was wrong in the same way, understating the stored payload.\n\nThe planner now proves a transition from the stored schema against the developer's resolved intent and emits its proof: surviving fields copy from their stored offsets, dropped fields keep occupying their bytes, and every stored field must be either copied or explicitly discarded. A change that no single copy order can express — one field moving right while another moves left, a same-name type change, two destination fields reading one stored field — falls back to a manual transition instead of emitting bytes its own proof does not justify.\n\nFixed-layout accounts gain a manual escape hatch. `--manual <field>` names an added field whose conversion the developer writes. The recorded transition mode and renames carry that intent, so repeated `make` runs keep the authored body instead of regenerating an automatic transition over it. Pairing it with `--rename` allows a rename whose type changed, which a verbatim byte copy cannot express; the previously impossible \"combine `first_name` and `last_name` into `name`\" migration is now a generated manual draft with the correct stored sizes. Contradictory answers fail closed in both directions, including across the persisted `[migrations.answers]` table.\n\nA generated manual draft now carries an offset table in its header: one row per field with its payload-relative range in the stored layout and in the destination, `removed` for acknowledged discards, and prefix notes for compact tails. The developer implementing the body can see at a glance which bytes can move and which stay put.\n\nA recorded manual transition now scopes itself to the hop it was written for. Previously the recorded mode seeded the _next_ adjacent change too, so any hop after a hand-written transition was branded manual forever, even one the byte-level proof fully justifies — removing a field two versions after a narrowing conversion wrongly demanded another manual body. Repeated `make` runs over the manual hop's own draft still replay its recorded mode, so authored bodies remain stable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Add the multisig example and fix generators\n\n_Packages:_ _pina_cli_, _pina_codama_renderer_cli_\n\n`examples/multisig_program` is a production-shaped multisig built on pina: bitmask voting over a sorted compact roster, unified proposal accounts carrying compiled vault messages or config action streams, timelocked execution with per-proposal ephemeral signers, vote revocation, proposal expiry, spending limits, rent collection, a generic Anchor-layout legacy import, and the full migration envelope. Building it surfaced two generator defects that this changeset also ships.\n\nFreshly scaffolded Rust CLI clients hard-coded `../../rust/<example>` as the dependency path, but the crate lives three directories deeper, so the manifest never resolved; existing examples only survived because update mode preserves their committed manifests, meaning no new example could scaffold a working CLI. The path is now derived from the configured rust and CLI output roots. The Dart CLI renderer emitted boolean instruction arguments as mandatory string options cast to `as String`, which failed `dart analyze` with a String-to-bool error; booleans are now declared as flags and read as `bool`, defaulting to false. The `pina docs` snapshot scratch space also moved under the workspace `target/` directory so recorded argument paths keep the same relative form on every machine and CI runner instead of leaking environment-specific absolute temp paths.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #466](https://github.com/pina-rs/pina/pull/466) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#462](https://github.com/pina-rs/pina/issues/462), [#491](https://github.com/pina-rs/pina/issues/491)\n\n- **pina_cli**: **Document the lint-driver origin overrides as operator-only.** `PINA_LINT_DRIVER_RELEASE`, `_REPO`, and `_BASE_URL` redirect the driver download and its checksum together, so they are a trusted-origin control rather than a convenience knob: whatever serves the override chooses which binary the CLI runs. The constants now say so, which is the documentation half of the 2026-09-21 audit's supply-chain finding; the version-pinned `npx` renderer fetches stay registry-trust-by-design.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n#### Reject Cargo library names that cannot be path components\n\n_Packages:_ _pina_cli_\n\nCargo accepts any string as a `[lib] name` and reports it verbatim through `cargo metadata`, so a checked-in manifest controls the value. Pina joined that name into file and directory paths (`target/deploy`, generated IDL files, client crate directories) without validating it, so `[lib] name = \"../../escaped\"` made `pina generate` write outside the project root — reproduced with an artifact landing next to the project directory while the command reported success.\n\n`Project::discover` now validates the library name once, where it enters the project model, and rejects anything outside `[A-Za-z0-9_-]` with the new `ProjectError::InvalidLibraryName`. Cargo already refuses the empty name before metadata reaches pina. Validating at the ingestion point keeps every current and future path join safe instead of patching individual sinks, and no legitimate library name is affected because Cargo's own naming rules are a subset of the accepted set.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_macros_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Qualify every `Address` in generated PDA code\n\n_Packages:_ _pina_macros_\n\n`#[pda]` emitted its generated `Address` types unqualified: the `program_id` parameter of `try_find_pda`, `find_pda`, and `assert_seeds`, and the seed parameter and stored field types of the generated seeds struct. Those spellings resolve against the user's own scope, so a program with its own type named `Address` could silently change which address type a generated signature takes. Every generated `Address` is now the crate-qualified form, matching what the surrounding generated code and the `#[account]` schema path already emitted.\n\nThe expansion also emits the same identity proof the schema path uses — `const _: fn(Address) -> pina::Address = |value| value;` — but only when a declaration actually has an `Address` seed, so a numeric-only `#[pda]` never requires `Address` in scope. The proof binds the caller's bare `Address` spelling to the crate's type, so a future unqualified spelling is a compile error in the user's crate rather than a silent shadow.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Reject PDA declarations that cannot derive\n\n_Packages:_ _pina_macros_\n\nEvery generated `#[pda]` derivation appends a bump seed: `try_find_pda` and `find_pda` search for the canonical one, `with_bump` supplies the stored or explicit one, and the generated loaders derive from the stored field. The runtime caps a derivation at 16 seeds total, so a declaration of 16 seeds produced 17 and no instruction could ever load the account.\n\nThe macro now caps the declared list at 15 seeds and names both numbers in the error. The cap applies whether or not the declaration has a `bump =` field, because the bump-free variant derives through `try_find_pda` and appends a bump there. A declaration that previously compiled with 16 seeds was already unreachable at runtime; it now fails the build instead of shipping a PDA that always returns `InvalidSeeds`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Fix generated CLI payer and PDA handling\n\n_Packages:_ _pina_cli_renderer_, _pina_codama_renderer_cli_\n\nThe generated TypeScript, Dart, and Rust CLIs had related defects rooted in the renderers. Fixed-seed PDA accounts could not be fetched from the TypeScript or Dart CLIs: `--address` was mandatory even though the help text promised PDA derivation, while accounts without any PDA node kept requiring it. The Dart `fetch` command also registered no subcommands, so `fetch <account>` could never dispatch.\n\nPayer-resolution accounts (accounts defaulted to the transaction payer) accepted an override option in every CLI, but the override was either silently ignored (TypeScript) or produced a transaction whose signer meta pointed at an address no loaded keypair signs for (Dart and Rust). Since these CLIs sign with exactly one keypair, all three renderers now resolve payer-resolution accounts to the loaded payer and stop emitting the unusable override options.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #486](https://github.com/pina-rs/pina/pull/486) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Keep untrusted IDL names inside generated doc comments\n\n_Packages:_ _pina_cpi_renderer_\n\n`CamelCaseString` derives `Deserialize` on its inner `String`, so the normalization its constructor applies never runs for JSON input: a name read from an IDL reaches the renderer verbatim, newlines included. Two sites interpolated such a name straight into a single-line `///` comment — the CPI account line in `render_account` and the argument line in `render_argument` — so everything after an embedded newline was emitted as uncommented Rust. A crafted IDL handed to `pina cpi --idl` produced a generated crate containing attacker-chosen items, which then compile (and execute under `#[cfg(test)]`) in the victim's workspace.\n\nBoth sites now route through `render_doc`, which splits on newlines and prefixes every line as a comment, so an embedded newline becomes another comment line instead of code. `pina_cpi_renderer` gains a regression test that deserializes a malicious IDL through the same `read_root_node` path `pina cpi` uses, asserts the newline survives parsing (so the test keeps exercising the real path), and fails if any payload line escapes its comment.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Fix CLI Explorer links and keypair validation\n\n_Packages:_ _pina_codama_renderer_cli_\n\nThe Dart CLI renderer printed an Explorer URL without a cluster selector, so a signature from a devnet or testnet run linked to the mainnet Explorer page. It now derives the selector from the resolved endpoint and appends `?cluster=devnet` or `?cluster=testnet`. Mainnet Beta and arbitrary custom endpoints stay bare, because the Explorer only understands the named clusters and a guessed selector for an unknown host would point at the wrong chain. The match is on the parsed hostname rather than a substring, so a custom endpoint that merely contains a cluster's host as a path or subdomain is not mislabeled.\n\nBoth renderers also accepted a keypair array of 64 values without checking the values themselves. `Uint8Array` and `Uint8List` wrap out-of-range bytes rather than rejecting them, so a malformed keypair file produced a different signer instead of an error — one that would simply fail every transaction with no explanation. Each byte must now be an integer from 0 to 255, and anything else fails with the existing keypair-loading error.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #465](https://github.com/pina-rs/pina/pull/465) · _Closed issues:_ [#462](https://github.com/pina-rs/pina/issues/462) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n### Notes\n\n#### Document the cost of the ATA address checks\n\n_Packages:_ _pina_\n\n`assert_associated_token_address` and `as_associated_token_account` each perform a canonical associated-token-address search, which costs on the order of a thousand compute units and grows with how far the address's canonical bump sits below 255. Both doc comments now state that cost and warn against calling the assert and the loader with the same wallet, mint, and token program in one handler, because the second call repeats a search the first already paid.\n\nThe guidance consolidates what two example handlers (`vesting_program`'s `Claim` and `Cancel`) had drifted into — each paid the search twice per instruction until the companion example change removed the redundant calls and recorded the measured savings. The same audit confirmed `escrow_program`'s `Take` derives its `taker_ata_b` exactly once, so its search is not redundant and stays. Docs-only change; no behavior, wire, or measurement impact.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #488](https://github.com/pina-rs/pina/pull/488) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#487](https://github.com/pina-rs/pina/issues/487), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Drop duplicated ATA derivations from hot paths\n\n_Packages:_ _pina_\n\nThe canonical associated-token-address search costs roughly 1,500 CU per call on Solana because every attempt re-hashes the seeds and re-runs the point-decompression check, and the total scales with how far the address's canonical bump sits below 255. Two handlers paid that search twice for the same account in one instruction:\n\n- `vesting_program`'s `Claim` asserted the vault was the schedule's associated token account in the validation chain and then derived the identical `(wallet, mint, token program)` tuple again when it read the vault balance. The chain assert is gone; the balance load (`as_associated_token_account`) calls the exact same address validation and still rejects a caller-supplied account, which is the property that stops the schedule's signature from draining a foreign vault. The check runs before any state mutation or CPI.\n- `vesting_program`'s `Cancel` had the same assert-then-load pair for the refund vault. The chain assert is gone and the refund-balance load is the vault's only derivation, again ahead of the `cancelled` write and every CPI.\n\nMeasured on the Surfpool simulation maximum (the same statistic the performance policy enforces): `vesting_program/claim` 22,202 to 20,642 (-1,560, -7.0%) and `vesting_program/cancel` 29,281 to 27,722 (-1,559, -5.3%). Failure attribution is preserved: the surviving load calls the identical `assert_associated_token_address` the removed chain step called, so a substituted vault still fails with `InvalidSeeds`.\n\n`escrow_program`'s `Take` was audited for the same shape and deliberately left alone: its `taker_ata_b` derivation is the account's only canonical pin (the handler's `CreateIdempotent` CPI derives the maker's ATA, and the transfer CPI only proves signer authority), so removing it would let a taker pay from any mint-B account they control. Two adversarial Surfpool cases added here prove both sides: a foreign vault is rejected by both vesting handlers with no state change, and `Take` rejects both a foreign and a funded non-canonical taker-controlled token-B account.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #487](https://github.com/pina-rs/pina/pull/487) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Retry the lint driver probe while the kernel reports it busy\n\n_Packages:_ _pina_\n\n`pina lint` starts the driver it just installed to check that it loads. The kernel refuses an `exec` while a write descriptor for the same file is still open — including one a concurrent fork inherited — and the install-then-probe sequence can overlap with that window, so the probe failed with a busy error on a driver that was perfectly runnable. The spawn now retries briefly for that one error and fails immediately for anything else, which also stops the driver tests from flaking under a parallel test run.\n\n`pina` carries no behavior change in this release line; the entry records the added test coverage for the public `into_discriminator!` macro.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #461](https://github.com/pina-rs/pina/pull/461) · _Related issues:_ [#457](https://github.com/pina-rs/pina/issues/457), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Reject empty escrow offers and prove the maker is writable\n\n_Packages:_ _pina_\n\n`escrow_program`'s `Make` handler now rejects an offer whose `amount_a` or `amount_b` is zero with a new `EscrowError::EmptyOffer` (code 2), before any account is created or any token moves. A zero `amount_b` let a maker escrow token A for nothing, and a zero `amount_a` let a taker receive the vault for nothing; both are fat-finger shapes rather than real offers. The variant is new rather than a reuse of `OfferKeyMismatch` or `TokenAccountMismatch`, because wire values are part of the program ABI and remapping an existing code would change what deployed clients decode. The doc comment on the variant becomes the message in the regenerated Codama IDL and the Rust, JavaScript, and Dart clients.\n\n`Take` now asserts the maker is writable next to the existing address check, so a read-only maker fails with Pina's own `InvalidAccountData` diagnostic instead of a generic runtime failure from the vault or escrow close CPIs. The `&mut AccountView` field already enforced this at parse time, so the assert is defense in depth: the test passes with it removed. It costs 16 CU on `Take` (32,241 to 32,257), which is the price of keeping the requirement visible in the handler, and the maker is credited twice there (vault rent and escrow rent).\n\nTwo Surfpool cases cover the behavior: one asserts `EmptyOffer` and that no balance moves and no account is created, and one presents a read-only maker through a non-payer keypair and asserts the failure plus unchanged escrow, vault, and taker balances.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_, _pina_macros_, _pina_skill_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the remaining audit findings in the example programs\n\n_Packages:_ _pina_\n\n`multisig_program` closes three findings. `MultisigImport` binds its provenance: the legacy account must sit at the PDA its parsed `create_key` derives under the caller-named legacy program, and the holder of that `create_key` must sign the import, so a fabricated account carrying a borrowed roster of keys that never consented no longer imports (the instruction gains the `legacy_create_key` signer account). `ConfigAuthorityExecute` rejects `AddSpendingLimit` actions with a new `SpendingLimitRequiresProposal` error — creating an allowance moves vault funds, which stays behind threshold and timelock even in controlled mode, so the config authority manages configuration rather than custody. `VaultExecute` additionally protects every program-owned writable non-signer account, not just the multisig and the executing proposal, so a vault message can no longer write another proposal or a spending limit through a nested CPI.\n\n`escrow_program` gains a maker `Cancel` (discriminator 3) that refunds the full vault balance and closes both accounts, so a maker is no longer stranded until a taker appears. `prop_amm_program`'s `Update` now accepts the oracle's stored authority in addition to the benchmark's static key, making `RotateAuthority` mean what it says — previously the rotated key could never publish a price. The multisig readme documents the permissionless `ConfigInitialize` race (initialize the config in the deployment transaction or a sniper owns the fee treasury) and the spending-limit rules, and the staking readme documents the same first-initializer shape for pool creation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Align the advisory gates on deny.toml\n\n_Packages:_ _pina_cli_\n\n`security:deny` ran `cargo-deny check bans licenses sources`, so the `[advisories]` section of `deny.toml` — and the four suppressions it documented — never executed. The task now runs the advisories check with `--workspace`, because cargo-deny's default graph roots at the workspace members' normal dependencies and therefore omits the dev/test stack (surfpool, litesvm, mollusk, solana-runtime) where every suppressed advisory actually lives. Without `--workspace` the check cannot see them at all.\n\n`security:audit` carried a second, different seven-id ignore list. `deny.toml` is now the single source of advisory policy: the task reads the `[advisories] ignore` array out of `deny.toml` and passes each id to `cargo-audit`, so the two gates cannot drift. Every suppression in that array carries a reachability justification and a review date, and each needs a tracking issue filed on the repository — the entries are all dev/test-only today, verified with `cargo tree -i <spec> -p pina -p pina_abi -p pina_macros -p pina_cli` and the `--workspace` cargo-deny run, but a file cannot open issues on its own.\n\nThe `[advisories]` section also needed the `RUSTSEC-2021-0139`, `RUSTSEC-2024-0375`, `RUSTSEC-2024-0384`, `RUSTSEC-2020-0016`, `RUSTSEC-2025-0134`, and `RUSTSEC-2026-0173` entries that only the old cargo-audit list had, so moving advisories into the deny gate does not weaken it.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n- **pina_cli**: **Stop the deploy stdin test racing a startup deadline.** The test that proves `solana` inherits a closed stdin polled the child against a three-second deadline, which Pina's `cargo metadata` project discovery exceeded on a loaded runner. It now waits for the marker the fake `solana` writes after reading EOF and keeps a thirty-second deadline purely as a hang bound. Test-only; no release note.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #469](https://github.com/pina-rs/pina/pull/469) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_cli_renderer_, _pina_codama_renderer_, _pina_cpi_renderer_, _pina_profile_, _pina_sdk_ids_, _pina_test_, _pina_codama_nodes_, _pina_codama_renderer_cpi_, _pina_codama_renderer_cli_, _pina_skill_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n\n- **pina_cpi_renderer**: **Derive the scaffold release-line test from the crate version.** `scaffold_declares_pina_from_the_configured_source` computes the expected `pina` pin from `CARGO_PKG_VERSION` the same way `ScaffoldDependency::Published` renders it, so the assertion follows the crate's release line instead of holding a hard-coded `0.19` that the next version bump would leave behind.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #490](https://github.com/pina-rs/pina/pull/490) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n- **pina_codama_nodes**: **Pin the client contract test to the released surfaces.** The vesting and staking clients gained accounts in this release (the claim clock sysvar and refund path, the staking reward vault), so the package's contract test pins the new slot counts and roles. Test-only; no release note.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #457](https://github.com/pina-rs/pina/pull/457) · _Related issues:_ [#442](https://github.com/pina-rs/pina/issues/442), [#448](https://github.com/pina-rs/pina/issues/448), [#456](https://github.com/pina-rs/pina/issues/456), [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n- _Packages:_ _pina_cli_npm_, _pina_cli_darwin_arm64_, _pina_cli_darwin_x64_, _pina_cli_freebsd_x64_, _pina_cli_linux_arm64_gnu_, _pina_cli_linux_arm64_musl_, _pina_cli_linux_x64_gnu_, _pina_cli_linux_x64_musl_, _pina_cli_win32_arm64_msvc_, _pina_cli_win32_x64_msvc_ **Document the lint-driver origin overrides as operator-only.** `PINA_LINT_DRIVER_RELEASE`, `_REPO`, and `_BASE_URL` redirect the driver download and its checksum together, so they are a trusted-origin control rather than a convenience knob: whatever serves the override chooses which binary the CLI runs. The constants now say so, which is the documentation half of the 2026-09-21 audit's supply-chain finding; the version-pinned `npx` renderer fetches stay registry-trust-by-design.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)\n#### Read transition offsets from the stored schema\n\n_Packages:_ _pina_skill_\n\nA generated automatic transition derived its byte offsets from a source schema with dropped and renamed fields already resolved. The bytes it edits are the stored layout, where a dropped field still occupies its space, so any field after a removal was read from the wrong offset and a field after a rename could be read from the wrong one too. The destination validation accepted the result whenever the shifted bytes still formed a valid layout, which made the corruption silent: `{ a, b, c } -> { a, c }` copied `a` to `a` and left `c` reading the old `b`. `SOURCE_SIZE` was wrong in the same way, understating the stored payload.\n\nThe planner now proves a transition from the stored schema against the developer's resolved intent and emits its proof: surviving fields copy from their stored offsets, dropped fields keep occupying their bytes, and every stored field must be either copied or explicitly discarded. A change that no single copy order can express — one field moving right while another moves left, a same-name type change, two destination fields reading one stored field — falls back to a manual transition instead of emitting bytes its own proof does not justify.\n\nFixed-layout accounts gain a manual escape hatch. `--manual <field>` names an added field whose conversion the developer writes. The recorded transition mode and renames carry that intent, so repeated `make` runs keep the authored body instead of regenerating an automatic transition over it. Pairing it with `--rename` allows a rename whose type changed, which a verbatim byte copy cannot express; the previously impossible \"combine `first_name` and `last_name` into `name`\" migration is now a generated manual draft with the correct stored sizes. Contradictory answers fail closed in both directions, including across the persisted `[migrations.answers]` table.\n\nA generated manual draft now carries an offset table in its header: one row per field with its payload-relative range in the stored layout and in the destination, `removed` for acknowledged discards, and prefix notes for compact tails. The developer implementing the body can see at a glance which bytes can move and which stay put.\n\nA recorded manual transition now scopes itself to the hop it was written for. Previously the recorded mode seeded the _next_ adjacent change too, so any hop after a hand-written transition was branded manual forever, even one the byte-level proof fully justifies — removing a field two versions after a narrowing conversion wrongly demanded another manual body. Repeated `make` runs over the manual hop's own draft still replay its recorded mode, so authored bodies remain stable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #481](https://github.com/pina-rs/pina/pull/481) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Generate every repository client with `pina generate`\n\n_Packages:_ _pina_skill_\n\n`pina generate` supersedes `pina codama generate`, which is removed. It discovers a project through its `pina.toml`, refreshes the IDL, and renders only the configured client ecosystems, so a repository generates its clients by running the command once per project instead of through a second command with its own path flags. The bundled skill reference documents the loop.\n\nDogfooding this path in the repository surfaced a bug in shared Dart CLI packages: `pina generate` published the whole staged `bin/` directory for each program, so generating one project deleted the entrypoints of every other project sharing the package. Only the single entrypoint owned by the invocation is published now.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #474](https://github.com/pina-rs/pina/pull/474) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
		},
		{
			"owner_id": "abi",
			"owner_kind": "group",
			"output": "default",
			"stream": "default",
			"path": "crates/pina_abi/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "abi [0.20.0](https://github.com/pina-rs/pina/releases/tag/abi/v0.20.0) (2026-09-23)",
				"summary": [
					"Grouped release for `abi`."
				],
				"sections": [
					{
						"title": "Breaking Changes",
						"collapsed": false,
						"entries": [
							"#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_abi_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					},
					{
						"title": "Features",
						"collapsed": false,
						"entries": [
							"#### Accept const capacities in schemas\n\n_Packages:_ _pina_abi_\n\nCompact account and instruction schemas now accept a `const` item wherever they previously required an integer literal:\n\n```rust\nconst MAX_MEMBERS: usize = 24;\n\n#[account(discriminator = MultisigAccountType, compact)]\npub struct Multisig {\n\tpub bump: u8,\n\tpub member_keys: Vec<Address, MAX_MEMBERS>,\n}\n```\n\n`Vec<T, N>`, `PodVec<T, N, PFX>`, `String<N>`, `PodString<N, PFX>`, an `Option<...>` around them, and fixed `[T; N]` arrays and instruction arguments all resolve a capacity through a constant. Constants may be arithmetic over other constants and may be declared in any module of the crate, so one bound is declared once and reused in the account, the instruction that writes it, and the helper that sizes it.\n\nPina resolves each capacity during expansion and records the number it evaluates to. The ABI layer still receives concrete values, so replacing a literal with a constant of the same value leaves `migrations/manifest.json`, the generated `tests/abi_layout.rs` assertions, `MAX_SIZE`/`MIN_SIZE`/`HEADER_SIZE`, and `projected_bytes(...)` byte-identical, and `pina migrations check` and the Codama IDL unchanged.\n\nA capacity that cannot be evaluated at expansion time now fails the build with a diagnostic naming the expression and pointing at the workaround, instead of the previous `unsupported compact field` or `arrays require an integer literal length` message. An associated constant such as `Bounds::MAX_MEMBERS` is not resolved; declare the bound as a `const` item.\n\n`pina_abi` gains `SchemaConsts`, the shared evaluator both the macros and the CLI use so the two layers agree on what a constant means. `pina_cli` resolves capacities while it parses a program, so migration and IDL tooling reads the same numbers the compiler does.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #463](https://github.com/pina-rs/pina/pull/463) · _Closed issues:_ [#460](https://github.com/pina-rs/pina/issues/460) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					},
					{
						"title": "Fixes",
						"collapsed": false,
						"entries": [
							"#### Enumerate the auto policy's kind names in the schema\n\n_Packages:_ _pina_abi_\n\n`MigrationAuto` has hand-written serde impls, so its JSON Schema is hand-written too, and it described its items as any string. A consumer validating a manifest against the published schema therefore admitted `[\"states\"]` while the reader rejected it with `unknown migration kind` — the schema promised more than the code accepted, which defers the failure to runtime.\n\nThe items now carry the valid names, read from `ContractKind::config_name`, the same source `from_config_name` resolves through, so the schema and the reader cannot drift apart. The checked-in artifacts under `crates/pina_abi/schemas/`, the frozen fixtures, and the published copies under the book are regenerated to match, and a test asserts agreement in both directions: every name the schema advertises decodes, and every spelling the reader rejects stays unadvertised.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #473](https://github.com/pina-rs/pina/pull/473) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#468](https://github.com/pina-rs/pina/issues/468), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_abi_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					},
					{
						"title": "Notes",
						"collapsed": false,
						"entries": [
							"#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_abi_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)",
							"#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_abi_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)"
						]
					}
				]
			},
			"rendered": "## abi [0.20.0](https://github.com/pina-rs/pina/releases/tag/abi/v0.20.0) (2026-09-23)\n\nGrouped release for `abi`.\n\n### Breaking Changes\n\n#### Version the ABI document on `pina_abi`'s own release line\n\n_Packages:_ _pina_abi_\n\n`pina_abi` leaves the `core` release group and owns its release line, so a CLI fix or renderer tweak can no longer move the ABI document version. The document records one `abiVersion` string — the `major.minor` committed in `crates/pina_abi/ABI_VERSION` — in place of the independent manifest and publication integer counters, and a release that changes the document shape advances it through a `breaking` changeset on this crate alone.\n\n`pina_cli` and `pina_macros` follow the new model: schema versions are addressed by their position in a contract history, a transition's adjacency is implied by the version it sits on, the physical descriptor is derived on load, and publication pins compute their schema hashes from the decoded content. `pina abi schema` prints a document's JSON Schema from the same types that read and write it.\n\nThe document also stops storing its own derivations. The physical layout descriptor, the per-version `version`/`schemaSha256`/`processSha256` fields, the transition's adjacency numbers and neighbour hashes, the frozen process proof, the instruction process constraints, and the receipt cluster label are all recomputed on load from the facts a reader cannot derive. `examples/*/migrations/manifest.json` and `publications.json` are regenerated by `pina migrations create`.\n\nReaders normalize any supported document through an ordered table of adjacent converters; the integer format 1–3 readers, their downgrade ladders, and the private compatibility shims are deleted. A document stamped above the reader, or below the oldest supported version, fails closed and names the supported version. Document types derive `JsonSchema`, so each document's schema is generated rather than hand-written and is published at a permanent versioned URL.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #468](https://github.com/pina-rs/pina/pull/468) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n### Features\n\n#### Accept const capacities in schemas\n\n_Packages:_ _pina_abi_\n\nCompact account and instruction schemas now accept a `const` item wherever they previously required an integer literal:\n\n```rust\nconst MAX_MEMBERS: usize = 24;\n\n#[account(discriminator = MultisigAccountType, compact)]\npub struct Multisig {\n\tpub bump: u8,\n\tpub member_keys: Vec<Address, MAX_MEMBERS>,\n}\n```\n\n`Vec<T, N>`, `PodVec<T, N, PFX>`, `String<N>`, `PodString<N, PFX>`, an `Option<...>` around them, and fixed `[T; N]` arrays and instruction arguments all resolve a capacity through a constant. Constants may be arithmetic over other constants and may be declared in any module of the crate, so one bound is declared once and reused in the account, the instruction that writes it, and the helper that sizes it.\n\nPina resolves each capacity during expansion and records the number it evaluates to. The ABI layer still receives concrete values, so replacing a literal with a constant of the same value leaves `migrations/manifest.json`, the generated `tests/abi_layout.rs` assertions, `MAX_SIZE`/`MIN_SIZE`/`HEADER_SIZE`, and `projected_bytes(...)` byte-identical, and `pina migrations check` and the Codama IDL unchanged.\n\nA capacity that cannot be evaluated at expansion time now fails the build with a diagnostic naming the expression and pointing at the workaround, instead of the previous `unsupported compact field` or `arrays require an integer literal length` message. An associated constant such as `Bounds::MAX_MEMBERS` is not resolved; declare the bound as a `const` item.\n\n`pina_abi` gains `SchemaConsts`, the shared evaluator both the macros and the CLI use so the two layers agree on what a constant means. `pina_cli` resolves capacities while it parses a program, so migration and IDL tooling reads the same numbers the compiler does.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #463](https://github.com/pina-rs/pina/pull/463) · _Closed issues:_ [#460](https://github.com/pina-rs/pina/issues/460) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n### Fixes\n\n#### Enumerate the auto policy's kind names in the schema\n\n_Packages:_ _pina_abi_\n\n`MigrationAuto` has hand-written serde impls, so its JSON Schema is hand-written too, and it described its items as any string. A consumer validating a manifest against the published schema therefore admitted `[\"states\"]` while the reader rejected it with `unknown migration kind` — the schema promised more than the code accepted, which defers the failure to runtime.\n\nThe items now carry the valid names, read from `ContractKind::config_name`, the same source `from_config_name` resolves through, so the schema and the reader cannot drift apart. The checked-in artifacts under `crates/pina_abi/schemas/`, the frozen fixtures, and the published copies under the book are regenerated to match, and a test asserts agreement in both directions: every name the schema advertises decodes, and every spelling the reader rejects stays unadvertised.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #473](https://github.com/pina-rs/pina/pull/473) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#468](https://github.com/pina-rs/pina/issues/468), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the confirmed 2026-09-19 sweep findings\n\n_Packages:_ _pina_abi_\n\n`#[derive(Accounts)]` now rejects optional account fields followed by positional fields at compile time: an absent optional consumes its program-address filler slot, so dropping the filler shifted every later binding by one and each validation ran against the wrong account. Move optional fields to the end; a trailing `remaining` slice remains allowed.\n\nThe generated PDA seed slices now emit in declaration order instead of constants-first, so derived addresses match the order the IDL publishes and clients derive the same address the program verifies.\n\nEnveloped instructions now pin the migration version byte at dispatch through an `IntoDiscriminator` gate derived from the checked-in manifest: a missing or unknown version byte fails closed before any handler runs, closing the zero-field fail-open. Programs whose manifests declare no instruction contracts expand to identical code.\n\nThe gate covers exactly the zero-field instructions where the fail-open existed — 38 across the workspace, at a cost of 3-9 CU each, recorded as reviewed `runtimeApprovedTotals` in `scripts/compute-unit-policy.json` (payload instructions already validate their version in the generated parse, so their dispatch is bit-identical to before).\n\nA migration manifest whose `rustName` is not a plain Rust identifier now fails validation with a typed error instead of panicking inside the macro expansion, and duplicate field names within one schema version are rejected per version.\n\nThe account migration executor zero-fills the grown region before each transition applies, matching the instruction and event workspaces, so a hand-written transition that skips an added field commits zeros rather than the account's own realloc residue.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #472](https://github.com/pina-rs/pina/pull/472) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n### Notes\n\n#### Rename `pina migrations make` to `pina migrations create`\n\n_Packages:_ _pina_abi_\n\n`pina migrations make` is now `pina migrations create`. The old spelling is not kept as an alias: the subcommand parses as an unknown command, so a script still calling `make` fails loudly instead of appearing to work.\n\nThe rename reaches every surface that named the command. Compiler diagnostics from `pina_macros` and the `pina_abi` document readers, the `pina migrations` help text, `pina init`'s scaffolded next steps, and the growth warnings `pina migrations check` and `status` print all say `create` now. Docs, the bundled `pina_skill` reference, and the migration walkthrough script follow.\n\nThe internal API moved with it: `pina_cli::migrations::make_migrations` is `create_migrations`, `make_migrations_with_answers` is `create_migrations_with_answers`, and the `MakeMigrationsOutput` result struct is `CreateMigrationsOutput`. `MigrationCommands::Make` is `MigrationCommands::Create`.\n\nTwo things deliberately did not change. Checked-in transition files keep their recorded `@generated by pina migrations make` header, because their bytes are pinned by SHA-256 in `migrations/manifest.json` and rewriting a published transition is exactly the history rewrite the pin exists to prevent; the generator only writes that header for new transitions. Released changelog and publication records keep the historical command name as written.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #475](https://github.com/pina-rs/pina/pull/475) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#491](https://github.com/pina-rs/pina/issues/491)\n\n#### Close the shadow-PDA and discriminator-collision gaps\n\n_Packages:_ _pina_abi_\n\nFixed-account `#[pda]` schemas now generate `load_checked_pda` and `load_checked_pda_mut` alongside the stored-bump `load_pda` pair. The checked loaders search the seeds for the canonical bump and reject both an account at any other address and a stored bump that is not canonical — the fixed-account counterpart of the compact family's `with_checked_pda`, and previously the one PDA family with no canonical option at all. The audit proved the gap executable: a shadow account created at a noncanonical bump whose stored bump field matches passes `load_pda` byte for byte, so a program whose seeds do not bind a required signer accepts the shadow and the canonical account as the same logical entity (`crates/pina/tests/audit_adversarial.rs`). The stored-bump loaders keep their single-derivation cost and their documentation of when they are safe.\n\nA new deny lint `deny_colliding_account_discriminators` closes the other proven framework finding. Discriminators are author-chosen integers and rustc only rejects duplicates within one enum, so two account enums that agree on a value and a serialized width pass every typed loader check — owner, discriminator, exact size — and either account deserializes as the other (the sealevel-attacks type-cosplay class; proven executable in the same test file). The lint collects every generated `impl HasDiscriminator` for types that also implement pina's account traits, evaluates the `VALUE` const and the repr width, and denies any value claimed by two different account types. Events share the trait shape but live in the log namespace, so they are excluded by construction. A UI fixture pair pins the collision and the clean case, and the lint ships in the catalog with the CLI's `lints.json`, `--explain` reference, and the docs page updated to match.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #483](https://github.com/pina-rs/pina/pull/483) · _Related issues:_ [#459](https://github.com/pina-rs/pina/issues/459), [#472](https://github.com/pina-rs/pina/issues/472), [#491](https://github.com/pina-rs/pina/issues/491)"
		}
	],
	"provider": {
		"kind": "github",
		"owner": "pina-rs",
		"repo": "pina",
		"host": null
	}
}
