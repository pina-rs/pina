---
pina: patch
---

Enforce the workspace's custom security dylints in CI, switch `devenv` to pnpm's built-in `pnpm-activate-env` helper for the Node.js version selected by `pnpm-workspace.yaml`, and replace the `paste` dependency in `pina` with `pastey`.
