---
pina_cli: fix
---

Use the published `codama-renderers-dart@0.5.1` renderer and Solana Kit Dart `^0.8.0` runtime packages for generated clients, removing the temporary renderer patch and Git dependency overrides.
