---
pina_cli: note
---

Mark `pina_cli` as `publish = false` since it depends on the unpublishable `pina_codama_renderer` crate and is distributed via GitHub release binary assets instead of crates.io.
