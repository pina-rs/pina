---
pina_cli: feat
pina_skill: feat
---

# Add deterministic builds to `pina build`

Add `pina build --verify` as an explicit Solana Verify 0.5.1 backend. The workflow stages only clean, Git-tracked inputs, preserves the canonical deployment output, and records a content-addressed artifact with Pina-computed build inputs and executable hash. Document installation, Docker and workspace prerequisites, security boundaries, automation output, and on-chain verification limits.
