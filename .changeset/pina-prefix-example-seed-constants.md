---
pina: none
---

# Prefix seed constants in the examples

Every example seed constant now leads with SEED_: PROFILE_SEED becomes SEED_PROFILE, STATE_SEED_PREFIX becomes SEED_STATE_PREFIX, and the same rule covers all remaining program and surfpool-test seed constants. Names only reorder; seed bytes, PDA addresses, and program behavior are unchanged.
