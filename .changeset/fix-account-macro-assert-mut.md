---
pina_macros: patch
---

Fix inverted logic in the `assert_mut()` method generated by the `#[account]` macro. Previously the generated code used `if !condition(self) { return Ok(self); }`, which returned `Ok` when the condition was _false_ — the opposite of the intended behavior. Now uses `if condition(self) { return Ok(self); }`, consistent with the `assert()` method.
