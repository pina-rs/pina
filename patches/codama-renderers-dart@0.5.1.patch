diff --git a/dist/index.node.cjs b/dist/index.node.cjs
index 2b0a2eae874c2487f80633e59c43101ace986eb0..a313596771be56da77dd96a7fcf3adfc271d6af3 100644
--- a/dist/index.node.cjs
+++ b/dist/index.node.cjs
@@ -662,8 +662,8 @@ function getTypeManifestVisitor(input) {
     },
     visitArrayType(node, { self }) {
       const itemManifest = visitorsCore.visit(node.item, self);
-      const encoderSizeExpr = getArraySizeExpression(node, "Encoder");
-      const decoderSizeExpr = getArraySizeExpression(node, "Decoder");
+      const encoderSizeExpr = getArraySizeExpression(node, "Encoder", self);
+      const decoderSizeExpr = getArraySizeExpression(node, "Decoder", self);
       const itemType = fragmentFromString(itemManifest.type.content);
       const itemEncoder = fragment`${use(
         "transformEncoder",
@@ -986,10 +986,54 @@ function getTypeManifestVisitor(input) {
       };
     },
     visitPreOffsetType(node, { self }) {
-      return visitorsCore.visit(node.type, self);
+      const manifest = visitorsCore.visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padLeftDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padLeftEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0 ? `(scope) => scope.preOffset ${node.offset}` : `(scope) => scope.preOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(preOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(preOffset: ${fn}))`
+      };
     },
     visitPostOffsetType(node, { self }) {
-      return visitorsCore.visit(node.type, self);
+      const manifest = visitorsCore.visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padRightDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padRightEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "preOffset":
+            return node.offset < 0 ? `(scope) => scope.preOffset ${node.offset}` : `(scope) => scope.preOffset + ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0 ? `(scope) => scope.postOffset ${node.offset}` : `(scope) => scope.postOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(postOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(postOffset: ${fn}))`
+      };
     },
     visitSolAmountType(node, { self }) {
       return visitorsCore.visit(node.number, self);
@@ -1082,23 +1126,24 @@ function getNumberCodecExpression(format, codecType) {
   const name = getNumberCodecName(format);
   return `get${name}${codecType}()`;
 }
-function getArraySizeExpression(node, codecType) {
-  if (!("count" in node) || !node.count) return "";
+function getArraySizeExpression(node, codecType, self) {
+  if (!("count" in node) || !node.count) return fragment``;
   const count = node.count;
   switch (count.kind) {
     case "fixedCountNode":
-      return `, size: FixedArraySize(${count.value})`;
+      return fragment`, size: FixedArraySize(${count.value})`;
     case "remainderCountNode":
-      return ", size: RemainderArraySize()";
+      return fragment`, size: RemainderArraySize()`;
     case "prefixedCountNode": {
       const resolvedPrefix = nodes.resolveNestedTypeNode(count.prefix);
-      if (resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
-        return "";
+      if (count.prefix.kind === "numberTypeNode" && resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
+        return fragment``;
       }
-      return `, size: PrefixedArraySize(${getNumberCodecExpression(resolvedPrefix.format, codecType)})`;
+      const prefix = visitorsCore.visit(count.prefix, self);
+      return fragment`, size: PrefixedArraySize(${codecType === "Encoder" ? prefix.encoder : prefix.decoder})`;
     }
     default:
-      return "";
+      return fragment``;
   }
 }
 function getMapSizeExpression(node, codecType) {
diff --git a/dist/index.node.js b/dist/index.node.js
index 808b54ffd58f0d9d980210e620f1ee1953de3b6d..a7a3103f35061fe5ffe34863cb52980ce7cbb858 100644
--- a/dist/index.node.js
+++ b/dist/index.node.js
@@ -658,8 +658,8 @@ function getTypeManifestVisitor(input) {
     },
     visitArrayType(node, { self }) {
       const itemManifest = visit(node.item, self);
-      const encoderSizeExpr = getArraySizeExpression(node, "Encoder");
-      const decoderSizeExpr = getArraySizeExpression(node, "Decoder");
+      const encoderSizeExpr = getArraySizeExpression(node, "Encoder", self);
+      const decoderSizeExpr = getArraySizeExpression(node, "Decoder", self);
       const itemType = fragmentFromString(itemManifest.type.content);
       const itemEncoder = fragment`${use(
         "transformEncoder",
@@ -982,10 +982,54 @@ function getTypeManifestVisitor(input) {
       };
     },
     visitPreOffsetType(node, { self }) {
-      return visit(node.type, self);
+      const manifest = visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padLeftDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padLeftEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0 ? `(scope) => scope.preOffset ${node.offset}` : `(scope) => scope.preOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(preOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(preOffset: ${fn}))`
+      };
     },
     visitPostOffsetType(node, { self }) {
-      return visit(node.type, self);
+      const manifest = visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padRightDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padRightEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "preOffset":
+            return node.offset < 0 ? `(scope) => scope.preOffset ${node.offset}` : `(scope) => scope.preOffset + ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0 ? `(scope) => scope.postOffset ${node.offset}` : `(scope) => scope.postOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(postOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(postOffset: ${fn}))`
+      };
     },
     visitSolAmountType(node, { self }) {
       return visit(node.number, self);
@@ -1078,23 +1122,24 @@ function getNumberCodecExpression(format, codecType) {
   const name = getNumberCodecName(format);
   return `get${name}${codecType}()`;
 }
-function getArraySizeExpression(node, codecType) {
-  if (!("count" in node) || !node.count) return "";
+function getArraySizeExpression(node, codecType, self) {
+  if (!("count" in node) || !node.count) return fragment``;
   const count = node.count;
   switch (count.kind) {
     case "fixedCountNode":
-      return `, size: FixedArraySize(${count.value})`;
+      return fragment`, size: FixedArraySize(${count.value})`;
     case "remainderCountNode":
-      return ", size: RemainderArraySize()";
+      return fragment`, size: RemainderArraySize()`;
     case "prefixedCountNode": {
       const resolvedPrefix = resolveNestedTypeNode(count.prefix);
-      if (resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
-        return "";
+      if (count.prefix.kind === "numberTypeNode" && resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
+        return fragment``;
       }
-      return `, size: PrefixedArraySize(${getNumberCodecExpression(resolvedPrefix.format, codecType)})`;
+      const prefix = visit(count.prefix, self);
+      return fragment`, size: PrefixedArraySize(${codecType === "Encoder" ? prefix.encoder : prefix.decoder})`;
     }
     default:
-      return "";
+      return fragment``;
   }
 }
 function getMapSizeExpression(node, codecType) {
diff --git a/src/visitors/getTypeManifestVisitor.ts b/src/visitors/getTypeManifestVisitor.ts
index aec6ff903e41d9a0a45ac559546372a00e10f646..243d3c66b93c5d9dc84f683d048c8613afe6f245 100644
--- a/src/visitors/getTypeManifestVisitor.ts
+++ b/src/visitors/getTypeManifestVisitor.ts
@@ -226,8 +226,8 @@ export function getTypeManifestVisitor(input: {
 
     visitArrayType(node: ArrayTypeNode, { self }) {
       const itemManifest = visit(node.item, self);
-      const encoderSizeExpr = getArraySizeExpression(node, "Encoder");
-      const decoderSizeExpr = getArraySizeExpression(node, "Decoder");
+      const encoderSizeExpr = getArraySizeExpression(node, "Encoder", self);
+      const decoderSizeExpr = getArraySizeExpression(node, "Decoder", self);
       const itemType = fragmentFromString(itemManifest.type.content);
       const itemEncoder = fragment`${use(
         "transformEncoder",
@@ -643,13 +643,61 @@ export function getTypeManifestVisitor(input: {
     },
 
     visitPreOffsetType(node: PreOffsetTypeNode, { self }) {
-      // Pre-offset types pass through to inner type in Dart
-      return visit(node.type, self);
+      const manifest = visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padLeftDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padLeftEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`,
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0
+              ? `(scope) => scope.preOffset ${node.offset}`
+              : `(scope) => scope.preOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(preOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(preOffset: ${fn}))`,
+      };
     },
 
     visitPostOffsetType(node: PostOffsetTypeNode, { self }) {
-      // Post-offset types pass through to inner type in Dart
-      return visit(node.type, self);
+      const manifest = visit(node.type, self);
+      if (node.strategy === "padded") {
+        return {
+          ...manifest,
+          decoder: fragment`${use("padRightDecoder", "solanaCodecsCore")}(${manifest.decoder}, ${node.offset})`,
+          encoder: fragment`${use("padRightEncoder", "solanaCodecsCore")}(${manifest.encoder}, ${node.offset})`,
+        };
+      }
+      const fn = (() => {
+        switch (node.strategy) {
+          case "absolute":
+            return node.offset < 0 ? `(scope) => scope.wrapBytes(${node.offset})` : `(_) => ${node.offset}`;
+          case "preOffset":
+            return node.offset < 0
+              ? `(scope) => scope.preOffset ${node.offset}`
+              : `(scope) => scope.preOffset + ${node.offset}`;
+          case "relative":
+          default:
+            return node.offset < 0
+              ? `(scope) => scope.postOffset ${node.offset}`
+              : `(scope) => scope.postOffset + ${node.offset}`;
+        }
+      })();
+      return {
+        ...manifest,
+        decoder: fragment`${use("offsetDecoder", "solanaCodecsCore")}(${manifest.decoder}, OffsetConfig(postOffset: ${fn}))`,
+        encoder: fragment`${use("offsetEncoder", "solanaCodecsCore")}(${manifest.encoder}, OffsetConfig(postOffset: ${fn}))`,
+      };
     },
 
     visitSolAmountType(node: SolAmountTypeNode, { self }) {
@@ -770,23 +818,25 @@ function getNumberCodecExpression(
 function getArraySizeExpression(
   node: ArrayTypeNode,
   codecType: "Encoder" | "Decoder",
-): string {
-  if (!("count" in node) || !node.count) return "";
+  self: Visitor<TypeManifest>,
+): Fragment {
+  if (!("count" in node) || !node.count) return fragment``;
   const count = node.count;
   switch (count.kind) {
     case "fixedCountNode":
-      return `, size: FixedArraySize(${count.value})`;
+      return fragment`, size: FixedArraySize(${count.value})`;
     case "remainderCountNode":
-      return ", size: RemainderArraySize()";
+      return fragment`, size: RemainderArraySize()`;
     case "prefixedCountNode": {
       const resolvedPrefix = resolveNestedTypeNode(count.prefix);
-      if (resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
-        return ""; // Default
+      if (count.prefix.kind === "numberTypeNode" && resolvedPrefix.format === "u32" && (!resolvedPrefix.endian || resolvedPrefix.endian === "le")) {
+        return fragment``; // Default
       }
-      return `, size: PrefixedArraySize(${getNumberCodecExpression(resolvedPrefix.format, codecType)})`;
+      const prefix = visit(count.prefix, self);
+      return fragment`, size: PrefixedArraySize(${codecType === "Encoder" ? prefix.encoder : prefix.decoder})`;
     }
     default:
-      return "";
+      return fragment``;
   }
 }
 
