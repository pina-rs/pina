//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use pina::Address;
use pina::CpiProgramId;
use pina::Program;

pub const HEAP_ALLOC_PROGRAM_ID: Address =
	pina::address!("BZtBGtYSgERx2zN12aGgQ6sNLh5r7XtcNCafqmsizgqt");

/// Marker for the `heapAllocProgram` program used by generated CPI builders.
#[derive(Clone, Copy, Debug)]
pub struct HeapAllocProgram;

impl CpiProgramId for HeapAllocProgram {
	const ID: Address = HEAP_ALLOC_PROGRAM_ID;
}

/// A validated executable account for the `heapAllocProgram` program.
pub type ProgramAccount<'a> = Program<'a, HeapAllocProgram>;

/// Whether `address` is the `heapAllocProgram` program this crate calls.
///
/// Check this before a CPI when the address arrives from caller input, so a
/// call can never be redirected to a program this crate was not imported
/// for.
#[inline(always)]
pub fn is_expected_program(address: &Address) -> bool {
	*address == HEAP_ALLOC_PROGRAM_ID
}

#[cfg(test)]
mod tests {
	use super::*;

	/// Binds the compiled-in ID to a literal.
	///
	/// A swapped dependency could otherwise retarget every CPI in this crate
	/// without the source changing, so the expected address is asserted here in
	/// full rather than only through the constant.
	#[test]
	fn binds_the_expected_program_id() {
		assert_eq!(
			HEAP_ALLOC_PROGRAM_ID,
			pina::address!("BZtBGtYSgERx2zN12aGgQ6sNLh5r7XtcNCafqmsizgqt")
		);
		assert_eq!(HeapAllocProgram::ID, HEAP_ALLOC_PROGRAM_ID);
		assert!(is_expected_program(&HEAP_ALLOC_PROGRAM_ID));
	}

	#[test]
	fn rejects_a_foreign_program_id() {
		let mut foreign_bytes = HEAP_ALLOC_PROGRAM_ID.to_bytes();
		foreign_bytes[0] ^= 0xFF;
		let foreign = pina::Address::new_from_array(foreign_bytes);
		assert_ne!(foreign, HEAP_ALLOC_PROGRAM_ID);
		assert!(!is_expected_program(&foreign));
	}
}
