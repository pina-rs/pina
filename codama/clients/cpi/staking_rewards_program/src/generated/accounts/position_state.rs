//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::Address;

#[allow(unused_imports)]
use crate::generated_types::*;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct PositionState {
	pub pool: Address,
	pub owner: Address,
	pub staked_amount: u64,
	pub reward_debt: u64,
	pub pending_rewards: u64,
	pub bump: u8,
}

/// Account discriminator declared by the program's IDL.
pub const POSITION_STATE_DISCRIMINATOR: [u8; 8] = [2, 0, 0, 0, 0, 0, 0, 0];

impl PositionState {
	/// Encoded size of this account's data.
	pub const LEN: usize = 97;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 8 && data[..8] == POSITION_STATE_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<PositionState> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 8;

		let pool = Address::new_from_array(data[cursor..cursor + 32].try_into().ok()?);
		cursor += 32;
		let owner = Address::new_from_array(data[cursor..cursor + 32].try_into().ok()?);
		cursor += 32;
		let staked_amount: u64 = u64::from_le_bytes(data[cursor..cursor + 8].try_into().ok()?);
		cursor += 8;
		let reward_debt: u64 = u64::from_le_bytes(data[cursor..cursor + 8].try_into().ok()?);
		cursor += 8;
		let pending_rewards: u64 = u64::from_le_bytes(data[cursor..cursor + 8].try_into().ok()?);
		cursor += 8;
		let bump: u8 = u8::from_le_bytes(data[cursor..cursor + 1].try_into().ok()?);

		Some(PositionState {
			pool,
			owner,
			staked_amount,
			reward_debt,
			pending_rewards,
			bump,
		})
	}
}
