//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::Address;

#[allow(unused_imports)]
use crate::generated_types::*;

/// Global pool configuration and governance root.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct PoolConfig {
	pub bump: u8,
	pub authority: Address,
	pub custodian_threshold: u16,
	pub challenge_window_secs: u32,
	pub deposit_lamports: u64,
	pub total_deposits: u64,
}

/// Account discriminator declared by the program's IDL.
pub const POOL_CONFIG_DISCRIMINATOR: [u8; 2] = [1, 0];

impl PoolConfig {
	/// Encoded size of this account's data.
	pub const LEN: usize = 57;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 2 && data[..2] == POOL_CONFIG_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<PoolConfig> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 2;

		let bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?.try_into().ok()?);
		cursor += 1;
		let authority = Address::new_from_array(data.get(cursor..cursor + 32)?.try_into().ok()?);
		cursor += 32;
		let custodian_threshold: u16 =
			u16::from_le_bytes(data.get(cursor..cursor + 2)?.try_into().ok()?);
		cursor += 2;
		let challenge_window_secs: u32 =
			u32::from_le_bytes(data.get(cursor..cursor + 4)?.try_into().ok()?);
		cursor += 4;
		let deposit_lamports: u64 =
			u64::from_le_bytes(data.get(cursor..cursor + 8)?.try_into().ok()?);
		cursor += 8;
		let total_deposits: u64 =
			u64::from_le_bytes(data.get(cursor..cursor + 8)?.try_into().ok()?);

		Some(PoolConfig {
			bump,
			authority,
			custodian_threshold,
			challenge_window_secs,
			deposit_lamports,
			total_deposits,
		})
	}
}
