//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

#[allow(unused_imports)]
use crate::generated_types::*;

/// Entities allowed to file tier-1 and tier-2 disclosure requests.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct RequesterRegistry {
	pub bump: u8,
	pub count: u8,
	pub keys: [u8; 512],
	pub max_tiers: [u8; 16],
}

/// Account discriminator declared by the program's IDL.
pub const REQUESTER_REGISTRY_DISCRIMINATOR: [u8; 2] = [6, 0];

impl RequesterRegistry {
	/// Encoded size of this account's data.
	pub const LEN: usize = 532;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 2 && data[..2] == REQUESTER_REGISTRY_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<RequesterRegistry> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 2;

		let bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?.try_into().ok()?);
		cursor += 1;
		let count: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?.try_into().ok()?);
		cursor += 1;
		let keys: [u8; 512] = data.get(cursor..cursor + 512)?.try_into().ok()?;
		cursor += 512;
		let max_tiers: [u8; 16] = data.get(cursor..cursor + 16)?.try_into().ok()?;

		Some(RequesterRegistry {
			bump,
			count,
			keys,
			max_tiers,
		})
	}
}
