//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

#[allow(unused_imports)]
use crate::generated_types::*;

/// Per-note audit record. The envelope and shares are encrypted by the
/// depositor's client and opaque to the program; the view key is the
/// per-note consent key that may grant tier-0 requests and file challenges.
///
/// Spent notes are *not* marked here: linking a nullifier to its commitment
/// would break the privacy the pool exists to provide. The nullifier set is
/// the only spent record.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct NoteCommitment {
	pub bump: u8,
	pub commitment: [u8; 32],
	pub view_pubkey: [u8; 32],
	pub envelope_len: u8,
	pub envelope: [u8; 128],
	pub shares: [u8; 144],
}

/// Account discriminator declared by the program's IDL.
pub const NOTE_COMMITMENT_DISCRIMINATOR: [u8; 2] = [8, 0];

impl NoteCommitment {
	/// Encoded size of this account's data.
	pub const LEN: usize = 340;

	/// Whether `data` carries this account's discriminator.
	#[inline(always)]
	pub fn matches(data: &[u8]) -> bool {
		data.len() >= 2 && data[..2] == NOTE_COMMITMENT_DISCRIMINATOR
	}

	/// Reads this account's fields from the account's data.
	///
	/// Returns `None` when the data is too short, the discriminator does not match, or a field is truncated.
	#[inline(always)]
	pub fn parse(data: &[u8]) -> Option<NoteCommitment> {
		if !Self::matches(data) {
			return None;
		}

		let mut cursor = 0usize;
		cursor += 2;

		let bump: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?.try_into().ok()?);
		cursor += 1;
		let commitment: [u8; 32] = data.get(cursor..cursor + 32)?.try_into().ok()?;
		cursor += 32;
		let view_pubkey: [u8; 32] = data.get(cursor..cursor + 32)?.try_into().ok()?;
		cursor += 32;
		let envelope_len: u8 = u8::from_le_bytes(data.get(cursor..cursor + 1)?.try_into().ok()?);
		cursor += 1;
		let envelope: [u8; 128] = data.get(cursor..cursor + 128)?.try_into().ok()?;
		cursor += 128;
		let shares: [u8; 144] = data.get(cursor..cursor + 144)?.try_into().ok()?;

		Some(NoteCommitment {
			bump,
			commitment,
			view_pubkey,
			envelope_len,
			envelope,
			shares,
		})
	}
}
