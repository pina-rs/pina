//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `config_authority_execute` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ConfigAuthorityExecute<'account> {
	/// CPI account `multisig`.
	/// Required privileges: writable.
	pub multisig: &'account AccountView,

	/// CPI account `authority`.
	/// Required privileges: read-only and signer.
	pub authority: &'account AccountView,

	/// CPI account `rentPayer`.
	/// Required privileges: writable and signer.
	pub rent_payer: &'account AccountView,

	/// CPI account `systemProgram`.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// CPI account `clock`.
	/// Required privileges: read-only.
	pub clock: &'account AccountView,

	/// CPI account `spendingLimitAccounts`.
	/// Spending limit accounts referenced by add/remove spending-limit
	/// actions, in any order.
	/// Required privileges: writable.
	pub spending_limit_accounts: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `config_authority_execute`.
	pub ix: ConfigAuthorityExecuteIx,
}

/// Instruction arguments for the `config_authority_execute` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct ConfigAuthorityExecuteIx {
	/// Instruction argument `actionsLen`.
	pub actions_len: u16,

	/// Instruction argument `actions`.
	pub actions: [u8; 128],
}

impl ConfigAuthorityExecuteIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 132;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 132], ProgramError> {
		let mut data = [0u8; 132];
		data[..2].copy_from_slice(&CONFIG_AUTHORITY_EXECUTE_DISCRIMINATOR);
		data[2..4].copy_from_slice(&self.actions_len.to_le_bytes());
		data[4..132].copy_from_slice(&self.actions);

		Ok(data)
	}
}

impl<'account> ConfigAuthorityExecute<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 6] = [
			CpiHandle::writable(self.multisig)?,
			CpiHandle::readonly_signer(self.authority),
			CpiHandle::writable_signer(self.rent_payer)?,
			CpiHandle::readonly(self.system_program),
			CpiHandle::readonly(self.clock),
			CpiHandle::writable(self.spending_limit_accounts)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CONFIG_AUTHORITY_EXECUTE_DISCRIMINATOR: [u8; 2] = [12, 0];
