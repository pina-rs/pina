//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `spending_limit_use` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct SpendingLimitUse<'account> {
	/// CPI account `multisig`.
	/// Required privileges: read-only.
	pub multisig: &'account AccountView,

	/// CPI account `spendingLimit`.
	/// Required privileges: writable.
	pub spending_limit: &'account AccountView,

	/// CPI account `member`.
	/// Required privileges: read-only and signer.
	pub member: &'account AccountView,

	/// CPI account `vault`.
	/// The vault PDA: SOL source or SPL transfer authority.
	/// Required privileges: writable.
	pub vault: &'account AccountView,

	/// CPI account `destination`.
	/// SOL destination, or the destination token account for SPL.
	/// Required privileges: writable.
	pub destination: &'account AccountView,

	/// CPI account `clock`.
	/// Required privileges: read-only.
	pub clock: &'account AccountView,

	/// CPI account `vaultTokenAccount`.
	/// SPL source token account; absent for SOL.
	/// Required privileges: writable.
	/// Pass `None` to use the target program ID as the account placeholder.
	pub vault_token_account: Option<&'account AccountView>,

	/// CPI account `mint`.
	/// SPL mint; absent for SOL.
	/// Required privileges: read-only.
	/// Pass `None` to use the target program ID as the account placeholder.
	pub mint: Option<&'account AccountView>,

	/// CPI account `tokenProgram`.
	/// SPL token program; absent for SOL.
	/// Required privileges: read-only.
	/// Pass `None` to use the target program ID as the account placeholder.
	pub token_program: Option<&'account AccountView>,

	/// CPI account `systemProgram`.
	/// System program; absent for SPL.
	/// Required privileges: read-only.
	/// Pass `None` to use the target program ID as the account placeholder.
	pub system_program: Option<&'account AccountView>,

	/// Instruction arguments encoded and sent as CPI data for `spending_limit_use`.
	pub ix: SpendingLimitUseIx,
}

/// Instruction arguments for the `spending_limit_use` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct SpendingLimitUseIx {
	/// Instruction argument `amount`.
	pub amount: u64,

	/// Instruction argument `decimals`.
	pub decimals: u8,
}

impl SpendingLimitUseIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 11;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 11], ProgramError> {
		let mut data = [0u8; 11];
		data[..2].copy_from_slice(&SPENDING_LIMIT_USE_DISCRIMINATOR);
		data[2..10].copy_from_slice(&self.amount.to_le_bytes());
		data[10..11].copy_from_slice(&self.decimals.to_le_bytes());

		Ok(data)
	}
}

impl<'account> SpendingLimitUse<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 10] = [
			CpiHandle::readonly(self.multisig),
			CpiHandle::writable(self.spending_limit)?,
			CpiHandle::readonly_signer(self.member),
			CpiHandle::writable(self.vault)?,
			CpiHandle::writable(self.destination)?,
			CpiHandle::readonly(self.clock),
			match self.vault_token_account {
				Some(account) => CpiHandle::writable(account)?,
				None => CpiHandle::readonly(program.account()),
			},
			match self.mint {
				Some(account) => CpiHandle::readonly(account),
				None => CpiHandle::readonly(program.account()),
			},
			match self.token_program {
				Some(account) => CpiHandle::readonly(account),
				None => CpiHandle::readonly(program.account()),
			},
			match self.system_program {
				Some(account) => CpiHandle::readonly(account),
				None => CpiHandle::readonly(program.account()),
			},
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const SPENDING_LIMIT_USE_DISCRIMINATOR: [u8; 2] = [13, 0];
