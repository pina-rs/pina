// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import 'package:args/command_runner.dart';

import '../context.dart';
import 'package:pina_codama_clients/multisig_program.dart';

final class VaultExecuteCommand extends Command<void> {
  VaultExecuteCommand() {
    argParser
      ..addOption('multisig', mandatory: true, help: "The multisig account")
      ..addOption('proposal', mandatory: true, help: "The proposal account")
      ..addOption('clock', mandatory: true, help: "The clock account")
      ..addOption(
        'message_accounts',
        mandatory: true,
        help:
            "The message's account keys, in message order. Accounts the message",
      );
  }

  @override
  String get name => 'vault_execute';

  @override
  String get description => "vault_execute";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final multisig = pubkey('--multisig', results['multisig']! as String);
    final proposal = pubkey('--proposal', results['proposal']! as String);
    final member = context.payerAddress;
    final clock = pubkey('--clock', results['clock']! as String);
    final messageAccounts = pubkey(
      '--message-accounts',
      results['message_accounts']! as String,
    );

    final instruction = getVaultExecuteInstruction(
      programAddress: context.programAddress,
      multisig: multisig,
      proposal: proposal,
      member: member,
      clock: clock,
      messageAccounts: messageAccounts,
    );
    await context.send([instruction]);
  }
}
