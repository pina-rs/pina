// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import 'dart:typed_data';

import 'package:args/command_runner.dart';
import 'package:solana_kit_accounts/solana_kit_accounts.dart';
import 'package:solana_kit_rpc_types/solana_kit_rpc_types.dart' hide TransactionVersion;

import '../context.dart';
import 'package:pina_codama_clients/role_registry_program.dart';

final class FetchRegistryConfigCommand extends Command<void> {
  FetchRegistryConfigCommand() {
    argParser
      ..addOption('address', help: 'Account address; overrides PDA derivation.')
      ..addOption('admin', mandatory: true, help: "PDA seed `admin`.");
  }

  @override
  String get name => 'registry-config';

  @override
  String get description => "registry_config";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final adminValue = pubkey('--admin', results['admin']! as String);
    final address = (results['address'] as String?) != null
        ? pubkey('--address', results['address']! as String)
        : (await findRegistryConfigPda(
            seeds: RegistryConfigSeeds(
              admin: adminValue,
            ),
            programAddress: context.programAddress,
          )).$1;
    final data = await context.fetchAccount(address);
    final encoded = Account<Uint8List>(
      address: address,
      data: data,
      executable: false,
      lamports: Lamports(BigInt.zero),
      programAddress: context.programAddress,
      space: BigInt.from(data.length),
    );
    final account = decodeRegistryConfig(encoded);
    printFields(
      context.json,
      'registry-config',
      address,
      <String, Object?>{
        'admin': account.data.admin,
        'role_count': account.data.roleCount,
        'bump': account.data.bump,
      },
    );
  }
}

final class FetchRoleEntryCommand extends Command<void> {
  FetchRoleEntryCommand() {
    argParser
      ..addOption('address', help: 'Account address; overrides PDA derivation.')
      ..addOption('registry', mandatory: true, help: "PDA seed `registry`.")
      ..addOption('role_id', mandatory: true, help: "PDA seed `role_id`.");
  }

  @override
  String get name => 'role-entry';

  @override
  String get description => "role_entry";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final registryValue = pubkey('--registry', results['registry']! as String);
    final roleIdValue = bigInteger('--role-id', results['role_id']! as String);
    final address = (results['address'] as String?) != null
        ? pubkey('--address', results['address']! as String)
        : (await findRoleEntryPda(
            seeds: RoleEntrySeeds(
              registry: registryValue,
              roleId: roleIdValue,
            ),
            programAddress: context.programAddress,
          )).$1;
    final data = await context.fetchAccount(address);
    final encoded = Account<Uint8List>(
      address: address,
      data: data,
      executable: false,
      lamports: Lamports(BigInt.zero),
      programAddress: context.programAddress,
      space: BigInt.from(data.length),
    );
    final account = decodeRoleEntry(encoded);
    printFields(
      context.json,
      'role-entry',
      address,
      <String, Object?>{
        'registry': account.data.registry,
        'role_id': account.data.roleId,
        'grantee': account.data.grantee,
        'permissions': account.data.permissions,
        'active': account.data.active,
        'bump': account.data.bump,
      },
    );
  }
}

final class FetchCommand extends Command<void> {
  @override
  String get name => 'fetch';

  @override
  String get description => 'Fetch and decode program accounts.';
}
