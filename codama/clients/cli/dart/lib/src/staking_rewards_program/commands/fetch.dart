// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import 'dart:typed_data';

import 'package:args/command_runner.dart';
import 'package:solana_kit_accounts/solana_kit_accounts.dart';
import 'package:solana_kit_rpc_types/solana_kit_rpc_types.dart' hide TransactionVersion;

import '../context.dart';
import 'package:pina_codama_clients/staking_rewards_program.dart';

final class FetchPoolStateCommand extends Command<void> {
  FetchPoolStateCommand() {
    argParser
      ..addOption('address', help: 'Account address; overrides PDA derivation.')
      ..addOption('stake_mint', mandatory: true, help: "PDA seed `stake_mint`.")
      ..addOption('reward_mint', mandatory: true, help: "PDA seed `reward_mint`.");
  }

  @override
  String get name => 'pool-state';

  @override
  String get description => "pool_state";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final stakeMintValue = pubkey('--stake-mint', results['stake_mint']! as String);
    final rewardMintValue = pubkey('--reward-mint', results['reward_mint']! as String);
    final address = (results['address'] as String?) != null
        ? pubkey('--address', results['address']! as String)
        : (await findPoolPda(
            seeds: PoolSeeds(
              stakeMint: stakeMintValue,
              rewardMint: rewardMintValue,
            ),
            programAddress: context.programAddress,
          )).$1;
    final data = await context.fetchAccount(address);
    final encoded = Account<Uint8List>(
      address: address,
      data: data,
      executable: false,
      lamports: Lamports(BigInt.zero),
      programAddress: context.programAddress,
      space: BigInt.from(data.length),
    );
    final account = decodePoolState(encoded);
    printFields(
      context.json,
      'pool-state',
      address,
      <String, Object?>{
        'admin': account.data.admin,
        'stake_mint': account.data.stakeMint,
        'reward_mint': account.data.rewardMint,
        'total_staked': account.data.totalStaked,
        'reward_index': account.data.rewardIndex,
        'paused': account.data.paused,
        'bump': account.data.bump,
      },
    );
  }
}

final class FetchPositionStateCommand extends Command<void> {
  FetchPositionStateCommand() {
    argParser
      ..addOption('address', help: 'Account address; overrides PDA derivation.')
      ..addOption('pool', mandatory: true, help: "PDA seed `pool`.")
      ..addOption('owner', mandatory: true, help: "PDA seed `owner`.");
  }

  @override
  String get name => 'position-state';

  @override
  String get description => "position_state";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final poolValue = pubkey('--pool', results['pool']! as String);
    final ownerValue = pubkey('--owner', results['owner']! as String);
    final address = (results['address'] as String?) != null
        ? pubkey('--address', results['address']! as String)
        : (await findPositionPda(
            seeds: PositionSeeds(
              pool: poolValue,
              owner: ownerValue,
            ),
            programAddress: context.programAddress,
          )).$1;
    final data = await context.fetchAccount(address);
    final encoded = Account<Uint8List>(
      address: address,
      data: data,
      executable: false,
      lamports: Lamports(BigInt.zero),
      programAddress: context.programAddress,
      space: BigInt.from(data.length),
    );
    final account = decodePositionState(encoded);
    printFields(
      context.json,
      'position-state',
      address,
      <String, Object?>{
        'pool': account.data.pool,
        'owner': account.data.owner,
        'staked_amount': account.data.stakedAmount,
        'reward_debt': account.data.rewardDebt,
        'pending_rewards': account.data.pendingRewards,
        'bump': account.data.bump,
      },
    );
  }
}

final class FetchCommand extends Command<void> {
  @override
  String get name => 'fetch';

  @override
  String get description => 'Fetch and decode program accounts.';
}
