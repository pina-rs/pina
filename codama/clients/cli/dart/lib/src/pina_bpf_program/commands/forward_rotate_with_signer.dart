// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import 'package:args/command_runner.dart';

import '../context.dart';
import 'package:pina_codama_clients/pina_bpf_program.dart';

final class ForwardRotateWithSignerCommand extends Command<void> {
  ForwardRotateWithSignerCommand() {
    argParser
      ..addOption('new_authority', mandatory: true, help: "newAuthority")
      ..addOption('oracle', mandatory: true, help: "The oracle account")
      ..addOption(
        'authority',
        mandatory: false,
        help: "The authority account [default: payer]",
      )
      ..addOption(
        'prop_amm_program',
        mandatory: true,
        help: "The prop_amm_program account",
      );
  }

  @override
  String get name => 'forward_rotate_with_signer';

  @override
  String get description => "forward_rotate_with_signer";

  @override
  Future<void> run() async {
    final results = argResults!;
    final context = await createContext(globalResults!);
    final oracle = pubkey('--oracle', results['oracle']! as String);
    final authority = (results['authority'] as String?) != null
        ? pubkey('--authority', results['authority']! as String)
        : context.payerAddress;
    final propAmmProgram = pubkey(
      '--prop-amm-program',
      results['prop_amm_program']! as String,
    );
    final newAuthorityValue = pubkey(
      '--new-authority',
      results['new_authority']! as String,
    );
    final instruction = getForwardRotateWithSignerInstruction(
      programAddress: context.programAddress,
      oracle: oracle,
      authority: authority,
      propAmmProgram: propAmmProgram,
      newAuthority: newAuthorityValue,
    );
    await context.send([instruction]);
  }
}
