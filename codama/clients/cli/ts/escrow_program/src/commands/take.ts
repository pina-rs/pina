// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getTakeInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const takeCommand = registerGlobals(new Command("take"))
	.description("take")
	.option("--taker <taker>", "The `taker` account [default: payer]")
	.requiredOption("--mint-a <mintA>", "The `mint_a` account")
	.requiredOption("--mint-b <mintB>", "The `mint_b` account")
	.requiredOption("--taker-ata-a <takerAtaA>", "The `taker_ata_a` account")
	.requiredOption("--taker-ata-b <takerAtaB>", "The `taker_ata_b` account")
	.requiredOption("--maker <maker>", "The `maker` account")
	.requiredOption("--maker-ata-b <makerAtaB>", "The `maker_ata_b` account")
	.requiredOption("--escrow <escrow>", "The `escrow` account")
	.requiredOption("--vault <vault>", "The `vault` account")
	.requiredOption(
		"--token-program <tokenProgram>",
		"The `token_program` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			taker: context.payer,
			mintA: pubkey("--mint-a", options.mintA),
			mintB: pubkey("--mint-b", options.mintB),
			takerAtaA: pubkey("--taker-ata-a", options.takerAtaA),
			takerAtaB: pubkey("--taker-ata-b", options.takerAtaB),
			maker: pubkey("--maker", options.maker),
			makerAtaB: pubkey("--maker-ata-b", options.makerAtaB),
			escrow: pubkey("--escrow", options.escrow),
			vault: pubkey("--vault", options.vault),
			tokenProgram: pubkey("--token-program", options.tokenProgram),
		};
		const instruction = getTakeInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
