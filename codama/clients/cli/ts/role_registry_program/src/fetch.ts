// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import {
	fetchRegistryConfig,
	fetchRoleEntry,
	findRegistryConfigPda,
	findRoleEntryPda,
} from "./client";
import { bigInteger, CliContext, pubkey, registerGlobals } from "./context";

export const fetchCommand = registerGlobals(new Command("fetch"))
	.description("Fetch and decode program accounts.")
	.addCommand(
		registerGlobals(new Command("registry-config"))
			.description("registry_config")
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.requiredOption("--admin <admin>", "PDA seed `admin`.")
			.action(async (options) => {
				const context = await CliContext.create(options);
				const admin = pubkey("--admin", options.admin as string);
				const address = options.address ??
					(await findRegistryConfigPda({ admin }, {
						programAddress: context.programAddress,
					}))[0];
				const account = await fetchRegistryConfig(context.rpc, address);
				printAccount(context.json, "registry-config", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("role-entry"))
			.description("role_entry")
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.requiredOption("--registry <registry>", "PDA seed `registry`.")
			.requiredOption("--role-id <roleId>", "PDA seed `role_id`.")
			.action(async (options) => {
				const context = await CliContext.create(options);
				const registry = pubkey("--registry", options.registry as string);
				const roleId = bigInteger("--role-id", options.roleId as string);
				const address = options.address ??
					(await findRoleEntryPda({ registry, roleId }, {
						programAddress: context.programAddress,
					}))[0];
				const account = await fetchRoleEntry(context.rpc, address);
				printAccount(context.json, "role-entry", address, account.data);
			}),
	);

function printAccount(
	json: boolean,
	name: string,
	address: string,
	data: unknown,
): void {
	if (json) {
		console.log(JSON.stringify({ address, ...(data as object) }, null, "\t"));
		return;
	}
	console.log(`${name} ${address}`);
	const fields = data as Record<string, unknown>;
	for (const [key, value] of Object.entries(fields)) {
		console.log(`  ${key}: ${value}`);
	}
}
