// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getAddRoleInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const addRoleCommand = registerGlobals(new Command("add_role"))
	.description("addRole")
	.requiredOption("--role-id <roleId>", "roleId")
	.requiredOption("--permissions <permissions>", "permissions")
	.requiredOption("--bump <bump>", "bump")
	.option("--admin <admin>", "The `admin` account [default: payer]")
	.requiredOption("--grantee <grantee>", "The `grantee` account")
	.requiredOption(
		"--registry-config <registryConfig>",
		"The `registry_config` account",
	)
	.requiredOption("--role-entry <roleEntry>", "The `role_entry` account")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			roleId: bigInteger("--role-id", options.roleId),
			permissions: bigInteger("--permissions", options.permissions),
			bump: smallInteger("--bump", options.bump),
			admin: context.payer,
			grantee: pubkey("--grantee", options.grantee),
			registryConfig: pubkey("--registry-config", options.registryConfig),
			roleEntry: pubkey("--role-entry", options.roleEntry),
		};
		const instruction = getAddRoleInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
