// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getDeactivateRoleInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const deactivateRoleCommand = registerGlobals(
	new Command("deactivate_role"),
)
	.description("deactivateRole")
	.option("--admin <admin>", "The `admin` account [default: payer]")
	.requiredOption(
		"--registry-config <registryConfig>",
		"The `registry_config` account",
	)
	.requiredOption("--role-entry <roleEntry>", "The `role_entry` account")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			admin: context.payer,
			registryConfig: pubkey("--registry-config", options.registryConfig),
			roleEntry: pubkey("--role-entry", options.roleEntry),
		};
		const instruction = getDeactivateRoleInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
