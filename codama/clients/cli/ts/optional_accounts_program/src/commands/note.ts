// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getNoteInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const noteCommand = registerGlobals(new Command("note"))
	.description("note")
	.option(
		"--authority <authority>",
		"The transaction fee payer; always required [default: payer]",
	)
	.requiredOption(
		"--note <note>",
		"An arbitrary readonly account attached as context",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			authority: context.payer,
			note: pubkey("--note", options.note),
		};
		const instruction = getNoteInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
