// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getUpdateInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const updateCommand = registerGlobals(new Command("update"))
	.description("update")
	.requiredOption("--value <value>", "value")
	.requiredOption("--memo <memo>", "memo")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.requiredOption("--referrer <referrer>", "The `referrer` account")
	.requiredOption("--state <state>", "The `state` account")
	.option(
		"--migration-payer <migrationPayer>",
		"The `migration_payer` account [default: payer]",
	)
	.requiredOption(
		"--system-program <systemProgram>",
		"The `system_program` account",
	)
	.requiredOption("--manual-state <manualState>", "The `manual_state` account")
	.requiredOption(
		"--compact-state <compactState>",
		"The `compact_state` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			value: bigInteger("--value", options.value),
			memo: smallInteger("--memo", options.memo),
			authority: context.payer,
			referrer: pubkey("--referrer", options.referrer),
			state: pubkey("--state", options.state),
			migrationPayer: context.payer,
			systemProgram: pubkey("--system-program", options.systemProgram),
			manualState: pubkey("--manual-state", options.manualState),
			compactState: pubkey("--compact-state", options.compactState),
		};
		const instruction = getUpdateInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
