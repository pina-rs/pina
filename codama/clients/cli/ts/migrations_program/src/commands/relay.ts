// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getRelayInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const relayCommand = registerGlobals(new Command("relay"))
	.description("relay")
	.requiredOption("--value <value>", "value")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.requiredOption("--referrer <referrer>", "The `referrer` account")
	.requiredOption("--state <state>", "The `state` account")
	.option(
		"--migration-payer <migrationPayer>",
		"The `migration_payer` account [default: payer]",
	)
	.requiredOption(
		"--migration-program <migrationProgram>",
		"The `migration_program` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			value: bigInteger("--value", options.value),
			authority: context.payer,
			referrer: pubkey("--referrer", options.referrer),
			state: pubkey("--state", options.state),
			migrationPayer: context.payer,
			migrationProgram: pubkey("--migration-program", options.migrationProgram),
		};
		const instruction = getRelayInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
