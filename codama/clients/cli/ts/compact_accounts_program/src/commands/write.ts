// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getWriteInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const writeCommand = registerGlobals(new Command("write"))
	.description("write")
	.requiredOption("--index <index>", "index")
	.requiredOption("--value <value>", "value")
	.option(
		"--authority <authority>",
		"Funds growth if a future write patch changes the encoded length [default: payer]",
	)
	.option("--journal <journal>", "The `journal` account [default: derived]")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			index: smallInteger("--index", options.index),
			value: bigInteger("--value", options.value),
			authority: context.payer,
			journal: options.journal,
		};
		const instruction = await getWriteInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
