// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getInitializePolicyInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const initializePolicyCommand = registerGlobals(
	new Command("initialize_policy"),
)
	.description("initializePolicy")
	.requiredOption("--bump <bump>", "bump")
	.requiredOption("--minimum <minimum>", "minimum")
	.requiredOption("--maximum <maximum>", "maximum")
	.requiredOption(
		"--required-approvals <requiredApprovals>",
		"requiredApprovals",
	)
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.option("--policy <policy>", "The `policy` account [default: derived]")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			bump: smallInteger("--bump", options.bump),
			minimum: bigInteger("--minimum", options.minimum),
			maximum: bigInteger("--maximum", options.maximum),
			requiredApprovals: smallInteger(
				"--required-approvals",
				options.requiredApprovals,
			),
			authority: context.payer,
			policy: options.policy,
		};
		const instruction = await getInitializePolicyInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
