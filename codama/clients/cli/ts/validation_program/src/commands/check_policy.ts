// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getCheckPolicyInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const checkPolicyCommand = registerGlobals(new Command("check_policy"))
	.description("checkPolicy")
	.requiredOption("--amount <amount>", "amount")
	.requiredOption("--memo <memo>", "memo")
	.requiredOption("--approvals <approvals>", "approvals")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.option("--policy <policy>", "The `policy` account [default: derived]")
	.requiredOption(
		"--audit <audit>",
		"A shared account may still require the transaction's writable flag",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			amount: bigInteger("--amount", options.amount),
			memo: options.memo,
			approvals: base58Vec("--approvals", options.approvals),
			authority: context.payer,
			policy: options.policy,
			audit: pubkey("--audit", options.audit),
		};
		const instruction = await getCheckPolicyInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
