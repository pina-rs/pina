// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getValidateExternalProgramInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const validateExternalProgramCommand = registerGlobals(
	new Command("validate_external_program"),
)
	.description("validateExternalProgram")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.requiredOption(
		"--external-program <externalProgram>",
		"The `external_program` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			authority: context.payer,
			externalProgram: pubkey("--external-program", options.externalProgram),
		};
		const instruction = getValidateExternalProgramInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
