// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import {
	fetchCustodianRegistry,
	fetchDisclosureLog,
	fetchDisclosureRequest,
	fetchMerkleTree,
	fetchNoteCommitment,
	fetchNullifierSet,
	fetchPoolConfig,
	fetchPoolVault,
	fetchRequesterRegistry,
	fetchVerifyingKeyAccount,
	findCustodianRegistryPda,
	findDisclosureLogPda,
	findDisclosureRequestPda,
	findMerkleTreePda,
	findNoteCommitmentPda,
	findNullifierSetPda,
	findPoolConfigPda,
	findPoolVaultPda,
	findRequesterRegistryPda,
	findVerifyingKeyAccountPda,
} from "./client";
import {
	bigInteger,
	CliContext,
	CliError,
	pubkey,
	registerGlobals,
} from "./context";

export const fetchCommand = registerGlobals(new Command("fetch"))
	.description("Fetch and decode program accounts.")
	.addCommand(
		registerGlobals(new Command("pool-config"))
			.description("Global pool configuration and governance root.")
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findPoolConfigPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchPoolConfig(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "pool-config", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("pool-vault"))
			.description(
				"The pool's SOL custody. Data-less by design: every lamport in it is\nbacked by an unspent note commitment in the tree.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findPoolVaultPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchPoolVault(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "pool-vault", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("merkle-tree"))
			.description(
				"Full-node incremental Merkle tree over Poseidon-2 hashes.\n\nLevel `ℓ` (0 = leaves) occupies `nodes` at offset\n`(1 << (depth+1)) − (1 << (depth+1−ℓ))`, so the root sits in the final\nslot. Empty subtrees hold the precomputed zero-hash chain, letting\nwitnesses cover unwritten leaves.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findMerkleTreePda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchMerkleTree(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "merkle-tree", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("nullifier-set"))
			.description("Append-only set of spent nullifiers.")
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findNullifierSetPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchNullifierSet(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "nullifier-set", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("custodian-registry"))
			.description(
				"The disclosure committee. A threshold of these keys must approve before\na disclosure may execute, and each approval is a signed transaction.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findCustodianRegistryPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchCustodianRegistry(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "custodian-registry", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("requester-registry"))
			.description(
				"Entities allowed to file tier-1 and tier-2 disclosure requests.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findRequesterRegistryPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchRequesterRegistry(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "requester-registry", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("note-commitment"))
			.description(
				"Per-note audit record. The envelope and shares are encrypted by the\ndepositor's client and opaque to the program; the view key is the\nper-note consent key that may grant tier-0 requests and file challenges.\n\nSpent notes are *not* marked here: linking a nullifier to its commitment\nwould break the privacy the pool exists to provide. The nullifier set is\nthe only spent record.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.requiredOption("--commitment <commitment>", "PDA seed `commitment`.")
			.action(async (options) => {
				const context = await CliContext.create(options);
				const commitment = pubkey("--commitment", options.commitment as string);
				const address = options.address ??
					(await findNoteCommitmentPda({ commitment }, {
						programAddress: context.programAddress,
					}))[0];
				const account = await fetchNoteCommitment(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "note-commitment", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("disclosure-request"))
			.description("One disclosure request and its state machine.")
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.requiredOption("--requester <requester>", "PDA seed `requester`.")
			.requiredOption("--nonce <nonce>", "PDA seed `nonce`.")
			.action(async (options) => {
				const context = await CliContext.create(options);
				const requester = pubkey("--requester", options.requester as string);
				const nonce = bigInteger("--nonce", options.nonce as string);
				const address = options.address ??
					(await findDisclosureRequestPda({ requester, nonce }, {
						programAddress: context.programAddress,
					}))[0];
				const account = await fetchDisclosureRequest(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "disclosure-request", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("disclosure-log"))
			.description(
				"Append-only public record of executed disclosures. The entry is written\nby the same instruction that marks the request executed, so execution\nwithout a log record cannot happen.",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.action(async (options) => {
				const context = await CliContext.create(options);
				const address = options.address === undefined
					? (await findDisclosureLogPda({
						programAddress: context.programAddress,
					}))[0]
					: pubkey("--address", options.address as string);
				const account = await fetchDisclosureLog(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(context.json, "disclosure-log", address, account.data);
			}),
	)
	.addCommand(
		registerGlobals(new Command("verifying-key-account"))
			.description(
				"One installed Groth16 verifying key. Slot 0 verifies withdraw proofs\n(two public inputs), slot 1 transfer proofs (three).",
			)
			.option(
				"--address <address>",
				"Account address; overrides PDA derivation.",
			)
			.requiredOption("--slot <slot>", "PDA seed `slot`.")
			.action(async (options) => {
				const context = await CliContext.create(options);
				const slot = bigInteger("--slot", options.slot as string);
				const address = options.address ??
					(await findVerifyingKeyAccountPda({ slot }, {
						programAddress: context.programAddress,
					}))[0];
				const account = await fetchVerifyingKeyAccount(context.rpc, address);
				if (account.programAddress !== context.programAddress) {
					throw new CliError(
						`account ${address} belongs to ${account.programAddress}, not the configured program`,
					);
				}
				printAccount(
					context.json,
					"verifying-key-account",
					address,
					account.data,
				);
			}),
	);

function printAccount(
	json: boolean,
	name: string,
	address: string,
	data: unknown,
): void {
	if (json) {
		console.log(JSON.stringify({ address, ...(data as object) }, null, "\t"));
		return;
	}
	console.log(`${name} ${address}`);
	const fields = data as Record<string, unknown>;
	for (const [key, value] of Object.entries(fields)) {
		console.log(`  ${key}: ${value}`);
	}
}
