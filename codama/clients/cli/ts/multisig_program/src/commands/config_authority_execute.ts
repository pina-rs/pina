// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getConfigAuthorityExecuteInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const configAuthorityExecuteCommand = registerGlobals(
	new Command("config_authority_execute"),
)
	.description("configAuthorityExecute")
	.requiredOption("--actions-len <actionsLen>", "actionsLen")
	.requiredOption("--actions <actions>", "actions")
	.requiredOption("--multisig <multisig>", "The `multisig` account")
	.requiredOption("--clock <clock>", "The `clock` account")
	.requiredOption(
		"--rent-collector <rentCollector>",
		"Refund destination for closed spending-limit accounts and member-tail",
	)
	.requiredOption(
		"--spending-limit-accounts <spendingLimitAccounts>",
		"Spending limit accounts referenced by add/remove spending-limit",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			actionsLen: smallInteger("--actions-len", options.actionsLen),
			actions: base58("--actions", options.actions),
			multisig: pubkey("--multisig", options.multisig),
			authority: context.payer,
			rentPayer: context.payer,
			clock: pubkey("--clock", options.clock),
			rentCollector: pubkey("--rent-collector", options.rentCollector),
			spendingLimitAccounts: pubkey(
				"--spending-limit-accounts",
				options.spendingLimitAccounts,
			),
		};
		const instruction = getConfigAuthorityExecuteInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
