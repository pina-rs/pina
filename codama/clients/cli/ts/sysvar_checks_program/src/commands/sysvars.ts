// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getSysvarsInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const sysvarsCommand = registerGlobals(new Command("sysvars"))
	.description("sysvars")
	.requiredOption("--clock <clock>", "The `clock` account")
	.requiredOption("--rent <rent>", "The `rent` account")
	.requiredOption(
		"--stake-history <stakeHistory>",
		"The `stake_history` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			clock: pubkey("--clock", options.clock),
			rent: pubkey("--rent", options.rent),
			stakeHistory: pubkey("--stake-history", options.stakeHistory),
		};
		const instruction = getSysvarsInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
