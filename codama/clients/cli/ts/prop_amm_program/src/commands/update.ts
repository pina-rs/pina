// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getUpdateInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const updateCommand = registerGlobals(new Command("update"))
	.description("update")
	.requiredOption("--new-price <newPrice>", "newPrice")
	.requiredOption("--oracle <oracle>", "The `oracle` account")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			newPrice: bigInteger("--new-price", options.newPrice),
			oracle: pubkey("--oracle", options.oracle),
			authority: context.payer,
		};
		const instruction = getUpdateInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
