// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getCreateInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const createCommand = registerGlobals(new Command("create"))
	.description("create")
	.requiredOption("--data-f32 <dataF32>", "dataF32")
	.requiredOption("--data-f64 <dataF64>", "dataF64")
	.requiredOption("--account <account>", "The `account` account")
	.option("--authority <authority>", "The `authority` account [default: payer]")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			dataF32: smallInteger("--data-f32", options.dataF32),
			dataF64: bigInteger("--data-f64", options.dataF64),
			account: pubkey("--account", options.account),
			authority: context.payer,
		};
		const instruction = getCreateInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
