// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getInitializeInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const initializeCommand = registerGlobals(new Command("initialize"))
	.description("initialize")
	.requiredOption("--total-amount <totalAmount>", "totalAmount")
	.requiredOption("--start-ts <startTs>", "startTs")
	.requiredOption("--cliff-ts <cliffTs>", "cliffTs")
	.requiredOption("--end-ts <endTs>", "endTs")
	.requiredOption("--bump <bump>", "bump")
	.option("--admin <admin>", "The `admin` account [default: payer]")
	.requiredOption("--beneficiary <beneficiary>", "The `beneficiary` account")
	.requiredOption("--mint <mint>", "The `mint` account")
	.option(
		"--vesting-state <vestingState>",
		"The `vesting_state` account [default: derived]",
	)
	.requiredOption("--vault <vault>", "The `vault` account")
	.requiredOption(
		"--token-program <tokenProgram>",
		"The `token_program` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			totalAmount: bigInteger("--total-amount", options.totalAmount),
			startTs: bigInteger("--start-ts", options.startTs),
			cliffTs: bigInteger("--cliff-ts", options.cliffTs),
			endTs: bigInteger("--end-ts", options.endTs),
			bump: smallInteger("--bump", options.bump),
			admin: context.payer,
			beneficiary: pubkey("--beneficiary", options.beneficiary),
			mint: pubkey("--mint", options.mint),
			vestingState: options.vestingState,
			vault: pubkey("--vault", options.vault),
			tokenProgram: pubkey("--token-program", options.tokenProgram),
		};
		const instruction = await getInitializeInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
