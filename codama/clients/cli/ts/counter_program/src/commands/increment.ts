// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getIncrementInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const incrementCommand = registerGlobals(new Command("increment"))
	.description(
		"Instruction data for `Increment`. No extra payload beyond the\ndiscriminator byte.",
	)
	.option(
		"--authority <authority>",
		"The counter's authority. Must sign to prove ownership [default: payer]",
	)
	.option(
		"--counter <counter>",
		"The counter PDA account (must already exist and be writable) [default: derived]",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			authority: context.payer,
			counter: options.counter,
		};
		const instruction = await getIncrementInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
