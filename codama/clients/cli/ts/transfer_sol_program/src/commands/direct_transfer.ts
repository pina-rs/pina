// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getDirectTransferInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const directTransferCommand = registerGlobals(
	new Command("direct_transfer"),
)
	.description(
		"Instruction data for `DirectTransfer`.\n\nSame layout as `CpiTransferInstruction` but with a different discriminator\nbyte.",
	)
	.requiredOption("--amount <amount>", "amount")
	.option(
		"--sender <sender>",
		"The sender. Must be owned by this program, writable, and a signer [default: payer]",
	)
	.requiredOption("--recipient <recipient>", "The recipient. Must be writable")
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			amount: bigInteger("--amount", options.amount),
			sender: context.payer,
			recipient: pubkey("--recipient", options.recipient),
		};
		const instruction = getDirectTransferInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
