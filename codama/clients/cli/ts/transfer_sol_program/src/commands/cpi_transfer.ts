// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getCpiTransferInstruction } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const cpiTransferCommand = registerGlobals(new Command("cpi_transfer"))
	.description(
		"Instruction data for `CpiTransfer`.\n\nLayout:\n```text\n| offset | size | field         |\n|--------|------|---------------|\n| 0      | 1    | discriminator |\n| 1      | 8    | amount (u64)  |\n```",
	)
	.requiredOption("--amount <amount>", "amount")
	.option(
		"--sender <sender>",
		"The sender. Must be a signer and writable (lamports will be debited) [default: payer]",
	)
	.requiredOption(
		"--recipient <recipient>",
		"The recipient. Must be writable (lamports will be credited)",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			amount: bigInteger("--amount", options.amount),
			sender: context.payer,
			recipient: pubkey("--recipient", options.recipient),
		};
		const instruction = getCpiTransferInstruction(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
