// This code was AUTOGENERATED by `@pina-rs/codama-renderer-cli`.
// Please DO NOT EDIT THIS FILE directly.
// Regenerate it from the program IDL instead.

import { Command } from "commander";
import { getForwardRotateWithPdaInstructionAsync } from "../client";
import {
	base58,
	base58Vec,
	bigInteger,
	CliContext,
	pubkey,
	registerGlobals,
	smallInteger,
} from "../context";

export const forwardRotateWithPdaCommand = registerGlobals(
	new Command("forward_rotate_with_pda"),
)
	.description("forwardRotateWithPda")
	.requiredOption("--bump <bump>", "bump")
	.requiredOption("--new-authority <newAuthority>", "newAuthority")
	.requiredOption("--oracle <oracle>", "The `oracle` account")
	.option(
		"--authority <authority>",
		"The `authority` account [default: derived]",
	)
	.requiredOption(
		"--prop-amm-program <propAmmProgram>",
		"The `prop_amm_program` account",
	)
	.action(async (options) => {
		const context = await CliContext.create(options);
		const input = {
			bump: smallInteger("--bump", options.bump),
			newAuthority: pubkey("--new-authority", options.newAuthority),
			oracle: pubkey("--oracle", options.oracle),
			authority: options.authority,
			propAmmProgram: pubkey("--prop-amm-program", options.propAmmProgram),
		};
		const instruction = await getForwardRotateWithPdaInstructionAsync(
			...[input],
			{ programAddress: context.programAddress },
		);
		await context.send(instruction);
	});
