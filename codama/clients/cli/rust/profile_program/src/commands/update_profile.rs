//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use profile_program_client::instructions::UpdateProfile;
use profile_program_client::instructions::UpdateProfileInstructionData;
use solana_sdk::pubkey::Pubkey;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct UpdateProfileArgs {
	#[arg(long)]
	name: String,
	#[arg(long)]
	bio: String,
	/// The profile's authority. Must sign to prove ownership [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// The profile PDA account (must already exist and be writable) [default: derived]
	#[arg(long)]
	profile: Option<String>,
}

pub(crate) fn run(context: &CliContext, args: UpdateProfileArgs) -> Result<(), CliError> {
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let profile = match &args.profile {
		Some(value) => CliContext::pubkey("--profile", value)?,
		None => {
			Pubkey::find_program_address(
				&["profile".as_bytes(), authority.as_ref()],
				&context.program_address,
			)
			.0
		}
	};
	let mut invalid: Option<String> = None;
	let data = UpdateProfileInstructionData::new(|data| {
		if let Err(error) = data.name.try_set(&args.name) {
			invalid = Some(format!("--name: {error}"));
		}
		if let Err(error) = data.bio.try_set(&args.bio) {
			invalid = Some(format!("--bio: {error}"));
		}
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;
	if let Some(message) = invalid {
		return Err(CliError::InvalidData { message });
	}

	let accounts = UpdateProfile { authority, profile };

	context.send(accounts.instruction(data))
}
