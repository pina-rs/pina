//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

//! Generated CLI for the `multisigProgram` program.

mod commands;
mod context;
mod fetch;
use clap::Parser;
use clap::Subcommand;
use context::CliContext;
use solana_sdk::pubkey::Pubkey;

/// Command-line interface for multisigProgram
#[derive(Debug, Parser)]
#[command(name = "multisig-program-cli", version = env!("CARGO_PKG_VERSION"), propagate_version = true)]
struct Cli {
	/// RPC endpoint: `mainnet`, `devnet`, `testnet`, `localhost`, or an https URL.
	#[arg(
		short = 'u',
		long,
		global = true,
		default_value = "devnet",
		env = "SOLANA_URL"
	)]
	url: String,

	/// Payer keypair file in the standard Solana JSON byte-array format.
	#[arg(
		short = 'k',
		long,
		global = true,
		default_value = "~/.config/solana/id.json",
		env = "PINA_KEYPAIR"
	)]
	keypair: String,

	/// Override the on-chain program address.
	#[arg(long, global = true)]
	program_id: Option<String>,

	/// Simulate the transaction and print its logs instead of sending.
	#[arg(long, global = true)]
	simulate: bool,

	/// Print machine-readable JSON output.
	#[arg(long, global = true)]
	json: bool,

	#[command(subcommand)]
	command: Command,
}

#[derive(Debug, Subcommand)]
enum Command {
	ConfigInitialize(commands::ConfigInitializeArgs),

	ConfigUpdate(commands::ConfigUpdateArgs),

	MultisigCreate(commands::MultisigCreateArgs),

	MultisigImport(commands::MultisigImportArgs),

	ProposalCreate(commands::ProposalCreateArgs),

	ProposalActivate(commands::ProposalActivateArgs),

	ProposalApprove(commands::ProposalApproveArgs),

	ProposalReject(commands::ProposalRejectArgs),

	ProposalRevoke(commands::ProposalRevokeArgs),

	ProposalCancel(commands::ProposalCancelArgs),

	VaultExecute(commands::VaultExecuteArgs),

	ConfigExecute(commands::ConfigExecuteArgs),

	ConfigAuthorityExecute(commands::ConfigAuthorityExecuteArgs),

	SpendingLimitUse(commands::SpendingLimitUseArgs),

	ProposalClose(commands::ProposalCloseArgs),
	/// Fetch and decode a program account.
	#[command(subcommand)]
	Fetch(fetch::Command),
}

fn main() {
	let cli = Cli::parse();
	if let Err(error) = run(cli) {
		eprintln!("error: {error}");
		std::process::exit(1);
	}
}

fn run(cli: Cli) -> Result<(), context::CliError> {
	let program_address = match &cli.program_id {
		Some(value) => CliContext::pubkey("--program-id", value)?,
		None => Pubkey::from_str_const("5BeQ7VMZHYdnUD6PyrMd29WQo2DLfo7N2NDXCDQZ5MQc"),
	};
	let context = CliContext::new(
		&cli.url,
		&cli.keypair,
		program_address,
		cli.simulate,
		cli.json,
	)?;

	match cli.command {
		Command::ConfigInitialize(args) => commands::config_initialize::run(&context, args),
		Command::ConfigUpdate(args) => commands::config_update::run(&context, args),
		Command::MultisigCreate(args) => commands::multisig_create::run(&context, args),
		Command::MultisigImport(args) => commands::multisig_import::run(&context, args),
		Command::ProposalCreate(args) => commands::proposal_create::run(&context, args),
		Command::ProposalActivate(args) => commands::proposal_activate::run(&context, args),
		Command::ProposalApprove(args) => commands::proposal_approve::run(&context, args),
		Command::ProposalReject(args) => commands::proposal_reject::run(&context, args),
		Command::ProposalRevoke(args) => commands::proposal_revoke::run(&context, args),
		Command::ProposalCancel(args) => commands::proposal_cancel::run(&context, args),
		Command::VaultExecute(args) => commands::vault_execute::run(&context, args),
		Command::ConfigExecute(args) => commands::config_execute::run(&context, args),
		Command::ConfigAuthorityExecute(args) => {
			commands::config_authority_execute::run(&context, args)
		}
		Command::SpendingLimitUse(args) => commands::spending_limit_use::run(&context, args),
		Command::ProposalClose(args) => commands::proposal_close::run(&context, args),
		Command::Fetch(command) => fetch::run(&context, command),
	}
}
