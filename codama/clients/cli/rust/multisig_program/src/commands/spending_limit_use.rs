//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use multisig_program_client::instructions::SpendingLimitUse;
use multisig_program_client::instructions::SpendingLimitUseInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct SpendingLimitUseArgs {
	#[arg(long)]
	amount: u64,
	#[arg(long)]
	decimals: u8,
	/// The `multisig` account
	#[arg(long)]
	multisig: String,
	/// The `spending_limit` account
	#[arg(long)]
	spending_limit: String,
	/// The `member` account [default: payer]
	#[arg(long)]
	member: Option<String>,
	/// The vault PDA: SOL source or SPL transfer authority
	#[arg(long)]
	vault: String,
	/// SOL destination, or the destination token account for SPL
	#[arg(long)]
	destination: String,
	/// The `clock` account
	#[arg(long)]
	clock: String,
	/// SPL source token account; absent for SOL
	#[arg(long)]
	vault_token_account: String,
	/// SPL mint; absent for SOL
	#[arg(long)]
	mint: String,
	/// SPL token program; absent for SOL
	#[arg(long)]
	token_program: String,
	/// System program; absent for SPL
	#[arg(long)]
	system_program: String,
}

pub(crate) fn run(context: &CliContext, args: SpendingLimitUseArgs) -> Result<(), CliError> {
	let multisig = CliContext::pubkey("--multisig", &args.multisig)?;
	let spending_limit = CliContext::pubkey("--spending_limit", &args.spending_limit)?;
	let member = match &args.member {
		Some(value) => CliContext::pubkey("--member", value)?,
		None => context.payer_pubkey(),
	};
	let vault = CliContext::pubkey("--vault", &args.vault)?;
	let destination = CliContext::pubkey("--destination", &args.destination)?;
	let clock = CliContext::pubkey("--clock", &args.clock)?;
	let vault_token_account =
		CliContext::pubkey("--vault_token_account", &args.vault_token_account)?;
	let mint = CliContext::pubkey("--mint", &args.mint)?;
	let token_program = CliContext::pubkey("--token_program", &args.token_program)?;
	let system_program = CliContext::pubkey("--system_program", &args.system_program)?;
	let data = SpendingLimitUseInstructionData::new(|data| {
		data.amount.set(args.amount);
		data.decimals = args.decimals;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = SpendingLimitUse {
		multisig,
		spending_limit,
		member,
		vault,
		destination,
		clock,
		vault_token_account: Some(vault_token_account),
		mint: Some(mint),
		token_program: Some(token_program),
		system_program: Some(system_program),
	};

	context.send(accounts.instruction(data))
}
