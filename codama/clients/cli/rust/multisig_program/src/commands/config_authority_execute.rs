//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use multisig_program_client::instructions::ConfigAuthorityExecute;
use multisig_program_client::instructions::ConfigAuthorityExecuteInstructionData;
use solana_sdk::pubkey::Pubkey;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct ConfigAuthorityExecuteArgs {
	#[arg(long)]
	actions_len: u16,
	#[arg(long)]
	actions: String,
	/// The `multisig` account
	#[arg(long)]
	multisig: String,
	/// The `authority` account [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// The `rent_payer` account [default: payer]
	#[arg(long)]
	rent_payer: Option<String>,
	/// The `clock` account
	#[arg(long)]
	clock: String,
	/// Spending limit accounts referenced by add/remove spending-limit actions, in any order
	#[arg(long)]
	spending_limit_accounts: String,
}

pub(crate) fn run(context: &CliContext, args: ConfigAuthorityExecuteArgs) -> Result<(), CliError> {
	let actions = CliContext::bytes::<128>("--actions", &args.actions)?;
	let multisig = CliContext::pubkey("--multisig", &args.multisig)?;
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let rent_payer = match &args.rent_payer {
		Some(value) => CliContext::pubkey("--rent_payer", value)?,
		None => context.payer_pubkey(),
	};
	let clock = CliContext::pubkey("--clock", &args.clock)?;
	let spending_limit_accounts =
		CliContext::pubkey("--spending_limit_accounts", &args.spending_limit_accounts)?;
	let data = ConfigAuthorityExecuteInstructionData::new(|data| {
		data.actions_len.set(args.actions_len);
		data.actions = actions;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = ConfigAuthorityExecute {
		multisig,
		authority,
		rent_payer,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
		clock,
		spending_limit_accounts,
	};

	context.send(accounts.instruction(data))
}
