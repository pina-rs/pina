//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use pina_bpf_program_client::instructions::CreatePda;
use pina_bpf_program_client::instructions::CreatePdaInstructionData;
use solana_sdk::pubkey::Pubkey;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct CreatePdaArgs {
	#[arg(long)]
	bump: u8,
	/// The `payer` account [default: payer]
	#[arg(long)]
	payer: Option<String>,
	/// The `state` account [default: derived]
	#[arg(long)]
	state: Option<String>,
}

pub(crate) fn run(context: &CliContext, args: CreatePdaArgs) -> Result<(), CliError> {
	let payer = match &args.payer {
		Some(value) => CliContext::pubkey("--payer", value)?,
		None => context.payer_pubkey(),
	};
	let state = match &args.state {
		Some(value) => CliContext::pubkey("--state", value)?,
		None => Pubkey::find_program_address(&["state".as_bytes()], &context.program_address).0,
	};
	let data = CreatePdaInstructionData::new(|data| {
		data.bump = args.bump;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = CreatePda {
		payer,
		state,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
	};

	context.send(accounts.instruction(data))
}
