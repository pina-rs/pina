//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use prop_amm_program_client::instructions::RotateAuthority;
use prop_amm_program_client::instructions::RotateAuthorityInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct RotateAuthorityArgs {
	#[arg(long)]
	new_authority: String,
	/// The `oracle` account
	#[arg(long)]
	oracle: String,
	/// The `authority` account [default: payer]
	#[arg(long)]
	authority: Option<String>,
}

pub(crate) fn run(context: &CliContext, args: RotateAuthorityArgs) -> Result<(), CliError> {
	let new_authority = CliContext::pubkey("--new_authority", &args.new_authority)?;
	let oracle = CliContext::pubkey("--oracle", &args.oracle)?;
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let data = RotateAuthorityInstructionData::new(|data| {
		data.new_authority = new_authority;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = RotateAuthority { oracle, authority };

	context.send(accounts.instruction(data))
}
