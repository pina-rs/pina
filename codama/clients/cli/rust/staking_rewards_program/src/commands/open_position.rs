//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use solana_sdk::pubkey::Pubkey;
use staking_rewards_program_client::instructions::OpenPosition;
use staking_rewards_program_client::instructions::OpenPositionInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct OpenPositionArgs {
	#[arg(long)]
	bump: u8,
	/// The `user` account [default: payer]
	#[arg(long)]
	user: Option<String>,
	/// The `pool_state` account
	#[arg(long)]
	pool_state: String,
	/// The `position_state` account
	#[arg(long)]
	position_state: String,
}

pub(crate) fn run(context: &CliContext, args: OpenPositionArgs) -> Result<(), CliError> {
	let user = match &args.user {
		Some(value) => CliContext::pubkey("--user", value)?,
		None => context.payer_pubkey(),
	};
	let pool_state = CliContext::pubkey("--pool_state", &args.pool_state)?;
	let position_state = CliContext::pubkey("--position_state", &args.position_state)?;
	let data = OpenPositionInstructionData::new(|data| {
		data.bump = args.bump;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = OpenPosition {
		user,
		pool_state,
		position_state,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
	};

	context.send(accounts.instruction(data))
}
