//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use staking_rewards_program_client::instructions::SetRewardIndex;
use staking_rewards_program_client::instructions::SetRewardIndexInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct SetRewardIndexArgs {
	#[arg(long)]
	new_index: u64,
	/// The `admin` account [default: payer]
	#[arg(long)]
	admin: Option<String>,
	/// The `pool_state` account
	#[arg(long)]
	pool_state: String,
}

pub(crate) fn run(context: &CliContext, args: SetRewardIndexArgs) -> Result<(), CliError> {
	let admin = match &args.admin {
		Some(value) => CliContext::pubkey("--admin", value)?,
		None => context.payer_pubkey(),
	};
	let pool_state = CliContext::pubkey("--pool_state", &args.pool_state)?;
	let data = SetRewardIndexInstructionData::new(|data| {
		data.new_index.set(args.new_index);
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = SetRewardIndex { admin, pool_state };

	context.send(accounts.instruction(data))
}
