//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use solana_sdk::pubkey::Pubkey;
use staking_rewards_program_client::instructions::InitializePool;
use staking_rewards_program_client::instructions::InitializePoolInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct InitializePoolArgs {
	#[arg(long)]
	bump: u8,
	/// The `admin` account [default: payer]
	#[arg(long)]
	admin: Option<String>,
	/// The `stake_mint` account
	#[arg(long)]
	stake_mint: String,
	/// The `reward_mint` account
	#[arg(long)]
	reward_mint: String,
	/// The `pool_state` account [default: derived]
	#[arg(long)]
	pool_state: Option<String>,
	/// The `stake_vault` account
	#[arg(long)]
	stake_vault: String,
	/// The `reward_vault` account
	#[arg(long)]
	reward_vault: String,
	/// The `token_program` account
	#[arg(long)]
	token_program: String,
}

pub(crate) fn run(context: &CliContext, args: InitializePoolArgs) -> Result<(), CliError> {
	let admin = match &args.admin {
		Some(value) => CliContext::pubkey("--admin", value)?,
		None => context.payer_pubkey(),
	};
	let stake_mint = CliContext::pubkey("--stake_mint", &args.stake_mint)?;
	let reward_mint = CliContext::pubkey("--reward_mint", &args.reward_mint)?;
	let pool_state = match &args.pool_state {
		Some(value) => CliContext::pubkey("--pool_state", value)?,
		None => {
			Pubkey::find_program_address(
				&["pool".as_bytes(), stake_mint.as_ref(), reward_mint.as_ref()],
				&context.program_address,
			)
			.0
		}
	};
	let stake_vault = CliContext::pubkey("--stake_vault", &args.stake_vault)?;
	let reward_vault = CliContext::pubkey("--reward_vault", &args.reward_vault)?;
	let token_program = CliContext::pubkey("--token_program", &args.token_program)?;
	let data = InitializePoolInstructionData::new(|data| {
		data.bump = args.bump;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = InitializePool {
		admin,
		stake_mint,
		reward_mint,
		pool_state,
		stake_vault,
		reward_vault,
		associated_token_program: Pubkey::from_str_const(
			"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
		),
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
		token_program,
	};

	context.send(accounts.instruction(data))
}
