//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use solana_sdk::pubkey::Pubkey;
use staking_rewards_program_client::instructions::Withdraw;
use staking_rewards_program_client::instructions::WithdrawInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct WithdrawArgs {
	#[arg(long)]
	amount: u64,
	/// The `user` account [default: payer]
	#[arg(long)]
	user: Option<String>,
	/// The `stake_mint` account
	#[arg(long)]
	stake_mint: String,
	/// The `pool_state` account
	#[arg(long)]
	pool_state: String,
	/// The `position_state` account
	#[arg(long)]
	position_state: String,
	/// The `user_stake_ata` account
	#[arg(long)]
	user_stake_ata: String,
	/// The `token_program` account
	#[arg(long)]
	token_program: String,
}

pub(crate) fn run(context: &CliContext, args: WithdrawArgs) -> Result<(), CliError> {
	let user = match &args.user {
		Some(value) => CliContext::pubkey("--user", value)?,
		None => context.payer_pubkey(),
	};
	let stake_mint = CliContext::pubkey("--stake_mint", &args.stake_mint)?;
	let pool_state = CliContext::pubkey("--pool_state", &args.pool_state)?;
	let position_state = CliContext::pubkey("--position_state", &args.position_state)?;
	let user_stake_ata = CliContext::pubkey("--user_stake_ata", &args.user_stake_ata)?;
	let token_program = CliContext::pubkey("--token_program", &args.token_program)?;
	let data = WithdrawInstructionData::new(|data| {
		data.amount.set(args.amount);
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Withdraw {
		user,
		stake_mint,
		pool_state,
		position_state,
		user_stake_ata,
		token_program,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
	};

	context.send(accounts.instruction(data))
}
