//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use transfer_sol_program_client::instructions::DirectTransfer;
use transfer_sol_program_client::instructions::DirectTransferInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct DirectTransferArgs {
	#[arg(long)]
	amount: u64,
	/// The recipient. Must be writable
	#[arg(long)]
	recipient: String,
}

pub(crate) fn run(context: &CliContext, args: DirectTransferArgs) -> Result<(), CliError> {
	let sender = context.payer_pubkey();
	let recipient = CliContext::pubkey("--recipient", &args.recipient)?;
	let data = DirectTransferInstructionData::new(|data| {
		data.amount.set(args.amount);
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = DirectTransfer { sender, recipient };

	context.send(accounts.instruction(data))
}
