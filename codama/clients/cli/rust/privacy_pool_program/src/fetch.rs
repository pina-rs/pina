//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use clap::Subcommand;
use serde_json::Value;
use serde_json::json;
use solana_sdk::pubkey::Pubkey;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Subcommand)]
pub enum Command {
	/// Global pool configuration and governance root.
	PoolConfig(PoolConfigArgs),
	/// The pool's SOL custody. Data-less by design: every lamport in it is
	/// backed by an unspent note commitment in the tree.
	PoolVault(PoolVaultArgs),
	/// Full-node incremental Merkle tree over Poseidon-2 hashes.
	///
	/// Level `ℓ` (0 = leaves) occupies `nodes` at offset
	/// `(1 << (depth+1)) − (1 << (depth+1−ℓ))`, so the root sits in the final
	/// slot. Empty subtrees hold the precomputed zero-hash chain, letting
	/// witnesses cover unwritten leaves.
	MerkleTree(MerkleTreeArgs),
	/// Append-only set of spent nullifiers.
	NullifierSet(NullifierSetArgs),
	/// The disclosure committee. A threshold of these keys must approve before
	/// a disclosure may execute, and each approval is a signed transaction.
	CustodianRegistry(CustodianRegistryArgs),
	/// Entities allowed to file tier-1 and tier-2 disclosure requests.
	RequesterRegistry(RequesterRegistryArgs),
	/// Per-note audit record. The envelope and shares are encrypted by the
	/// depositor's client and opaque to the program; the view key is the
	/// per-note consent key that may grant tier-0 requests and file challenges.
	///
	/// Spent notes are *not* marked here: linking a nullifier to its commitment
	/// would break the privacy the pool exists to provide. The nullifier set is
	/// the only spent record.
	NoteCommitment(NoteCommitmentArgs),
	/// One disclosure request and its state machine.
	DisclosureRequest(DisclosureRequestArgs),
	/// Append-only public record of executed disclosures. The entry is written
	/// by the same instruction that marks the request executed, so execution
	/// without a log record cannot happen.
	DisclosureLog(DisclosureLogArgs),
	/// One installed Groth16 verifying key. Slot 0 verifies withdraw proofs
	/// (two public inputs), slot 1 transfer proofs (three).
	VerifyingKeyAccount(VerifyingKeyAccountArgs),
}

#[derive(Debug, Args)]
pub struct PoolConfigArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct PoolVaultArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct MerkleTreeArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct NullifierSetArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct CustodianRegistryArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct RequesterRegistryArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct NoteCommitmentArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
	/// PDA seed `commitment`.
	#[arg(long)]
	commitment: Option<String>,
}

#[derive(Debug, Args)]
pub struct DisclosureRequestArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
	/// PDA seed `requester`.
	#[arg(long)]
	requester: Option<String>,
	/// PDA seed `nonce`.
	#[arg(long)]
	nonce: Option<u64>,
}

#[derive(Debug, Args)]
pub struct DisclosureLogArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
}

#[derive(Debug, Args)]
pub struct VerifyingKeyAccountArgs {
	/// Account address; overrides PDA derivation.
	#[arg(long)]
	address: Option<String>,
	/// PDA seed `slot`.
	#[arg(long)]
	slot: Option<u64>,
}

pub(crate) fn run(context: &CliContext, command: Command) -> Result<(), CliError> {
	match command {
		Command::PoolConfig(args) => fetch_pool_config(context, args),
		Command::PoolVault(args) => fetch_pool_vault(context, args),
		Command::MerkleTree(args) => fetch_merkle_tree(context, args),
		Command::NullifierSet(args) => fetch_nullifier_set(context, args),
		Command::CustodianRegistry(args) => fetch_custodian_registry(context, args),
		Command::RequesterRegistry(args) => fetch_requester_registry(context, args),
		Command::NoteCommitment(args) => fetch_note_commitment(context, args),
		Command::DisclosureRequest(args) => fetch_disclosure_request(context, args),
		Command::DisclosureLog(args) => fetch_disclosure_log(context, args),
		Command::VerifyingKeyAccount(args) => fetch_verifying_key_account(context, args),
	}
}

fn fetch_pool_config(context: &CliContext, args: PoolConfigArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-config".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::PoolConfig::from_bytes(&data) {
		Ok(account) => pool_config_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "pool-config", &address, &value);
	Ok(())
}

fn fetch_pool_vault(context: &CliContext, args: PoolVaultArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-vault".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::PoolVault::from_bytes(&data) {
		Ok(account) => pool_vault_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "pool-vault", &address, &value);
	Ok(())
}

fn fetch_merkle_tree(context: &CliContext, args: MerkleTreeArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-tree".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::MerkleTree::from_bytes(&data) {
		Ok(account) => merkle_tree_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "merkle-tree", &address, &value);
	Ok(())
}

fn fetch_nullifier_set(context: &CliContext, args: NullifierSetArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-nullifiers".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::NullifierSet::from_bytes(&data) {
		Ok(account) => nullifier_set_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "nullifier-set", &address, &value);
	Ok(())
}

fn fetch_custodian_registry(
	context: &CliContext,
	args: CustodianRegistryArgs,
) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-custodians".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::CustodianRegistry::from_bytes(&data) {
		Ok(account) => custodian_registry_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "custodian-registry", &address, &value);
	Ok(())
}

fn fetch_requester_registry(
	context: &CliContext,
	args: RequesterRegistryArgs,
) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-requesters".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::RequesterRegistry::from_bytes(&data) {
		Ok(account) => requester_registry_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "requester-registry", &address, &value);
	Ok(())
}

fn fetch_note_commitment(context: &CliContext, args: NoteCommitmentArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let commitment = match &args.commitment {
				Some(value) => CliContext::pubkey("--commitment", value)?,
				None => {
					return Err(CliError::MissingSeed {
						account: "note-commitment".to_string(),
						seed: "--commitment".to_string(),
					});
				}
			};
			let seeds: &[&[u8]] = &["privacy-pool-note".as_bytes(), commitment.as_ref()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::NoteCommitment::from_bytes(&data) {
		Ok(account) => note_commitment_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "note-commitment", &address, &value);
	Ok(())
}

fn fetch_disclosure_request(
	context: &CliContext,
	args: DisclosureRequestArgs,
) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let requester = match &args.requester {
				Some(value) => CliContext::pubkey("--requester", value)?,
				None => {
					return Err(CliError::MissingSeed {
						account: "disclosure-request".to_string(),
						seed: "--requester".to_string(),
					});
				}
			};
			let nonce = args.nonce.ok_or_else(|| {
				CliError::MissingSeed {
					account: "disclosure-request".to_string(),
					seed: "--nonce".to_string(),
				}
			})?;
			let nonce_bytes = nonce.to_le_bytes();
			let seeds: &[&[u8]] = &[
				"privacy-pool-request".as_bytes(),
				requester.as_ref(),
				nonce_bytes.as_ref(),
			];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::DisclosureRequest::from_bytes(&data) {
		Ok(account) => disclosure_request_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "disclosure-request", &address, &value);
	Ok(())
}

fn fetch_disclosure_log(context: &CliContext, args: DisclosureLogArgs) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let seeds: &[&[u8]] = &["privacy-pool-log".as_bytes()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::DisclosureLog::from_bytes(&data) {
		Ok(account) => disclosure_log_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "disclosure-log", &address, &value);
	Ok(())
}

fn fetch_verifying_key_account(
	context: &CliContext,
	args: VerifyingKeyAccountArgs,
) -> Result<(), CliError> {
	let address = match &args.address {
		Some(value) => CliContext::pubkey("--address", value)?,
		None => {
			let slot = args.slot.ok_or_else(|| {
				CliError::MissingSeed {
					account: "verifying-key-account".to_string(),
					seed: "--slot".to_string(),
				}
			})?;
			let slot_bytes = slot.to_le_bytes();
			let seeds: &[&[u8]] = &["privacy-pool-vkey".as_bytes(), slot_bytes.as_ref()];
			Pubkey::find_program_address(seeds, &context.program_address).0
		}
	};
	let data = context.fetch_account(&address)?;
	let value = match privacy_pool_program_client::accounts::VerifyingKeyAccount::from_bytes(&data)
	{
		Ok(account) => verifying_key_account_json(account),
		Err(error) => {
			return Err(CliError::AccountDecode {
				address: address.to_string(),
				message: error.to_string(),
			});
		}
	};
	print_value(context.json, "verifying-key-account", &address, &value);
	Ok(())
}

fn pool_config_json(account: &privacy_pool_program_client::accounts::PoolConfigZc) -> Value {
	json!({
		"bump": account.bump,
		"authority": account.authority.to_string(),
		"custodian_threshold": account.custodian_threshold.get(),
		"challenge_window_secs": account.challenge_window_secs.get(),
		"deposit_lamports": account.deposit_lamports.get(),
		"total_deposits": account.total_deposits.get(),
	})
}

fn pool_vault_json(account: &privacy_pool_program_client::accounts::PoolVaultZc) -> Value {
	json!({
		"bump": account.bump,
	})
}

fn merkle_tree_json(account: &privacy_pool_program_client::accounts::MerkleTreeZc) -> Value {
	json!({
		"bump": account.bump,
		"next_leaf_index": account.next_leaf_index.get(),
		"nodes": account.nodes.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"roots_len": account.roots_len.get(),
		"roots": account.roots.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn nullifier_set_json(account: &privacy_pool_program_client::accounts::NullifierSetZc) -> Value {
	json!({
		"bump": account.bump,
		"count": account.count.get(),
		"nullifiers": account.nullifiers.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn custodian_registry_json(
	account: &privacy_pool_program_client::accounts::CustodianRegistryZc,
) -> Value {
	json!({
		"bump": account.bump,
		"custodians": account.custodians.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn requester_registry_json(
	account: &privacy_pool_program_client::accounts::RequesterRegistryZc,
) -> Value {
	json!({
		"bump": account.bump,
		"count": account.count,
		"keys": account.keys.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"max_tiers": account.max_tiers.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn note_commitment_json(
	account: &privacy_pool_program_client::accounts::NoteCommitmentZc,
) -> Value {
	json!({
		"bump": account.bump,
		"commitment": account.commitment.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"view_pubkey": account.view_pubkey.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"envelope_len": account.envelope_len,
		"envelope": account.envelope.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"shares": account.shares.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn disclosure_request_json(
	account: &privacy_pool_program_client::accounts::DisclosureRequestZc,
) -> Value {
	json!({
		"bump": account.bump,
		"requester": account.requester.to_string(),
		"nonce": account.nonce.get(),
		"tier": account.tier,
		"commitment": account.commitment.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"notice_len": account.notice_len,
		"notice": account.notice.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"legal_basis_hash": account.legal_basis_hash.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"created_at": account.created_at.get(),
		"challenge_deadline": account.challenge_deadline.get(),
		"status": account.status,
		"granted": account.granted,
		"approvals": account.approvals,
	})
}

fn disclosure_log_json(account: &privacy_pool_program_client::accounts::DisclosureLogZc) -> Value {
	json!({
		"bump": account.bump,
		"count": account.count.get(),
		"entries": account.entries.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn verifying_key_account_json(
	account: &privacy_pool_program_client::accounts::VerifyingKeyAccountZc,
) -> Value {
	json!({
		"bump": account.bump,
		"slot": account.slot,
		"alpha_g1": account.alpha_g1.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"beta_g2": account.beta_g2.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"gamma_g2": account.gamma_g2.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"delta_g2": account.delta_g2.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"ic_len": account.ic_len,
		"ic0": account.ic0.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"ic1": account.ic1.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"ic2": account.ic2.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
		"ic3": account.ic3.iter().map(|byte| format!("{byte:02x}")).collect::<String>(),
	})
}

fn print_value(json_output: bool, name: &str, address: &Pubkey, value: &Value) {
	if json_output {
		println!(
			"{}",
			serde_json::to_string_pretty(value).unwrap_or_default()
		);
		return;
	}

	println!("{name} {address}");
	if let Some(fields) = value.as_object() {
		for (key, value) in fields {
			println!("  {key}: {value}");
		}
	}
}
