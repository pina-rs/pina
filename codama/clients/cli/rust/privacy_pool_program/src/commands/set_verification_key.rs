//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use privacy_pool_program_client::instructions::SetVerificationKey;
use privacy_pool_program_client::instructions::SetVerificationKeyInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct SetVerificationKeyArgs {
	#[arg(long)]
	bump: u8,
	#[arg(long)]
	slot: u8,
	#[arg(long)]
	ic_len: u8,
	#[arg(long)]
	alpha_g1: String,
	#[arg(long)]
	beta_g2: String,
	#[arg(long)]
	gamma_g2: String,
	#[arg(long)]
	delta_g2: String,
	#[arg(long)]
	ic0: String,
	#[arg(long)]
	ic1: String,
	#[arg(long)]
	ic2: String,
	#[arg(long)]
	ic3: String,
	/// The `authority` account [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// The `pool_config` account
	#[arg(long)]
	pool_config: String,
	/// The `verifying_key_account` account
	#[arg(long)]
	verifying_key_account: String,
	/// The `system_program` account
	#[arg(long)]
	system_program: String,
}

pub(crate) fn run(context: &CliContext, args: SetVerificationKeyArgs) -> Result<(), CliError> {
	let alpha_g1 = CliContext::bytes::<64>("--alpha_g1", &args.alpha_g1)?;
	let beta_g2 = CliContext::bytes::<128>("--beta_g2", &args.beta_g2)?;
	let gamma_g2 = CliContext::bytes::<128>("--gamma_g2", &args.gamma_g2)?;
	let delta_g2 = CliContext::bytes::<128>("--delta_g2", &args.delta_g2)?;
	let ic0 = CliContext::bytes::<64>("--ic0", &args.ic0)?;
	let ic1 = CliContext::bytes::<64>("--ic1", &args.ic1)?;
	let ic2 = CliContext::bytes::<64>("--ic2", &args.ic2)?;
	let ic3 = CliContext::bytes::<64>("--ic3", &args.ic3)?;
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let pool_config = CliContext::pubkey("--pool_config", &args.pool_config)?;
	let verifying_key_account =
		CliContext::pubkey("--verifying_key_account", &args.verifying_key_account)?;
	let system_program = CliContext::pubkey("--system_program", &args.system_program)?;
	let data = SetVerificationKeyInstructionData::new(|data| {
		data.bump = args.bump;
		data.slot = args.slot;
		data.ic_len = args.ic_len;
		data.alpha_g1 = alpha_g1;
		data.beta_g2 = beta_g2;
		data.gamma_g2 = gamma_g2;
		data.delta_g2 = delta_g2;
		data.ic0 = ic0;
		data.ic1 = ic1;
		data.ic2 = ic2;
		data.ic3 = ic3;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = SetVerificationKey {
		authority,
		pool_config,
		verifying_key_account,
		system_program,
	};

	context.send(accounts.instruction(data))
}
