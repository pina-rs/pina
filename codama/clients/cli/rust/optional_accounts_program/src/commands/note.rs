//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use optional_accounts_program_client::instructions::Note;
use optional_accounts_program_client::instructions::NoteInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct NoteArgs {
	/// The transaction fee payer; always required [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// An arbitrary readonly account attached as context
	#[arg(long)]
	note: String,
}

pub(crate) fn run(context: &CliContext, args: NoteArgs) -> Result<(), CliError> {
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let note = CliContext::pubkey("--note", &args.note)?;
	let data = NoteInstructionData::new(|_data| {}).map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Note {
		authority,
		note: Some(note),
	};

	context.send(accounts.instruction(data))
}
