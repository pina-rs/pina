//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use solana_sdk::pubkey::Pubkey;
use vesting_program_client::instructions::Initialize;
use vesting_program_client::instructions::InitializeInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct InitializeArgs {
	#[arg(long)]
	total_amount: u64,
	#[arg(long)]
	start_ts: u64,
	#[arg(long)]
	cliff_ts: u64,
	#[arg(long)]
	end_ts: u64,
	#[arg(long)]
	bump: u8,
	/// The `beneficiary` account
	#[arg(long)]
	beneficiary: String,
	/// The `mint` account
	#[arg(long)]
	mint: String,
	/// The `vesting_state` account [default: derived]
	#[arg(long)]
	vesting_state: Option<String>,
	/// The `vault` account
	#[arg(long)]
	vault: String,
	/// The admin's source ATA: a schedule becomes active only by moving its whole allocation into the vault in this same instruction, so a valid-looking schedule can never promise value it does not hold
	#[arg(long)]
	admin_ata: String,
	/// The `token_program` account
	#[arg(long)]
	token_program: String,
}

pub(crate) fn run(context: &CliContext, args: InitializeArgs) -> Result<(), CliError> {
	let admin = context.payer_pubkey();
	let beneficiary = CliContext::pubkey("--beneficiary", &args.beneficiary)?;
	let mint = CliContext::pubkey("--mint", &args.mint)?;
	let vesting_state = match &args.vesting_state {
		Some(value) => CliContext::pubkey("--vesting_state", value)?,
		None => {
			Pubkey::find_program_address(
				&[
					"vesting".as_bytes(),
					admin.as_ref(),
					beneficiary.as_ref(),
					mint.as_ref(),
				],
				&context.program_address,
			)
			.0
		}
	};
	let vault = CliContext::pubkey("--vault", &args.vault)?;
	let admin_ata = CliContext::pubkey("--admin_ata", &args.admin_ata)?;
	let token_program = CliContext::pubkey("--token_program", &args.token_program)?;
	let data = InitializeInstructionData::new(|data| {
		data.total_amount.set(args.total_amount);
		data.start_ts.set(args.start_ts);
		data.cliff_ts.set(args.cliff_ts);
		data.end_ts.set(args.end_ts);
		data.bump = args.bump;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Initialize {
		admin,
		beneficiary,
		mint,
		vesting_state,
		vault,
		admin_ata,
		associated_token_program: Pubkey::from_str_const(
			"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
		),
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
		token_program,
	};

	context.send(accounts.instruction(data))
}
