//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use solana_sdk::pubkey::Pubkey;
use validation_program_client::instructions::InitializePolicy;
use validation_program_client::instructions::InitializePolicyInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct InitializePolicyArgs {
	#[arg(long)]
	bump: u8,
	#[arg(long)]
	minimum: u64,
	#[arg(long)]
	maximum: u64,
	#[arg(long)]
	required_approvals: u8,
	/// The `authority` account [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// The `policy` account [default: derived]
	#[arg(long)]
	policy: Option<String>,
}

pub(crate) fn run(context: &CliContext, args: InitializePolicyArgs) -> Result<(), CliError> {
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let policy = match &args.policy {
		Some(value) => CliContext::pubkey("--policy", value)?,
		None => {
			Pubkey::find_program_address(
				&["validation-policy".as_bytes(), authority.as_ref()],
				&context.program_address,
			)
			.0
		}
	};
	let data = InitializePolicyInstructionData::new(|data| {
		data.bump = args.bump;
		data.minimum.set(args.minimum);
		data.maximum.set(args.maximum);
		data.required_approvals = args.required_approvals;
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = InitializePolicy {
		authority,
		policy,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
	};

	context.send(accounts.instruction(data))
}
