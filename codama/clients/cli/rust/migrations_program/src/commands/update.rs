//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use migrations_program_client::instructions::Update;
use migrations_program_client::instructions::UpdateInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct UpdateArgs {
	#[arg(long)]
	value: u64,
	#[arg(long)]
	memo: u16,
	/// The `authority` account [default: payer]
	#[arg(long)]
	authority: Option<String>,
	/// The `referrer` account
	#[arg(long)]
	referrer: String,
	/// The `state` account
	#[arg(long)]
	state: String,
	/// The `migration_payer` account [default: payer]
	#[arg(long)]
	migration_payer: Option<String>,
	/// The `system_program` account
	#[arg(long)]
	system_program: String,
	/// The `manual_state` account
	#[arg(long)]
	manual_state: String,
	/// The `compact_state` account
	#[arg(long)]
	compact_state: String,
}

pub(crate) fn run(context: &CliContext, args: UpdateArgs) -> Result<(), CliError> {
	let authority = match &args.authority {
		Some(value) => CliContext::pubkey("--authority", value)?,
		None => context.payer_pubkey(),
	};
	let referrer = CliContext::pubkey("--referrer", &args.referrer)?;
	let state = CliContext::pubkey("--state", &args.state)?;
	let migration_payer = match &args.migration_payer {
		Some(value) => CliContext::pubkey("--migration_payer", value)?,
		None => context.payer_pubkey(),
	};
	let system_program = CliContext::pubkey("--system_program", &args.system_program)?;
	let manual_state = CliContext::pubkey("--manual_state", &args.manual_state)?;
	let compact_state = CliContext::pubkey("--compact_state", &args.compact_state)?;
	let data = UpdateInstructionData::new(|data| {
		data.value.set(args.value);
		data.memo.set(args.memo);
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Update {
		authority,
		referrer: Some(referrer),
		state: Some(state),
		migration_payer: Some(migration_payer),
		system_program: Some(system_program),
		manual_state: Some(manual_state),
		compact_state: Some(compact_state),
	};

	context.send(accounts.instruction(data))
}
