//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use clap::Args;
use sysvar_checks_program_client::instructions::Sysvars;
use sysvar_checks_program_client::instructions::SysvarsInstructionData;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct SysvarsArgs {
	/// The `clock` account
	#[arg(long)]
	clock: String,
	/// The `rent` account
	#[arg(long)]
	rent: String,
	/// The `stake_history` account
	#[arg(long)]
	stake_history: String,
}

pub(crate) fn run(context: &CliContext, args: SysvarsArgs) -> Result<(), CliError> {
	let clock = CliContext::pubkey("--clock", &args.clock)?;
	let rent = CliContext::pubkey("--rent", &args.rent)?;
	let stake_history = CliContext::pubkey("--stake_history", &args.stake_history)?;
	let data = SysvarsInstructionData::new(|_data| {}).map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Sysvars {
		clock,
		rent,
		stake_history,
	};

	context.send(accounts.instruction(data))
}
