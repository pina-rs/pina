//! This code was AUTOGENERATED by `pina_cli_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from the program IDL instead.
#![allow(
	dead_code,
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]

use account_realloc_program_client::instructions::Realloc;
use account_realloc_program_client::instructions::ReallocInstructionData;
use clap::Args;
use solana_sdk::pubkey::Pubkey;

use crate::context::CliContext;
use crate::context::CliError;

#[derive(Debug, Args)]
pub struct ReallocArgs {
	#[arg(long)]
	len: u16,
	/// The `sample` account
	#[arg(long)]
	sample: String,
}

pub(crate) fn run(context: &CliContext, args: ReallocArgs) -> Result<(), CliError> {
	let authority = context.payer_pubkey();
	let sample = CliContext::pubkey("--sample", &args.sample)?;
	let data = ReallocInstructionData::new(|data| {
		data.len.set(args.len);
	})
	.map_err(|_| {
		CliError::InvalidData {
			message: "instruction data rejected the provided arguments".to_string(),
		}
	})?;

	let accounts = Realloc {
		authority,
		sample,
		system_program: Pubkey::from_str_const("11111111111111111111111111111111"),
	};

	context.send(accounts.instruction(data))
}
