/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
} from "@solana/kit";
import {
	getAccountMetaFactory,
	type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { OPTIONAL_ACCOUNTS_PROGRAM_PROGRAM_ADDRESS } from "../programs";
import { getZeroPodDiscriminatorDecoder } from "../zeropodCodecs";

export const NOTE_DISCRIMINATOR = 3;

export function getNoteDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(NOTE_DISCRIMINATOR);
}

export type NoteInstruction<
	TProgram extends string = typeof OPTIONAL_ACCOUNTS_PROGRAM_PROGRAM_ADDRESS,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountNote extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountAuthority extends string ?
					& ReadonlySignerAccount<TAccountAuthority>
					& AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountNote extends string ? ReadonlyAccount<TAccountNote>
				: TAccountNote,
			...TRemainingAccounts,
		]
	>;

export type NoteInstructionData = { discriminator: number };

export type NoteInstructionDataArgs = {};

export function getNoteInstructionDataEncoder(): FixedSizeEncoder<
	NoteInstructionDataArgs
> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()]]),
		(value) => ({ ...value, discriminator: 3 }),
	);
}

export function getNoteInstructionDataDecoder(): FixedSizeDecoder<
	NoteInstructionData
> {
	return getStructDecoder([[
		"discriminator",
		getZeroPodDiscriminatorDecoder(NOTE_DISCRIMINATOR, getU8Decoder()),
	]]);
}

export function getNoteInstructionDataCodec(): FixedSizeCodec<
	NoteInstructionDataArgs,
	NoteInstructionData
> {
	return combineCodec(
		getNoteInstructionDataEncoder(),
		getNoteInstructionDataDecoder(),
	);
}

export type NoteInput<
	TAccountAuthority extends string = string,
	TAccountNote extends string = string,
> = {
	/** The transaction fee payer; always required. */
	authority: TransactionSigner<TAccountAuthority>;
	/** An arbitrary readonly account attached as context. */
	note?: Address<TAccountNote>;
};

export function getNoteInstruction<
	TAccountAuthority extends string,
	TAccountNote extends string,
	TProgramAddress extends Address =
		typeof OPTIONAL_ACCOUNTS_PROGRAM_PROGRAM_ADDRESS,
>(
	input: NoteInput<TAccountAuthority, TAccountNote>,
	config?: { programAddress?: TProgramAddress },
): NoteInstruction<TProgramAddress, TAccountAuthority, TAccountNote> {
	// Program address.
	const programAddress = config?.programAddress ??
		OPTIONAL_ACCOUNTS_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: false },
		note: { value: input.note ?? null, isWritable: false },
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze(
		{
			accounts: [
				getAccountMeta("authority", accounts.authority),
				getAccountMeta("note", accounts.note),
			],
			data: getNoteInstructionDataEncoder().encode({}),
			programAddress,
		} as NoteInstruction<TProgramAddress, TAccountAuthority, TAccountNote>,
	);
}

export type ParsedNoteInstruction<
	TProgram extends string = typeof OPTIONAL_ACCOUNTS_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		/** The transaction fee payer; always required. */
		authority: TAccountMetas[0];
		/** An arbitrary readonly account attached as context. */
		note?: TAccountMetas[1] | undefined;
	};
	data: NoteInstructionData;
};

export function parseNoteInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction:
		& Instruction<TProgram>
		& InstructionWithAccounts<TAccountMetas>
		& InstructionWithData<ReadonlyUint8Array>,
): ParsedNoteInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		throw new SolanaError(
			SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
			{
				actualAccountMetas: instruction.accounts.length,
				expectedAccountMetas: 2,
			},
		);
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	const getNextOptionalAccount = () => {
		const accountMeta = getNextAccount();
		return accountMeta.address === instruction.programAddress
			? undefined
			: accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { authority: getNextAccount(), note: getNextOptionalAccount() },
		data: getNoteInstructionDataDecoder().decode(instruction.data),
	};
}
