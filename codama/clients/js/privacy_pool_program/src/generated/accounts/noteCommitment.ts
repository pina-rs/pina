/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	combineCodec,
	decodeAccount,
	type EncodedAccount,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	fixDecoderSize,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	fixEncoderSize,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { findNoteCommitmentPda, type NoteCommitmentSeeds } from "../pdas";
import {
	fixPinaPodEncoderSize,
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
} from "../pinaPodCodecs";

export const NOTE_COMMITMENT_DISCRIMINATOR = 8;

export function getNoteCommitmentDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(NOTE_COMMITMENT_DISCRIMINATOR);
}

export const NOTE_COMMITMENT_DISCRIMINATOR2 = 0;

export function getNoteCommitmentDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(NOTE_COMMITMENT_DISCRIMINATOR2);
}

/**
 * Per-note audit record. The envelope and shares are encrypted by the
 * depositor's client and opaque to the program; the view key is the
 * per-note consent key that may grant tier-0 requests and file challenges.
 *
 * Spent notes are *not* marked here: linking a nullifier to its commitment
 * would break the privacy the pool exists to provide. The nullifier set is
 * the only spent record.
 */
export type NoteCommitment = {
	discriminator: number;
	migrationVersion: number;
	bump: number;
	/** The commitment; also the PDA seed. */
	commitment: ReadonlyUint8Array;
	/** Per-note consent/notification public key. */
	viewPubkey: ReadonlyUint8Array;
	/** Active bytes of the encrypted note plaintext, client-opaque. */
	envelopeLen: number;
	envelope: ReadonlyUint8Array;
	/** Concatenated per-custodian encrypted key shares, client-opaque. */
	shares: ReadonlyUint8Array;
};

export type NoteCommitmentArgs = {
	bump: number;
	/** The commitment; also the PDA seed. */
	commitment: ReadonlyUint8Array;
	/** Per-note consent/notification public key. */
	viewPubkey: ReadonlyUint8Array;
	/** Active bytes of the encrypted note plaintext, client-opaque. */
	envelopeLen: number;
	envelope: ReadonlyUint8Array;
	/** Concatenated per-custodian encrypted key shares, client-opaque. */
	shares: ReadonlyUint8Array;
};

/** Gets the encoder for {@link NoteCommitmentArgs} account data. */
export function getNoteCommitmentEncoder(): FixedSizeEncoder<
	NoteCommitmentArgs
> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["bump", getU8Encoder()],
			["commitment", fixPinaPodEncoderSize(getBytesEncoder(), 32)],
			["viewPubkey", fixPinaPodEncoderSize(getBytesEncoder(), 32)],
			["envelopeLen", getU8Encoder()],
			["envelope", fixPinaPodEncoderSize(getBytesEncoder(), 128)],
			["shares", fixPinaPodEncoderSize(getBytesEncoder(), 144)],
		]),
		(value) => ({ ...value, discriminator: 8, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link NoteCommitment} account data. */
export function getNoteCommitmentDecoder(): FixedSizeDecoder<NoteCommitment> {
	return getStructDecoder([
		[
			"discriminator",
			getPinaPodDiscriminatorDecoder(
				NOTE_COMMITMENT_DISCRIMINATOR,
				getU8Decoder(),
			),
		],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["bump", getU8Decoder()],
		["commitment", fixDecoderSize(getBytesDecoder(), 32)],
		["viewPubkey", fixDecoderSize(getBytesDecoder(), 32)],
		["envelopeLen", getU8Decoder()],
		["envelope", fixDecoderSize(getBytesDecoder(), 128)],
		["shares", fixDecoderSize(getBytesDecoder(), 144)],
	]);
}

/** Gets the codec for {@link NoteCommitment} account data. */
export function getNoteCommitmentCodec(): FixedSizeCodec<
	NoteCommitmentArgs,
	NoteCommitment
> {
	return combineCodec(getNoteCommitmentEncoder(), getNoteCommitmentDecoder());
}

export function decodeNoteCommitment<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<NoteCommitment, TAddress>;
export function decodeNoteCommitment<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<NoteCommitment, TAddress>;
export function decodeNoteCommitment<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<NoteCommitment, TAddress> | MaybeAccount<NoteCommitment, TAddress> {
	return decodeAccount(
		encodedAccount as MaybeEncodedAccount<TAddress>,
		getNoteCommitmentDecoder(),
	);
}

export async function fetchNoteCommitment<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<NoteCommitment, TAddress>> {
	const maybeAccount = await fetchMaybeNoteCommitment(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeNoteCommitment<
	TAddress extends string = string,
>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<NoteCommitment, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeNoteCommitment(maybeAccount);
}

export async function fetchAllNoteCommitment(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<NoteCommitment>[]> {
	const maybeAccounts = await fetchAllMaybeNoteCommitment(
		rpc,
		addresses,
		config,
	);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeNoteCommitment(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<NoteCommitment>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) =>
		decodeNoteCommitment(maybeAccount)
	);
}

export async function fetchNoteCommitmentFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: NoteCommitmentSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<NoteCommitment>> {
	const maybeAccount = await fetchMaybeNoteCommitmentFromSeeds(
		rpc,
		seeds,
		config,
	);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeNoteCommitmentFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: NoteCommitmentSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<NoteCommitment>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findNoteCommitmentPda(seeds, { programAddress });
	return await fetchMaybeNoteCommitment(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const NOTE_COMMITMENT_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `NoteCommitment` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (noteCommitmentNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ noteCommitment: address, payer }).make());
 * }
 * ```
 */
export function noteCommitmentNeedsMigration(
	data: ReadonlyUint8Array,
): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 8) {
		return false;
	}
	return data[1]! < 0;
}
