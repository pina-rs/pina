/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	combineCodec,
	decodeAccount,
	type EncodedAccount,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	fixDecoderSize,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	fixEncoderSize,
	getAddressDecoder,
	getAddressEncoder,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	getU64Decoder,
	getU64Encoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { type DisclosureRequestSeeds, findDisclosureRequestPda } from "../pdas";
import {
	fixPinaPodEncoderSize,
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
} from "../pinaPodCodecs";

export const DISCLOSURE_REQUEST_DISCRIMINATOR = 9;

export function getDisclosureRequestDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(DISCLOSURE_REQUEST_DISCRIMINATOR);
}

export const DISCLOSURE_REQUEST_DISCRIMINATOR2 = 0;

export function getDisclosureRequestDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(DISCLOSURE_REQUEST_DISCRIMINATOR2);
}

/** One disclosure request and its state machine. */
export type DisclosureRequest = {
	discriminator: number;
	migrationVersion: number;
	bump: number;
	/** Filing entity. */
	requester: Address;
	/** Client-chosen disambiguator for repeat filings. */
	nonce: bigint;
	/** Tier this request was filed at; see the `TIER_*` constants. */
	tier: number;
	/** Target note commitment. */
	commitment: ReadonlyUint8Array;
	/** Active bytes of the encrypted notice to the subject, client-opaque. */
	noticeLen: number;
	notice: ReadonlyUint8Array;
	/** Hash of the legal basis document; required nonzero above tier 0. */
	legalBasisHash: ReadonlyUint8Array;
	/** Filing unix timestamp. */
	createdAt: bigint;
	/** Challenge deadline unix timestamp (tier 1). */
	challengeDeadline: bigint;
	/** Current status; see the `STATUS_*` constants. */
	status: number;
	/** Set when the note's view key has granted consent. */
	granted: number;
	/** Custodian approval bitmask. */
	approvals: number;
};

export type DisclosureRequestArgs = {
	bump: number;
	/** Filing entity. */
	requester: Address;
	/** Client-chosen disambiguator for repeat filings. */
	nonce: number | bigint;
	/** Tier this request was filed at; see the `TIER_*` constants. */
	tier: number;
	/** Target note commitment. */
	commitment: ReadonlyUint8Array;
	/** Active bytes of the encrypted notice to the subject, client-opaque. */
	noticeLen: number;
	notice: ReadonlyUint8Array;
	/** Hash of the legal basis document; required nonzero above tier 0. */
	legalBasisHash: ReadonlyUint8Array;
	/** Filing unix timestamp. */
	createdAt: number | bigint;
	/** Challenge deadline unix timestamp (tier 1). */
	challengeDeadline: number | bigint;
	/** Current status; see the `STATUS_*` constants. */
	status: number;
	/** Set when the note's view key has granted consent. */
	granted: number;
	/** Custodian approval bitmask. */
	approvals: number;
};

/** Gets the encoder for {@link DisclosureRequestArgs} account data. */
export function getDisclosureRequestEncoder(): FixedSizeEncoder<
	DisclosureRequestArgs
> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["bump", getU8Encoder()],
			["requester", getAddressEncoder()],
			["nonce", getU64Encoder()],
			["tier", getU8Encoder()],
			["commitment", fixPinaPodEncoderSize(getBytesEncoder(), 32)],
			["noticeLen", getU8Encoder()],
			["notice", fixPinaPodEncoderSize(getBytesEncoder(), 96)],
			["legalBasisHash", fixPinaPodEncoderSize(getBytesEncoder(), 32)],
			["createdAt", getU64Encoder()],
			["challengeDeadline", getU64Encoder()],
			["status", getU8Encoder()],
			["granted", getU8Encoder()],
			["approvals", getU8Encoder()],
		]),
		(value) => ({ ...value, discriminator: 9, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link DisclosureRequest} account data. */
export function getDisclosureRequestDecoder(): FixedSizeDecoder<
	DisclosureRequest
> {
	return getStructDecoder([
		[
			"discriminator",
			getPinaPodDiscriminatorDecoder(
				DISCLOSURE_REQUEST_DISCRIMINATOR,
				getU8Decoder(),
			),
		],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["bump", getU8Decoder()],
		["requester", getAddressDecoder()],
		["nonce", getU64Decoder()],
		["tier", getU8Decoder()],
		["commitment", fixDecoderSize(getBytesDecoder(), 32)],
		["noticeLen", getU8Decoder()],
		["notice", fixDecoderSize(getBytesDecoder(), 96)],
		["legalBasisHash", fixDecoderSize(getBytesDecoder(), 32)],
		["createdAt", getU64Decoder()],
		["challengeDeadline", getU64Decoder()],
		["status", getU8Decoder()],
		["granted", getU8Decoder()],
		["approvals", getU8Decoder()],
	]);
}

/** Gets the codec for {@link DisclosureRequest} account data. */
export function getDisclosureRequestCodec(): FixedSizeCodec<
	DisclosureRequestArgs,
	DisclosureRequest
> {
	return combineCodec(
		getDisclosureRequestEncoder(),
		getDisclosureRequestDecoder(),
	);
}

export function decodeDisclosureRequest<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<DisclosureRequest, TAddress>;
export function decodeDisclosureRequest<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DisclosureRequest, TAddress>;
export function decodeDisclosureRequest<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
	| Account<DisclosureRequest, TAddress>
	| MaybeAccount<DisclosureRequest, TAddress> {
	return decodeAccount(
		encodedAccount as MaybeEncodedAccount<TAddress>,
		getDisclosureRequestDecoder(),
	);
}

export async function fetchDisclosureRequest<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<DisclosureRequest, TAddress>> {
	const maybeAccount = await fetchMaybeDisclosureRequest(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeDisclosureRequest<
	TAddress extends string = string,
>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<DisclosureRequest, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeDisclosureRequest(maybeAccount);
}

export async function fetchAllDisclosureRequest(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<DisclosureRequest>[]> {
	const maybeAccounts = await fetchAllMaybeDisclosureRequest(
		rpc,
		addresses,
		config,
	);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeDisclosureRequest(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<DisclosureRequest>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) =>
		decodeDisclosureRequest(maybeAccount)
	);
}

export async function fetchDisclosureRequestFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: DisclosureRequestSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<DisclosureRequest>> {
	const maybeAccount = await fetchMaybeDisclosureRequestFromSeeds(
		rpc,
		seeds,
		config,
	);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeDisclosureRequestFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: DisclosureRequestSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<DisclosureRequest>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findDisclosureRequestPda(seeds, { programAddress });
	return await fetchMaybeDisclosureRequest(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const DISCLOSURE_REQUEST_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `DisclosureRequest` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (disclosureRequestNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ disclosureRequest: address, payer }).make());
 * }
 * ```
 */
export function disclosureRequestNeedsMigration(
	data: ReadonlyUint8Array,
): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 9) {
		return false;
	}
	return data[1]! < 0;
}
