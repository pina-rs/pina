/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findProfilePda } from '../pdas';
import { PROFILE_PROGRAM_PROGRAM_ADDRESS } from '../programs';

export const ADD_TAG_DISCRIMINATOR = 2;

export function getAddTagDiscriminatorBytes(): ReadonlyUint8Array { return getU8Encoder().encode(ADD_TAG_DISCRIMINATOR); }

export type AddTagInstruction<TProgram extends string = typeof PROFILE_PROGRAM_PROGRAM_ADDRESS, TAccountAuthority extends string | AccountMeta<string> = string, TAccountProfile extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountProfile extends string ? WritableAccount<TAccountProfile> : TAccountProfile, ...TRemainingAccounts]>;

export type AddTagInstructionData = { tag: bigint;  };

export type AddTagInstructionDataArgs = { tag: number | bigint;  };

export function getAddTagInstructionDataEncoder(): FixedSizeEncoder<AddTagInstructionDataArgs> {
    return getStructEncoder([['tag', getU64Encoder()]]);
}

export function getAddTagInstructionDataDecoder(): FixedSizeDecoder<AddTagInstructionData> {
    return getStructDecoder([['tag', getU64Decoder()]]);
}

export function getAddTagInstructionDataCodec(): FixedSizeCodec<AddTagInstructionDataArgs, AddTagInstructionData> {
    return combineCodec(getAddTagInstructionDataEncoder(), getAddTagInstructionDataDecoder());
}

export type AddTagAsyncInput<TAccountAuthority extends string = string, TAccountProfile extends string = string> =  {
  /** The profile's authority. Must sign to prove ownership. */
authority: TransactionSigner<TAccountAuthority>;
/** The profile PDA account (must already exist and be writable). */
profile?: Address<TAccountProfile>;
tag: AddTagInstructionDataArgs["tag"];
}

export async function getAddTagInstructionAsync<TAccountAuthority extends string, TAccountProfile extends string, TProgramAddress extends Address = typeof PROFILE_PROGRAM_PROGRAM_ADDRESS>(input: AddTagAsyncInput<TAccountAuthority, TAccountProfile>, config?: { programAddress?: TProgramAddress } ): Promise<AddTagInstruction<TProgramAddress, TAccountAuthority, TAccountProfile>> {
  // Program address.
const programAddress = config?.programAddress ?? PROFILE_PROGRAM_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { authority: { value: input.authority ?? null, isWritable: false }, profile: { value: input.profile ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.profile.value) {
accounts.profile.value = await findProfilePda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("authority", accounts.authority), getAccountMeta("profile", accounts.profile)], data: getAddTagInstructionDataEncoder().encode(args as AddTagInstructionDataArgs), programAddress } as AddTagInstruction<TProgramAddress, TAccountAuthority, TAccountProfile>);
}

export type AddTagInput<TAccountAuthority extends string = string, TAccountProfile extends string = string> =  {
  /** The profile's authority. Must sign to prove ownership. */
authority: TransactionSigner<TAccountAuthority>;
/** The profile PDA account (must already exist and be writable). */
profile: Address<TAccountProfile>;
tag: AddTagInstructionDataArgs["tag"];
}

export function getAddTagInstruction<TAccountAuthority extends string, TAccountProfile extends string, TProgramAddress extends Address = typeof PROFILE_PROGRAM_PROGRAM_ADDRESS>(input: AddTagInput<TAccountAuthority, TAccountProfile>, config?: { programAddress?: TProgramAddress } ): AddTagInstruction<TProgramAddress, TAccountAuthority, TAccountProfile> {
  // Program address.
const programAddress = config?.programAddress ?? PROFILE_PROGRAM_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { authority: { value: input.authority ?? null, isWritable: false }, profile: { value: input.profile ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("authority", accounts.authority), getAccountMeta("profile", accounts.profile)], data: getAddTagInstructionDataEncoder().encode(args as AddTagInstructionDataArgs), programAddress } as AddTagInstruction<TProgramAddress, TAccountAuthority, TAccountProfile>);
}

export type ParsedAddTagInstruction<TProgram extends string = typeof PROFILE_PROGRAM_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/** The profile's authority. Must sign to prove ownership. */
authority: TAccountMetas[0];
/** The profile PDA account (must already exist and be writable). */
profile: TAccountMetas[1];
};
data: AddTagInstructionData; };

export function parseAddTagInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedAddTagInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { authority: getNextAccount(), profile: getNextAccount() }, data: getAddTagInstructionDataDecoder().decode(instruction.data) };
}