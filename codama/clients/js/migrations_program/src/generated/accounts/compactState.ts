/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	addDecoderSizePrefix,
	addEncoderSizePrefix,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	type Codec,
	combineCodec,
	decodeAccount,
	type Decoder,
	type EncodedAccount,
	type Encoder,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	getArrayDecoder,
	getArrayEncoder,
	getStructDecoder,
	getStructEncoder,
	getU16Decoder,
	getU16Encoder,
	getU8Decoder,
	getU8Encoder,
	getUtf8Decoder,
	getUtf8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	offsetDecoder,
	offsetEncoder,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import {
	getPinaPodBoundedArrayDecoder,
	getPinaPodBoundedArrayEncoder,
	getPinaPodBoundedCountDecoder,
	getPinaPodBoundedStringDecoder,
	getPinaPodBoundedStringEncoder,
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
	getPinaPodUtf8Decoder,
} from "../pinaPodCodecs";

export const COMPACT_STATE_DISCRIMINATOR = 3;

export function getCompactStateDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(COMPACT_STATE_DISCRIMINATOR);
}

export const COMPACT_STATE_DISCRIMINATOR2 = 1;

export function getCompactStateDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(COMPACT_STATE_DISCRIMINATOR2);
}

export type CompactState = {
	discriminator: number;
	migrationVersion: number;
	name: string;
	tags: Array<number>;
};

export type CompactStateArgs = { name: string; tags: Array<number> };

/** Gets the encoder for {@link CompactStateArgs} account data. */
export function getCompactStateEncoder(): Encoder<CompactStateArgs> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], [
			"migrationVersion",
			getU8Encoder(),
		], [
			"name",
			offsetEncoder(
				getPinaPodBoundedStringEncoder(
					addEncoderSizePrefix(
						getUtf8Encoder(),
						offsetEncoder(
							offsetEncoder(getU8Encoder(), { preOffset: () => 2 }),
							{ postOffset: ({ preOffset }) => preOffset + 0 },
						),
					),
					4,
				),
				{ preOffset: ({ preOffset }) => preOffset + 3 },
			),
		], [
			"tags",
			getPinaPodBoundedArrayEncoder(
				getArrayEncoder(getU16Encoder(), {
					size: offsetEncoder(
						offsetEncoder(getU16Encoder(), { preOffset: () => 3 }),
						{ postOffset: ({ preOffset }) => preOffset + 0 },
					),
				}),
				2,
			),
		]]),
		(value) => ({ ...value, discriminator: 3, migrationVersion: 1 }),
	);
}

/** Gets the decoder for {@link CompactState} account data. */
export function getCompactStateDecoder(): Decoder<CompactState> {
	return getStructDecoder([
		[
			"discriminator",
			getPinaPodDiscriminatorDecoder(
				COMPACT_STATE_DISCRIMINATOR,
				getU8Decoder(),
			),
		],
		["migrationVersion", getPinaPodMigrationVersionDecoder(1, getU8Decoder())],
		[
			"name",
			offsetDecoder(
				getPinaPodBoundedStringDecoder(
					addDecoderSizePrefix(
						getPinaPodUtf8Decoder(),
						getPinaPodBoundedCountDecoder(
							offsetDecoder(
								offsetDecoder(getU8Decoder(), { preOffset: () => 2 }),
								{ postOffset: ({ preOffset }) => preOffset + 0 },
							),
							4,
						),
					),
					4,
				),
				{ preOffset: ({ preOffset }) => preOffset + 3 },
			),
		],
		[
			"tags",
			getPinaPodBoundedArrayDecoder(
				getArrayDecoder(getU16Decoder(), {
					size: offsetDecoder(
						offsetDecoder(getU16Decoder(), { preOffset: () => 3 }),
						{ postOffset: ({ preOffset }) => preOffset + 0 },
					),
				}),
				getPinaPodBoundedCountDecoder(
					offsetDecoder(
						offsetDecoder(getU16Decoder(), { preOffset: () => 3 }),
						{ postOffset: ({ preOffset }) => preOffset + 0 },
					),
					2,
				),
				2,
			),
		],
	]);
}

/** Gets the codec for {@link CompactState} account data. */
export function getCompactStateCodec(): Codec<CompactStateArgs, CompactState> {
	return combineCodec(getCompactStateEncoder(), getCompactStateDecoder());
}

export function decodeCompactState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<CompactState, TAddress>;
export function decodeCompactState<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<CompactState, TAddress>;
export function decodeCompactState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<CompactState, TAddress> | MaybeAccount<CompactState, TAddress> {
	return decodeAccount(
		encodedAccount as MaybeEncodedAccount<TAddress>,
		getCompactStateDecoder(),
	);
}

export async function fetchCompactState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<CompactState, TAddress>> {
	const maybeAccount = await fetchMaybeCompactState(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeCompactState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<CompactState, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeCompactState(maybeAccount);
}

export async function fetchAllCompactState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<CompactState>[]> {
	const maybeAccounts = await fetchAllMaybeCompactState(rpc, addresses, config);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeCompactState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<CompactState>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) => decodeCompactState(maybeAccount));
}

/** The account schema version this client was generated from. */
export const COMPACT_STATE_MIGRATION_VERSION = 1;

/**
 * Cheap envelope check for a fetched `CompactState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (compactStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ compactState: address, payer }).make());
 * }
 * ```
 */
export function compactStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 3) {
		return false;
	}
	return data[1]! < 1;
}
