/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	combineCodec,
	decodeAccount,
	type EncodedAccount,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getAddressDecoder,
	getAddressEncoder,
	getStructDecoder,
	getStructEncoder,
	getU64Decoder,
	getU64Encoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { findProgramConfigPda } from "../pdas";
import {
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
} from "../pinaPodCodecs";

export const PROGRAM_CONFIG_DISCRIMINATOR = 1;

export function getProgramConfigDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(PROGRAM_CONFIG_DISCRIMINATOR);
}

export const PROGRAM_CONFIG_DISCRIMINATOR2 = 0;

export function getProgramConfigDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(PROGRAM_CONFIG_DISCRIMINATOR2);
}

/**
 * Global program configuration: the authority that may update it, the
 * treasury that collects multisig creation fees, and the fee itself.
 */
export type ProgramConfig = {
	discriminator: number;
	migrationVersion: number;
	bump: number;
	authority: Address;
	treasury: Address;
	creationFee: bigint;
};

export type ProgramConfigArgs = {
	bump: number;
	authority: Address;
	treasury: Address;
	creationFee: number | bigint;
};

/** Gets the encoder for {@link ProgramConfigArgs} account data. */
export function getProgramConfigEncoder(): FixedSizeEncoder<ProgramConfigArgs> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["bump", getU8Encoder()],
			["authority", getAddressEncoder()],
			["treasury", getAddressEncoder()],
			["creationFee", getU64Encoder()],
		]),
		(value) => ({ ...value, discriminator: 1, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link ProgramConfig} account data. */
export function getProgramConfigDecoder(): FixedSizeDecoder<ProgramConfig> {
	return getStructDecoder([
		[
			"discriminator",
			getPinaPodDiscriminatorDecoder(
				PROGRAM_CONFIG_DISCRIMINATOR,
				getU8Decoder(),
			),
		],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["bump", getU8Decoder()],
		["authority", getAddressDecoder()],
		["treasury", getAddressDecoder()],
		["creationFee", getU64Decoder()],
	]);
}

/** Gets the codec for {@link ProgramConfig} account data. */
export function getProgramConfigCodec(): FixedSizeCodec<
	ProgramConfigArgs,
	ProgramConfig
> {
	return combineCodec(getProgramConfigEncoder(), getProgramConfigDecoder());
}

export function decodeProgramConfig<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<ProgramConfig, TAddress>;
export function decodeProgramConfig<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ProgramConfig, TAddress>;
export function decodeProgramConfig<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ProgramConfig, TAddress> | MaybeAccount<ProgramConfig, TAddress> {
	return decodeAccount(
		encodedAccount as MaybeEncodedAccount<TAddress>,
		getProgramConfigDecoder(),
	);
}

export async function fetchProgramConfig<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<ProgramConfig, TAddress>> {
	const maybeAccount = await fetchMaybeProgramConfig(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeProgramConfig<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<ProgramConfig, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeProgramConfig(maybeAccount);
}

export async function fetchAllProgramConfig(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<ProgramConfig>[]> {
	const maybeAccounts = await fetchAllMaybeProgramConfig(
		rpc,
		addresses,
		config,
	);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeProgramConfig(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<ProgramConfig>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) => decodeProgramConfig(maybeAccount));
}

export async function fetchProgramConfigFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<ProgramConfig>> {
	const maybeAccount = await fetchMaybeProgramConfigFromSeeds(rpc, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeProgramConfigFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<ProgramConfig>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findProgramConfigPda({ programAddress });
	return await fetchMaybeProgramConfig(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const PROGRAM_CONFIG_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `ProgramConfig` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (programConfigNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ programConfig: address, payer }).make());
 * }
 * ```
 */
export function programConfigNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 1) {
		return false;
	}
	return data[1]! < 0;
}
