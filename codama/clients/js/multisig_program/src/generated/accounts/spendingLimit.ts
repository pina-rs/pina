/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	type Codec,
	combineCodec,
	decodeAccount,
	type Decoder,
	type EncodedAccount,
	type Encoder,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	getAddressDecoder,
	getAddressEncoder,
	getArrayDecoder,
	getArrayEncoder,
	getI64Decoder,
	getI64Encoder,
	getStructDecoder,
	getStructEncoder,
	getU16Decoder,
	getU16Encoder,
	getU64Decoder,
	getU64Encoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	offsetDecoder,
	offsetEncoder,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { findSpendingLimitPda, type SpendingLimitSeeds } from "../pdas";
import {
	getPinaPodBoundedArrayDecoder,
	getPinaPodBoundedArrayEncoder,
	getPinaPodBoundedCountDecoder,
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
} from "../pinaPodCodecs";

export const SPENDING_LIMIT_DISCRIMINATOR = 4;

export function getSpendingLimitDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SPENDING_LIMIT_DISCRIMINATOR);
}

export const SPENDING_LIMIT_DISCRIMINATOR2 = 0;

export function getSpendingLimitDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SPENDING_LIMIT_DISCRIMINATOR2);
}

/** A member-scoped allowance to spend from a vault without a vote. */
export type SpendingLimit = {
	discriminator: number;
	migrationVersion: number;
	bump: number;
	multisig: Address;
	createKey: Address;
	/** Vault the allowance draws from. */
	vaultIndex: number;
	/** The default address means SOL; anything else is an SPL mint. */
	mint: Address;
	/** Allowance per period, in native mint decimals. */
	amount: bigint;
	/** Remaining allowance in the current period. */
	remainingAmount: bigint;
	/** When the current period started. */
	lastReset: bigint;
	/** Reset period; see the `PERIOD_*` constants. */
	period: number;
	/** Members allowed to draw on this limit, as concatenated addresses. */
	members: Array<number>;
	/**
	 * Allowed destinations as concatenated addresses; empty means
	 * unrestricted.
	 */
	destinations: Array<number>;
};

export type SpendingLimitArgs = {
	bump: number;
	multisig: Address;
	createKey: Address;
	/** Vault the allowance draws from. */
	vaultIndex: number;
	/** The default address means SOL; anything else is an SPL mint. */
	mint: Address;
	/** Allowance per period, in native mint decimals. */
	amount: number | bigint;
	/** Remaining allowance in the current period. */
	remainingAmount: number | bigint;
	/** When the current period started. */
	lastReset: number | bigint;
	/** Reset period; see the `PERIOD_*` constants. */
	period: number;
	/** Members allowed to draw on this limit, as concatenated addresses. */
	members: Array<number>;
	/**
	 * Allowed destinations as concatenated addresses; empty means
	 * unrestricted.
	 */
	destinations: Array<number>;
};

/** Gets the encoder for {@link SpendingLimitArgs} account data. */
export function getSpendingLimitEncoder(): Encoder<SpendingLimitArgs> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["bump", getU8Encoder()],
			["multisig", getAddressEncoder()],
			["createKey", getAddressEncoder()],
			["vaultIndex", getU8Encoder()],
			["mint", getAddressEncoder()],
			["amount", getU64Encoder()],
			["remainingAmount", getU64Encoder()],
			["lastReset", getI64Encoder()],
			["period", getU8Encoder()],
			[
				"members",
				offsetEncoder(
					getPinaPodBoundedArrayEncoder(
						getArrayEncoder(getU8Encoder(), {
							size: offsetEncoder(
								offsetEncoder(getU16Encoder(), { preOffset: () => 125 }),
								{ postOffset: ({ preOffset }) => preOffset + 0 },
							),
						}),
						512,
					),
					{ preOffset: ({ preOffset }) => preOffset + 4 },
				),
			],
			[
				"destinations",
				getPinaPodBoundedArrayEncoder(
					getArrayEncoder(getU8Encoder(), {
						size: offsetEncoder(
							offsetEncoder(getU16Encoder(), { preOffset: () => 127 }),
							{ postOffset: ({ preOffset }) => preOffset + 0 },
						),
					}),
					256,
				),
			],
		]),
		(value) => ({ ...value, discriminator: 4, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link SpendingLimit} account data. */
export function getSpendingLimitDecoder(): Decoder<SpendingLimit> {
	return getStructDecoder([
		[
			"discriminator",
			getPinaPodDiscriminatorDecoder(
				SPENDING_LIMIT_DISCRIMINATOR,
				getU8Decoder(),
			),
		],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["bump", getU8Decoder()],
		["multisig", getAddressDecoder()],
		["createKey", getAddressDecoder()],
		["vaultIndex", getU8Decoder()],
		["mint", getAddressDecoder()],
		["amount", getU64Decoder()],
		["remainingAmount", getU64Decoder()],
		["lastReset", getI64Decoder()],
		["period", getU8Decoder()],
		[
			"members",
			offsetDecoder(
				getPinaPodBoundedArrayDecoder(
					getArrayDecoder(getU8Decoder(), {
						size: offsetDecoder(
							offsetDecoder(getU16Decoder(), { preOffset: () => 125 }),
							{ postOffset: ({ preOffset }) => preOffset + 0 },
						),
					}),
					getPinaPodBoundedCountDecoder(
						offsetDecoder(
							offsetDecoder(getU16Decoder(), { preOffset: () => 125 }),
							{ postOffset: ({ preOffset }) => preOffset + 0 },
						),
						512,
					),
					512,
				),
				{ preOffset: ({ preOffset }) => preOffset + 4 },
			),
		],
		[
			"destinations",
			getPinaPodBoundedArrayDecoder(
				getArrayDecoder(getU8Decoder(), {
					size: offsetDecoder(
						offsetDecoder(getU16Decoder(), { preOffset: () => 127 }),
						{ postOffset: ({ preOffset }) => preOffset + 0 },
					),
				}),
				getPinaPodBoundedCountDecoder(
					offsetDecoder(
						offsetDecoder(getU16Decoder(), { preOffset: () => 127 }),
						{ postOffset: ({ preOffset }) => preOffset + 0 },
					),
					256,
				),
				256,
			),
		],
	]);
}

/** Gets the codec for {@link SpendingLimit} account data. */
export function getSpendingLimitCodec(): Codec<
	SpendingLimitArgs,
	SpendingLimit
> {
	return combineCodec(getSpendingLimitEncoder(), getSpendingLimitDecoder());
}

export function decodeSpendingLimit<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<SpendingLimit, TAddress>;
export function decodeSpendingLimit<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SpendingLimit, TAddress>;
export function decodeSpendingLimit<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SpendingLimit, TAddress> | MaybeAccount<SpendingLimit, TAddress> {
	return decodeAccount(
		encodedAccount as MaybeEncodedAccount<TAddress>,
		getSpendingLimitDecoder(),
	);
}

export async function fetchSpendingLimit<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<SpendingLimit, TAddress>> {
	const maybeAccount = await fetchMaybeSpendingLimit(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeSpendingLimit<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<SpendingLimit, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeSpendingLimit(maybeAccount);
}

export async function fetchAllSpendingLimit(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<SpendingLimit>[]> {
	const maybeAccounts = await fetchAllMaybeSpendingLimit(
		rpc,
		addresses,
		config,
	);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeSpendingLimit(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<SpendingLimit>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) => decodeSpendingLimit(maybeAccount));
}

export async function fetchSpendingLimitFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: SpendingLimitSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<SpendingLimit>> {
	const maybeAccount = await fetchMaybeSpendingLimitFromSeeds(
		rpc,
		seeds,
		config,
	);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeSpendingLimitFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: SpendingLimitSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<SpendingLimit>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findSpendingLimitPda(seeds, { programAddress });
	return await fetchMaybeSpendingLimit(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const SPENDING_LIMIT_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `SpendingLimit` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (spendingLimitNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ spendingLimit: address, payer }).make());
 * }
 * ```
 */
export function spendingLimitNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 4) {
		return false;
	}
	return data[1]! < 0;
}
