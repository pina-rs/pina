/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type WritableAccount,
} from "@solana/kit";
import {
	getAccountMetaFactory,
	type InstructionAccountInput,
	type InstructionAccountInputAddress,
	type InstructionSignerInput,
	type ResolvedInstructionAccount,
	type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import {
	getPinaPodDiscriminatorDecoder,
	getPinaPodMigrationVersionDecoder,
} from "../pinaPodCodecs";
import { MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const VAULT_EXECUTE_DISCRIMINATOR = 10;

export function getVaultExecuteDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(VAULT_EXECUTE_DISCRIMINATOR);
}

export const VAULT_EXECUTE_DISCRIMINATOR2 = 0;

export function getVaultExecuteDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(VAULT_EXECUTE_DISCRIMINATOR2);
}

export type VaultExecuteInstruction<
	TProgram extends string = typeof MULTISIG_PROGRAM_PROGRAM_ADDRESS,
	TAccountMultisig extends string | AccountMeta<string> = string,
	TAccountProposal extends string | AccountMeta<string> = string,
	TAccountMember extends string | AccountMeta<string> = string,
	TAccountClock extends string | AccountMeta<string> = string,
	TAccountMessageAccounts extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountMultisig extends string ? ReadonlyAccount<TAccountMultisig>
				: TAccountMultisig,
			TAccountProposal extends string ? WritableAccount<TAccountProposal>
				: TAccountProposal,
			TAccountMember extends string ?
					& ReadonlySignerAccount<TAccountMember>
					& AccountSignerMeta<TAccountMember>
				: TAccountMember,
			TAccountClock extends string ? ReadonlyAccount<TAccountClock>
				: TAccountClock,
			TAccountMessageAccounts extends string
				? ReadonlyAccount<TAccountMessageAccounts>
				: TAccountMessageAccounts,
			...TRemainingAccounts,
		]
	>;

export type VaultExecuteInstructionData = {
	discriminator: number;
	migrationVersion: number;
};

export type VaultExecuteInstructionDataArgs = {};

export function getVaultExecuteInstructionDataEncoder(): FixedSizeEncoder<
	VaultExecuteInstructionDataArgs
> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], [
			"migrationVersion",
			getU8Encoder(),
		]]),
		(value) => ({ ...value, discriminator: 10, migrationVersion: 0 }),
	);
}

export function getVaultExecuteInstructionDataDecoder(): FixedSizeDecoder<
	VaultExecuteInstructionData
> {
	return getStructDecoder([[
		"discriminator",
		getPinaPodDiscriminatorDecoder(VAULT_EXECUTE_DISCRIMINATOR, getU8Decoder()),
	], [
		"migrationVersion",
		getPinaPodMigrationVersionDecoder(0, getU8Decoder()),
	]]);
}

export function getVaultExecuteInstructionDataCodec(): FixedSizeCodec<
	VaultExecuteInstructionDataArgs,
	VaultExecuteInstructionData
> {
	return combineCodec(
		getVaultExecuteInstructionDataEncoder(),
		getVaultExecuteInstructionDataDecoder(),
	);
}

export type VaultExecuteInput<
	TAccountMultisig extends InstructionAccountInput = InstructionAccountInput,
	TAccountProposal extends InstructionAccountInput = InstructionAccountInput,
	TAccountMember extends InstructionSignerInput = InstructionSignerInput,
	TAccountClock extends InstructionAccountInput = InstructionAccountInput,
	TAccountMessageAccounts extends InstructionAccountInput =
		InstructionAccountInput,
> = {
	multisig: TAccountMultisig;
	proposal: TAccountProposal;
	member: TAccountMember;
	clock: TAccountClock;
	/**
	 * The message's account keys, in message order. Accounts the message
	 * marks writable must also be writable in this instruction.
	 */
	messageAccounts: TAccountMessageAccounts;
};

export function getVaultExecuteInstruction<
	TAccountMultisig extends InstructionAccountInput,
	TAccountProposal extends InstructionAccountInput,
	TAccountMember extends InstructionSignerInput,
	TAccountClock extends InstructionAccountInput,
	TAccountMessageAccounts extends InstructionAccountInput,
	TProgramAddress extends Address = typeof MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
	input: VaultExecuteInput<
		TAccountMultisig,
		TAccountProposal,
		TAccountMember,
		TAccountClock,
		TAccountMessageAccounts
	>,
	config?: { programAddress?: TProgramAddress },
): VaultExecuteInstruction<
	TProgramAddress,
	ResolvedInstructionAccountMeta<
		TAccountMultisig,
		InstructionAccountInputAddress<TAccountMultisig>
	>,
	ResolvedInstructionAccountMeta<
		TAccountProposal,
		InstructionAccountInputAddress<TAccountProposal>
	>,
	ResolvedInstructionAccountMeta<
		TAccountMember,
		InstructionAccountInputAddress<TAccountMember>
	>,
	ResolvedInstructionAccountMeta<
		TAccountClock,
		InstructionAccountInputAddress<TAccountClock>
	>,
	ResolvedInstructionAccountMeta<
		TAccountMessageAccounts,
		InstructionAccountInputAddress<TAccountMessageAccounts>
	>
> {
	// Program address.
	const programAddress = config?.programAddress ??
		MULTISIG_PROGRAM_PROGRAM_ADDRESS;

	// Account meta helper.
	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

	// Original accounts.
	const originalAccounts = {
		multisig: {
			value: input.multisig ?? null,
			isSigner: false,
			isWritable: false,
		},
		proposal: {
			value: input.proposal ?? null,
			isSigner: false,
			isWritable: true,
		},
		member: { value: input.member ?? null, isSigner: true, isWritable: false },
		clock: { value: input.clock ?? null, isSigner: false, isWritable: false },
		messageAccounts: {
			value: input.messageAccounts ?? null,
			isSigner: false,
			isWritable: false,
		},
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	return Object.freeze({
		accounts: [
			getAccountMeta("multisig", accounts.multisig),
			getAccountMeta("proposal", accounts.proposal),
			getAccountMeta("member", accounts.member),
			getAccountMeta("clock", accounts.clock),
			getAccountMeta("messageAccounts", accounts.messageAccounts),
		],
		data: getVaultExecuteInstructionDataEncoder().encode({}),
		programAddress,
	} as VaultExecuteInstruction<
		TProgramAddress,
		ResolvedInstructionAccountMeta<
			TAccountMultisig,
			InstructionAccountInputAddress<TAccountMultisig>
		>,
		ResolvedInstructionAccountMeta<
			TAccountProposal,
			InstructionAccountInputAddress<TAccountProposal>
		>,
		ResolvedInstructionAccountMeta<
			TAccountMember,
			InstructionAccountInputAddress<TAccountMember>
		>,
		ResolvedInstructionAccountMeta<
			TAccountClock,
			InstructionAccountInputAddress<TAccountClock>
		>,
		ResolvedInstructionAccountMeta<
			TAccountMessageAccounts,
			InstructionAccountInputAddress<TAccountMessageAccounts>
		>
	>);
}

export type ParsedVaultExecuteInstruction<
	TProgram extends string = typeof MULTISIG_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		multisig: TAccountMetas[0];
		proposal: TAccountMetas[1];
		member: TAccountMetas[2];
		clock: TAccountMetas[3];
		/**
		 * The message's account keys, in message order. Accounts the message
		 * marks writable must also be writable in this instruction.
		 */
		messageAccounts: TAccountMetas[4];
	};
	data: VaultExecuteInstructionData;
};

export function parseVaultExecuteInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction:
		& Instruction<TProgram>
		& InstructionWithAccounts<TAccountMetas>
		& InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultExecuteInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 5) {
		throw new SolanaError(
			SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
			{
				actualAccountMetas: instruction.accounts.length,
				expectedAccountMetas: 5,
			},
		);
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			multisig: getNextAccount(),
			proposal: getNextAccount(),
			member: getNextAccount(),
			clock: getNextAccount(),
			messageAccounts: getNextAccount(),
		},
		data: getVaultExecuteInstructionDataDecoder().decode(instruction.data),
	};
}
