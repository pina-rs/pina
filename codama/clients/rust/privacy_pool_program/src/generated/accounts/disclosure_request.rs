//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct DisclosureRequest {
	/// One disclosure request and its state machine.
	pub discriminator: u8,
	pub migration_version: u8,
	pub bump: u8,
	/// Filing entity.
	pub requester: solana_pubkey::Pubkey,
	/// Client-chosen disambiguator for repeat filings.
	pub nonce: u64,
	/// Tier this request was filed at; see the `TIER_*` constants.
	pub tier: u8,
	/// Target note commitment.
	pub commitment: [u8; 32],
	/// Active bytes of the encrypted notice to the subject, client-opaque.
	pub notice_len: u8,
	pub notice: [u8; 96],
	/// Hash of the legal basis document; required nonzero above tier 0.
	pub legal_basis_hash: [u8; 32],
	/// Filing unix timestamp.
	pub created_at: u64,
	/// Challenge deadline unix timestamp (tier 1).
	pub challenge_deadline: u64,
	/// Current status; see the `STATUS_*` constants.
	pub status: u8,
	/// Set when the note's view key has granted consent.
	pub granted: u8,
	/// Custodian approval bitmask.
	pub approvals: u8,
}

pub const DISCLOSURE_REQUEST_DISCRIMINATOR: u8 = 9u8;

pub const DISCLOSURE_REQUEST_MIGRATION_VERSION: u8 = 0u8;

impl DisclosureRequest {
	pub const LEN: usize = core::mem::size_of::<DisclosureRequestZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut DisclosureRequestZc),
	) -> Result<&mut DisclosureRequestZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = DISCLOSURE_REQUEST_DISCRIMINATOR;
			account.migration_version = DISCLOSURE_REQUEST_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(
		data: &[u8],
	) -> Result<&DisclosureRequestZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != DISCLOSURE_REQUEST_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != DISCLOSURE_REQUEST_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(
		data: &mut [u8],
	) -> Result<&mut DisclosureRequestZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != DISCLOSURE_REQUEST_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != DISCLOSURE_REQUEST_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl DisclosureRequest {
	pub fn find_pda(requester: &solana_pubkey::Pubkey, nonce: u64) -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&[
				"privacy-pool-request".as_bytes(),
				requester.as_ref(),
				&nonce.to_le_bytes(),
			],
			&crate::PRIVACY_POOL_PROGRAM_ID,
		)
	}

	pub fn create_pda(
		requester: &solana_pubkey::Pubkey,
		nonce: u64,
		bump: u8,
	) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&[
				"privacy-pool-request".as_bytes(),
				requester.as_ref(),
				&nonce.to_le_bytes(),
				&[bump],
			],
			&crate::PRIVACY_POOL_PROGRAM_ID,
		)
	}
}

/// Whether raw account bytes are stale for this contract: the envelope names this account's discriminator and carries a version older than
/// [`DISCLOSURE_REQUEST_MIGRATION_VERSION`]. Current or foreign bytes return false; decoding explains the difference.
///
/// Version 0 is the initial version, so no bytes can ever be stale.
pub fn disclosure_request_needs_migration(_data: &[u8]) -> bool {
	false
}

/// Why `DisclosureRequest::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum DisclosureRequestVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version predates this client: migrate the account on-chain, then retry.
	Stale { stored: u8 },
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for DisclosureRequestVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid DisclosureRequest account data"),
			Self::Stale { stored } => {
				write!(
					f,
					"migration version mismatch: expected 0, received {stored} (the data predates \
					 this client; migrate it by sending a transaction to the program, or decode \
					 it with a client generated from an older IDL)"
				)
			}
			Self::Future { stored } => {
				write!(
					f,
					"migration version mismatch: expected 0, received {stored} (the data was \
					 written by a newer program; upgrade this client)"
				)
			}
		}
	}
}

impl DisclosureRequest {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`DisclosureRequest::from_bytes`].
	pub fn try_from_bytes(
		data: &[u8],
	) -> Result<&DisclosureRequestZc, DisclosureRequestVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| DisclosureRequestVersionError::InvalidData)?;
		if account.discriminator != DISCLOSURE_REQUEST_DISCRIMINATOR {
			return Err(DisclosureRequestVersionError::InvalidData);
		}
		if account.migration_version > DISCLOSURE_REQUEST_MIGRATION_VERSION {
			return Err(DisclosureRequestVersionError::Future {
				stored: account.migration_version,
			});
		}
		Ok(account)
	}
}

#[cfg(test)]
mod disclosure_request_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<DisclosureRequestZc>()];
		data[..1].copy_from_slice(&[9]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let error = DisclosureRequest::try_from_bytes(&envelope(1 as u8))
			.err()
			.expect("a future envelope must fail");
		assert_eq!(error, DisclosureRequestVersionError::Future { stored: 1 });
		assert_eq!(
			DisclosureRequestVersionError::Future { stored: 1 }.to_string(),
			"migration version mismatch: expected 0, received 1 (the data was written by a newer \
			 program; upgrade this client)"
		);
		assert!(
			DisclosureRequest::try_from_bytes(&envelope(0 as u8)).is_ok(),
			"the current version must decode",
		);
	}
}
