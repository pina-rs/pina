//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct PoolVault {
	/// The pool's SOL custody. Data-less by design: every lamport in it is
	/// backed by an unspent note commitment in the tree.
	pub discriminator: u8,
	pub migration_version: u8,
	pub bump: u8,
}

pub const POOL_VAULT_DISCRIMINATOR: u8 = 2u8;

pub const POOL_VAULT_MIGRATION_VERSION: u8 = 0u8;

impl PoolVault {
	pub const LEN: usize = core::mem::size_of::<PoolVaultZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut PoolVaultZc),
	) -> Result<&mut PoolVaultZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = POOL_VAULT_DISCRIMINATOR;
			account.migration_version = POOL_VAULT_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(data: &[u8]) -> Result<&PoolVaultZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != POOL_VAULT_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != POOL_VAULT_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(
		data: &mut [u8],
	) -> Result<&mut PoolVaultZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != POOL_VAULT_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != POOL_VAULT_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl PoolVault {
	pub fn find_pda() -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&["privacy-pool-vault".as_bytes()],
			&crate::PRIVACY_POOL_PROGRAM_ID,
		)
	}

	pub fn create_pda(bump: u8) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&["privacy-pool-vault".as_bytes(), &[bump]],
			&crate::PRIVACY_POOL_PROGRAM_ID,
		)
	}
}

/// Whether raw account bytes are stale for this contract: the envelope names this account's discriminator and carries a version older than
/// [`POOL_VAULT_MIGRATION_VERSION`]. Current or foreign bytes return false; decoding explains the difference.
///
/// Version 0 is the initial version, so no bytes can ever be stale.
pub fn pool_vault_needs_migration(_data: &[u8]) -> bool {
	false
}

/// Why `PoolVault::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum PoolVaultVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version predates this client: migrate the account on-chain, then retry.
	Stale { stored: u8 },
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for PoolVaultVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid PoolVault account data"),
			Self::Stale { stored } => {
				write!(
					f,
					"migration version mismatch: expected 0, received {stored} (the data predates \
					 this client; migrate it by sending a transaction to the program, or decode \
					 it with a client generated from an older IDL)"
				)
			}
			Self::Future { stored } => {
				write!(
					f,
					"migration version mismatch: expected 0, received {stored} (the data was \
					 written by a newer program; upgrade this client)"
				)
			}
		}
	}
}

impl PoolVault {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`PoolVault::from_bytes`].
	pub fn try_from_bytes(data: &[u8]) -> Result<&PoolVaultZc, PoolVaultVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| PoolVaultVersionError::InvalidData)?;
		if account.discriminator != POOL_VAULT_DISCRIMINATOR {
			return Err(PoolVaultVersionError::InvalidData);
		}
		if account.migration_version > POOL_VAULT_MIGRATION_VERSION {
			return Err(PoolVaultVersionError::Future {
				stored: account.migration_version,
			});
		}
		Ok(account)
	}
}

#[cfg(test)]
mod pool_vault_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<PoolVaultZc>()];
		data[..1].copy_from_slice(&[2]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let error = PoolVault::try_from_bytes(&envelope(1 as u8))
			.err()
			.expect("a future envelope must fail");
		assert_eq!(error, PoolVaultVersionError::Future { stored: 1 });
		assert_eq!(
			PoolVaultVersionError::Future { stored: 1 }.to_string(),
			"migration version mismatch: expected 0, received 1 (the data was written by a newer \
			 program; upgrade this client)"
		);
		assert!(
			PoolVault::try_from_bytes(&envelope(0 as u8)).is_ok(),
			"the current version must decode",
		);
	}
}
