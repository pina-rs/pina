[package]
name = "security_11_admin_key_compromise_insecure"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
pina = { path = "../../../crates/pina", features = ["logs", "derive"] }
