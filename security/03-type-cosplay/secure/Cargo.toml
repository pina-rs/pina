[package]
name = "security_03_type_cosplay_secure"
version = "0.0.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["lib"]

[dependencies]
pina = { workspace = true, features = ["logs", "derive"] }

[lints]
workspace = true
