[package]
name = "security_10_sysvar_address_checking_secure"
version = "0.0.0"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[features]
bpf-entrypoint = []

[dependencies]
pina = { workspace = true, features = ["logs", "derive"] }

[lints]
workspace = true
